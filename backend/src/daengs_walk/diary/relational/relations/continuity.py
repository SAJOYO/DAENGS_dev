"""Endpoint equality is not evidence of continuous spatial coverage."""

from .contracts import slot


def evaluate(frame, previous):
    return slot(
        "not_implemented", "Requires intermediate route coverage of the same spatial relation"
    )

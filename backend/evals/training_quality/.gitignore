# 연기 시험(smoke) 산출물은 품질 데이터가 아니다 — git 에도 안 올린다
# (`evals/answer_quality/.gitignore` 와 같은 관례).
*smoke*

# review_*.md 는 판정 파일에서 언제든 다시 만들어진다 (`review --label <lap>`).
# 원본은 answers_*.jsonl + judgments_*.jsonl 이므로 결과물은 안 올린다.
review_*.md

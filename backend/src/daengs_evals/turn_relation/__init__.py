"""시험 ④ 앞 대화 기억 — Turn Resolver 가 현재 발화를 앞 대화에 맞게 잇는가.

dev 의 `orchestration/resolver.py`(#416 · D-069)는 라우터 **앞**에서 현재 발화를 NEW · FOLLOW_UP ·
CORRECTION · REPEAT 중 하나로 가르고, 확신이 0.6 아래면 잇기를 포기한다. D-069 가 이 모양을 고른
이유가 "관계를 값으로 남기면 랩에서 그 값만 보고 어디서 틀렸는지 셀 수 있다" 였다 — 그 값을 세는
자리가 여기다. **판정기(OpenAI)는 안 쓴다.** 정답은 대본에 사람이 적고, 관계는 리졸버가 낸 값 그대로다.

대본(`evals/turn_relation/scripts_v1.jsonl`) 한 줄 = 앞 턴 1~2개 + (대기 중 되묻기) + 현재 발화 +
기대 관계. 수집기는 오케스트레이터를 직접 불러(DB 없음) 리졸버를 감싼 기록기로 관계·확신·재진술을
받아 적는다. 모델 호출은 대본당 리졸버 1 + 라우터 1 + general 1 이 상한이고, 표지어가 없으면 리졸버는
모델 없이 NEW 를 낸다 — 그 **빠른 길이 놓치는 이어짐**(`followup_no_marker` 층)이 이 시험의 발견 하나다.

윤주님 `conversation_quality` 13건은 여기 안 넣었다 — 그 케이스는 내가 안 보고 만든 것이라 **홀드아웃**으로
남긴다.
"""

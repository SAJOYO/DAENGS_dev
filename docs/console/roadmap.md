# 관리자 콘솔 로드맵 — `frontend/app/console` · `/admin/*` · `/auth/*`

> **living doc.** "왜 필요한가 · 지금 무엇이 있고 · 무엇이 열려 있나" 만 말한다. 결정의 *왜* 는 `D-` 번호가
> 가리키는 곳에 있고 여기서 다시 쓰지 않는다 (`docs/life/roadmap.md` 와 같은 규칙).
>
> **갱신 규칙 한 줄** — 카드를 머지하면 해당 행의 상태를 바꾸고, 그 카드가 새로 미룬 것은 트랙에 한 줄 더한다.
> 상태 표기: ✅ 끝 · 🔵 진행 · ⬜ 열림 · ⏸ 보류(이유 필수) · 🚫 하지 않음.
>
> 작성 2026-09-03 (#174). 사람 결정 셋(Priority · 신고 경로 · 09-21 전 DB 변경)이 이날 닫혔다 (§7).

---

## 0. 한 장 요약

- **콘솔은 코드 하나, 배포 두 곳이다.** 로컬 `daengs.~/console`(개발 DB) · GCP `daengapp.~/console`(운영 DB).
  "GCP 용 콘솔" 을 따로 만들지 않는다 — **운영 값이 있는 쪽이 운영 콘솔**이고, 로컬 콘솔은 그 화면을 배포 전에
  확인하는 자리다. 로컬 콘솔의 중요성이 덜해 보이는 이유가 그것이고, 그건 문제가 아니라 배치다 (§2 배포).
- **콘솔이 점검 도구에서 운영 도구로 넘어가는 중이다.** 열린 메뉴 넷 가운데 둘(기능 점검 · 크롤)은 여전히 "우리
  기능을 우리가 두들겨 본다" 이지만, 상태(#180)와 관리자 계정(#207)은 운영 화면이다. 아직 없는 것은 **사용자가
  남긴 것**(신고 · 회원 · 대화)이고, 준비 중 2(회원 · 반려견 · 운영 지표)는 카드 문구만 있다 (§2).
- **AI 답변 신고는 앱에 이미 있는데 콘솔이 모른다.** 답변을 길게 누르면 **메일 앱**이 열려 `sajoyodaengs@gmail.com`
  로 간다 — 사용자 ID · request_id 없이 답변 원문만. 서버로 오게 하려면 "어느 답변인지" 가리킬 행이 필요하고,
  그것이 #131(대화 저장)의 `chat_turns` 다 (§3).
- **첫 카드는 신고가 아니라 그 앞의 것들이었다.** 감사 로그(#203)와 역할별 계정 발급(#207)이 끝났고, 남은 하나는
  **관리자가 회원 대화를 보는 범위**(새 `D-`, A0 — 사람 몫)다. 신고 화면(A1)은 그 위에 선다 (§5).
- **09-21 전에도 DB 를 바꾼다** (사람 결정, §7). 대가는 카드마다 두 DB(로컬 · GCP) 손 적용이다
  (`docs/deploy/roadmap.md` §2-5).

---

## 1. 왜 필요한가

"있으면 좋다" 가 아니라 **지금 그 일을 어떻게 하고 있나** 로 잰다. 콘솔이 없어서 안 되는 일은 없다 — 전부
psql · Gmail · SSH 로 된다. 콘솔이 바꾸는 것은 **그 일을 할 수 있는 사람의 수**와 **흔적이 남는지**다.

| 일 | 지금 어떻게 하나 | 콘솔이 있으면 | 트랙 |
| --- | --- | --- | --- |
| 신고 메일이 왔다 | Gmail 을 연다. 답변 원문은 있는데 누가 · 언제 · 어떤 질문에 · 무엇을 근거로 낸 답인지 없다. 재현하려면 점검 탭에 같은 질문을 넣어 본다 | 신고 목록 → 그 turn 의 질문 · 답변 · `public_response`(근거 · status) · 처리 상태. 같은 답변이 몇 번 신고됐는지 | A1 |
| 회원이 탈퇴 · 정지를 요청했다 | ✅ 이미 콘솔 (#211 · #212) | — | — |
| 팀원에게 콘솔 계정을 주고 싶다 | ✅ 이미 콘솔 (#207) | — | — |
| 누가 복호화를 봤나 | ✅ `admin_audit_log` 에 남는다 (#203 이 테이블을, #212 가 복호화 조회를). **보는 화면은 아직 psql 뿐** | 감사 로그 조회 화면 | A4-1 |
| 서비스가 살아 있나 | ✅ 이미 콘솔 (#180) | — | — |
| 사람들이 무엇을 묻나 | 모른다. D-037 이 원문 로깅을 금지했고, 허용된 메타데이터의 저장처도 없다 | 능력별 분포 · FAILED 비율 · 지연 (원문 없이) | B2 · B3 |
| 크롤이 실패했다 | ✅ 이미 콘솔 (#76) | — | — |
| 답변 품질을 점검한다 | ✅ 이미 콘솔 (#30 · #36 · #170 · #181) | — | — |

**발표(09-21)에서의 값**은 부차적이지만 있다 — 앱 데모 옆에 운영 화면이 있으면 "기능" 이 아니라 "서비스" 로 읽힌다.

---

## 2. 지금 상태 (2026-09-04, `origin/dev` `eb804cc` + #207 실측)

### 메뉴 7개

| 메뉴 (`console/page.tsx`) | 카드 권한 | 화면 | 뒤의 API | 상태 | 어긋난 것 |
| --- | --- | --- | --- | --- | --- |
| 상태 | `read` | `/console/status` — 항목 여덟, 30초 폴링. `absent`(이 환경엔 없음)와 `down`(죽음)의 색·문구가 다르다 | `/admin/status`(READ) | ✅ #180 | 사람이 손댈 자리는 없다 — 읽기 전용이다 |
| 기능 / 검색 점검 | `read` | `/console/search` — 탭 4: 훈련 RAG · 생활 RAG(`/life/ask` + `/life/walk-conditions`) · 피부 스크리닝 · 어시스턴트(`/assistant/query`) | `/training/chat`(SEARCH_INSPECT) · `/life/ask` `/life/walk-conditions`(READ, 앱 회원도) · `/screen/v1/screen`(**무인증**) | ✅ #30 · #36 · #170 · #181 · #213 | 산책 기록 · gait · place · journey 탭 없음 (C4). 어시스턴트 탭은 `RoutePlan` 이 공개 응답에 없어 라우터 종류 · 모델 이름을 못 보여 준다 |
| 수집 / 크롤 | `read` (트리거 `ops:write`) | `/console/crawl` — 소스별 마지막 실행, 5초 폴링, 수동 트리거 | `/admin/crawl` | ✅ #76 · #95 | **GCP 에서는 반쪽이다** — 크롤러가 안 떠서(`deploy/roadmap.md` §2-4) 트리거는 202 만 주고 아무 일도 없다. 표는 09-02 덤프 시점 행 |
| 지식 베이스 | `kb:write` | 없음 | 없음 | ⬜ 준비 중 | 설명문 "청크와 그래프 추출" — 그래프는 폐기된 GraphRAG (`training/decision_graphrag_abandoned_0824.md`). 훈련 RAG 는 승인 매니페스트 14문서 **재적재 금지**(`training/rag-demo.md`), 생활 RAG 적재는 개발 PC CLI(GPU, 55분). **업로드 UI 는 지금 성립하지 않는다** (§6) |
| 관리자 계정 | `admin:manage` | `/console/admins` — 목록 · 발급 · role 변경 · 정지/해제 | `/admin/admins`(ADMIN_MANAGE) | ✅ #207 | 본인 비밀번호 변경 화면이 없다 — 초기 비밀번호는 발급자가 전달한다 |
| 회원 · 반려견 | `read` | `/console/users` — 검색 · 마스킹 상세 · 반려견 · 원문 보기(`pii:read`) · 정지/해제(`ops:write`) | `/admin/app-users`(READ · PII_READ · OPS_WRITE) | ✅ #211 · #212 | **목록이 없다** — 검색만 된다 (A2c) |
| 운영 지표 | `metrics:read` | 없음 | 없음 — 저장되는 지표 자체가 없다 | ⬜ 준비 중 | D-037 이 원문을 금지했고, 허용된 메타데이터의 저장처는 미정 |

> **생활 RAG 탭이 읽어야 하는 계약 — `detail` 이 두 모양이다** (#213). `/life/ask` 는 스스로 이름
> 붙인 결과를 `{code, message}` **객체**로 주고(#177 의 기권 `no_evidence` · 경계 거절
> `*_boundary`), 그 전부터 있던 것만 문자열이다. `ApiError.message` 는 문자열일 때만 서버 문구를
> 담으므로(`lib/api.ts` 의 `detailOf`) 객체형은 `ApiError.body` 에서 읽는다. 안 읽으면 **기권이
> "라우트 없음"으로, 응급 거절이 "요청을 처리하지 못했습니다"로** 표시된다 — 둘 다 실제로 그랬다.
> 백엔드 쪽 같은 판단은 `orchestration/adapters/life.py` 의 `_outcome()` 이고 **둘은 같이 움직인다.**

### 인증 · 권한 · 운영 뼈대

| 축 | 상태 | 근거 |
| --- | --- | --- |
| 로그인 · refresh · `/auth/me` · 라우트 가드 | ✅ httpOnly 쿠키 JWE, 5분 access, 401 → 조용한 재발급 | #11 · #29 · D-015 |
| role 5단계 (ADMIN · OPERATOR · CURATOR · ANALYST · VIEWER) | ✅ 콘솔에서 발급·변경한다 (#207). `Perm.ADMIN_MANAGE` 가 처음으로 실제로 갈리는 자리 — ADMIN 만 이 메뉴가 보인다 | D-014 |
| 최초 계정 | `uv run seed-admin` 이 SQL 출력 → psql 손 적용. **두 번째부터는 콘솔**이다 (#207). GCP 는 09-02 덤프의 `admin_users` 그대로 | D-015 |
| 로그인 잠금 | `(login_id, IP)` 5회/10분, **프로세스 메모리** — 재시작에 풀린다. #207 로 계정이 여럿이 될 수 있게 됐지만 잠금 자체는 그대로다 (Redis 이관은 D3) | `services/login_attempts.py` |
| 로그 | `logger.info` 가 어디에도 안 남는다 | draft 카드 `fix: 앱 로그가 어디에도 남지 않는 문제` (Hold P3) |
| 감사 로그 | ✅ `admin_audit_log` (#203). 남는 것은 로그인 시도 넷과 계정 발급·role 변경·정지·해제(#207). **보는 화면은 아직 없다** (A4-1) — 그 선(감사 로그 = DB 테이블 / 운영 로그 = 파일)은 §6 | #203 · #207 |
| 앱 회원 API | `/auth/app/*`(카카오) · `/app/pets` · `/app/walks` · `/app/gait` — 전부 **본인 것만**. 관리자용 목록 조회 없음. `current_app_user` 는 관리자 토큰을 401 로 막는다 | #19 · #88 · D-043 · `core/deps.py` |

### 배포

| | 로컬 서버 | GCP |
| --- | --- | --- |
| 주소 | `daengs.weareithero.cloud/console` | `daengapp.weareithero.cloud/console` |
| DB | 개발 (`vectordb`, 팀 공용) | 운영 (09-02 덤프 스냅샷, 복제 없음) |
| 배포 | dev 머지 → 러너 자동 | dev→main 스냅샷 → `git push gcp main` → VM pull (`deploy/runbook.md` §6) |
| 크롤러 | 뜬다 (코퍼스 정본) | 안 뜬다 |
| 역할 | 화면을 배포 전에 확인 | **운영 콘솔** |

---

## 3. 신고 경로 — 지금과 목표

```
지금 (DAENGS_APP ui/chat/ReportAnswer.kt)
  답변 길게 누르기 → 메일 앱 → sajoyodaengs@gmail.com
  본문 = 신고 이유 + 답변 원문.  사용자 ID · 토큰 · request_id · 질문 없음
  ("서버 대화 저장은 v0.0.0 범위가 아니므로" — 앱 주석)

목표 (2026-09-03 사람 결정 — 둘 다 유지, §7)
  답변 길게 누르기 → POST /app/reports {turn_id, reason}      ← 앱 v0.0.1 (#131 의 chat_turns 가 있을 때)
                  → answer_reports → GET /admin/reports → /console/reports
  메일 경로는 남긴다 — 구버전 앱 · 저장 안 된 대화(v0.0.0 무상태 경로)용.
  콘솔은 API 로 온 것만 본다. 메일 건은 콘솔에 옮겨 적지 않는다 (§6).
```

**신고 행이 가리키는 것** — `chat_turns.id`. 거기에 `user_content` · `assistant_content` · `public_response`
(status · citations · 능력) · `request_id` · `agent_categories` 가 있다 (#131 `db/init/07_chats.sql`). 콘솔 상세
화면은 이 행을 그대로 보여 주면 된다 — 신고 테이블에 답변을 복사하지 않는다 (§6).

**넘어야 할 것 — 권한.** D-048 은 "사용자가 다시 여는 제품 데이터" 로서의 저장을 허용했지 **관리자가 회원 대화를
본다** 를 정하지 않았다. 신고된 turn 만 보는가 · 그 세션 전체를 보는가 · 누가(어느 `Perm`) 보는가 · 사용자에게
무엇을 고지하는가 — 새 `D-` 가 필요하고 A0 이 그것을 낸다 (§7). 본 사실은 A4 감사 로그에 남긴다.

**앱 쪽 동반** — `ReportAnswer.kt` 가 turn id 를 알아야 한다. 앱 카드는 DAENGS_APP 이 연다. 이 문서는 API 계약
(요청 필드 · 응답)만 A1 에 적는다.

---

## 4. 트랙

각 행: 무엇 · 왜 · 전제 · 크기(Project Size 기준) · 상태. **모든 행은 번호 · PR · 파일을 하나 이상 가리킨다.**
이름(`answer_reports` · `/admin/app-users` 등)은 **안**이다 — 카드가 열릴 때 정한다.

### A. 운영 — 사용자가 남긴 것을 본다

| # | 무엇 | 왜 | 전제 | 크기 | 상태 |
| --- | --- | --- | --- | --- | --- |
| A0 | **관리자가 회원 대화를 보는 범위 결정** (`D-`) — 신고된 turn 만 / 세션 / Perm(`pii:read` 로 묶을지 새 `report:read`) / 고지 문구 | §3. 코드 없이 결정만. A1 이 이 번호를 인용한다 | #131 의 D-048 | XS | ⬜ 사람 몫 (§7) |
| A4 | **감사 로그 테이블** `admin_audit_log`(누가 · 언제 · 무엇을 · 대상 id) + 기록 헬퍼. 첫 소비자는 **로그인 시도**(성공 · 실패 · 정지 거부, 2026-09-04 사람 결정)이고 A2 의 복호화 조회가 뒤따른다 | Hold 로그 카드 메모 "감사 로그는 DB" · D-012(복호화는 `core/crypto.py` 한 곳) | 없음 — 두 DB 손 적용 | S | ✅ #203 (개발 DB 적용됨 · **운영 DB 는 2026-09-05**) |
| A5 | **감사 로그 보존 · 정리** — 로그인 시도까지 들어와 행이 빨리 는다. 지우는 주기와 방법(월별 파티션 / 오래된 행 삭제 / 그대로 두기) | #203 이 일부러 안 정했다 — 얼마나 빨리 느는지 보고 정하는 것이 맞아서 | #203 뒤 실제 증가량 | XS | ⏸ 증가량을 본 뒤 |
| A3 | **관리자 계정 관리** — `GET/POST /admin/admins` · `PATCH`(role · status) + `/console/admins`. `seed-admin` 은 최초 1회로 남겼다 | D-014 5단계가 정의만이었다. 이제 `pii:read` 를 못 가진 계정을 실제로 만들 수 있어 A2b 의 가리기가 검증된다 | A4 ✅ | S | ✅ #207 (DB 변경 없음 — `admin_users` 는 두 DB 에 이미 있다) |
| A3-1 | **본인 비밀번호 변경** — 발급받은 사람이 초기 비밀번호를 바꾸는 길 (`PATCH /admin/admins/me/password`) | #207 이 초기 비밀번호를 발급자가 전달하는 것으로 두고 미뤘다. 받은 사람이 못 바꾸면 그 값이 계속 팀 채널에 남는다 | #207 | XS | ⬜ |
| A4-1 | **감사 로그 조회 화면** `/console/audit` — 로그인 시도 · 계정 변경 · **개인정보 원문 조회** | #203 이 테이블만 만들었고 보는 길이 psql 뿐이다. #207 · #212 가 기록을 여덟 종류로 늘렸고, 그중 `pii_revealed` 는 **이 테이블이 생긴 첫째 이유**다 (§1) | #203 · #207 · #212 | S | ⬜ |
| A2a | **회원 조회** — `GET /admin/app-users?email=`·`?kakao_id=` · 상세(마스킹 · 반려견) · `/console/users`. 전부 `READ`, 쓰기 없음 | §1 둘째 줄. `console/page.tsx` 의 준비 중 카드 | 없음 — DB 변경도 없다 | S | ✅ #211 |
| A2b | **원문 복호화와 상태 변경** — `GET /admin/app-users/{id}/pii`(`pii:read`, 부를 때마다 `admin.app_user.pii_revealed`) · `PATCH status`(`ops:write`) + 세션 끊기 | 감사에 남길 가치가 있는 것은 "가려서 봤다"가 아니라 "원문을 열어 봤다" — 그 선에서 A2 를 갈랐다 | A4 ✅ · A3 ✅ · #211 ✅ | S | ✅ #212 (`Perm.PII_READ` 의 첫 사용처. 탈퇴 회원의 상태는 관리자가 못 되돌린다) |
| A2c | **회원 목록** — 검색 말고 훑어보는 길. **권한을 같이 정해야 한다** | #211 이 일부러 안 만들었다 — `/admin/app-users` 가 `Perm.READ` 라 목록을 열면 VIEWER 까지 전 회원을 넘겨보게 된다. "메일 보낸 사람 찾기"와 "회원 훑기"는 다른 일이다 | 사람 결정(권한) — §7 | XS | ⏸ 필요해지면 |
| A1 | **AI 답변 신고** — `answer_reports`(turn_id FK · app_user_id · reason · status open/reviewed/dismissed · reviewed_by) · `POST /app/reports` · `GET /admin/reports` · `/console/reports` 목록 + 상세(turn 원문 · `public_response`) + 처리 | §3 · §1 첫 줄 | #131 ✅ 머지됨 · **운영 DB 적용**(`main` 에 아직 없다) · A0 · A4 · 앱 카드(DAENGS_APP) | M | ⬜ |

### B. 관측 — 시스템이 남긴 것을 본다

| # | 무엇 | 왜 | 전제 | 크기 | 상태 |
| --- | --- | --- | --- | --- | --- |
| B1 | **상태 페이지** `/console/status` — 항목 여덟(`db` · 임베딩 예열 · `screening` 가중치 · Redis 일 예산 · place · journey · 크롤 · gait). 항목별 짧은 timeout 이고 하나가 죽어도 200. 읽기 전용, DB 무변경 | §1 "살아 있나". 지금은 첫 요청이 알려 준다 | `GET /admin/status` 하나 (BE) | S | ✅ #180 |
| B2 | **요청 메타데이터 저장** — D-037 허용 열만(request_id · principal 종류 · 능력 · status · reason code · elapsed_ms) → 테이블 or 파일 집계. **저장처는 사람 결정** | `운영 지표` 카드가 보여 줄 것이 없다. Hold 로그 카드가 전제 | 로그 카드 해제 · §7 | M | ⬜ 저장처 결정 뒤 |
| B3 | **대화 집계** — 세션 수 · `agent_categories` 분포 · `assistant_status` FAILED 비율 · 요약 생성 수. 제품 테이블 집계, 원문 노출 없음 | #131 뒤 가장 싸게 "사람들이 무엇을 묻나" 를 보는 길 | #131 ✅ 머지됨 — 운영 DB 적용만 남음 | S | ⬜ |
| B4 | `운영 지표` 화면 `/console/metrics` — B2 · B3 를 한 화면에 | 준비 중 카드 | B2 or B3 | S | ⬜ |

### C. 점검 도구 — 지금 메뉴 손질

| # | 무엇 | 왜 | 전제 | 크기 | 상태 |
| --- | --- | --- | --- | --- | --- |
| C1 | **`/assistant/query` 점검 탭** — 앱과 같은 경로 · 같은 축소 응답, `context`(location · active_dog_id) 입력, `results[]` status 와 라우팅 결과 표시 | 앱 ChatScreen 의 실제 경로인데 콘솔이 안 두들긴다. A0 스모크(#169)를 사람이 curl 로 했다 | 없음 (FE 만) | S | ✅ #181. `RoutePlan` 이 공개 응답에 없어 **라우터 종류(deterministic/llm)·모델 이름은 화면에 안 뜬다** — 응답 계약을 늘려야 하는 별도 카드감 |
| C2 | **준비 중 카드 문구 현실화** — 지식 베이스 설명에서 "그래프" 제거 · 크롤 카드에 "GCP 에서는 트리거 없음" 안내 · `href` 리터럴 유니온에 새 라우트 | §2 표 "어긋난 것" | 없음 | XS | ✅ #179 |
| C3 | 생활 파트 A4(이름 정리 `/life/ask` · `/life/walk-conditions`)의 **콘솔 호출 경로 동반** | `docs/life/roadmap.md` A4 가 "콘솔 점검 탭 호출 경로 동반" 을 명시 | life A4 카드 안 | — | ✅ #176 |
| C4 | 산책 기록(`/app/walks`) · gait · place · journey 점검 탭 | life A8 🚫 의 재개 조건 "콘솔에 산책 점검 탭이 생기면". 관리자 토큰으로는 본인 소유 API 를 못 부른다(`current_app_user`) → **테스트 회원 토큰**이 필요 | §7 | M | ⏸ 앱 API 가 본인 것만 주는 구조라 형태부터 정해야 |

### D. 보안 후속 — 콘솔 탭과 백엔드가 같이 간다

콘솔 탭만 가리면 "API 는 열려 있는데 화면만 안 보이는 계정" 이 생긴다 (`inspect-tabs.tsx` · `console/page.tsx` 주석).
그래서 **가리는 PR 과 인증을 거는 PR 이 같은 PR** 이어야 한다.

| # | 무엇 | 왜 | 소유 | 상태 |
| --- | --- | --- | --- | --- |
| D1 | `/screen/*` 인증 + rate limit — 백엔드에 걸고 같은 PR 에서 `inspect-tabs.tsx` 피부 탭을 권한으로 가림 | `docs/orchestration/architecture.md` 준비도 표 "보안 후속은 별도" | 스크리닝 파트 + 콘솔 | ⬜ |
| D2 | `/gait/*` 무인증 → `/app/gait/*` 전환 뒤 닫기 | `docs/gait/worklog.md` 미해결 5 | gait 파트 | ⬜ 링크만 |
| D3 | 로그인 잠금을 Redis 로 — 워커를 늘리거나 재시작을 견뎌야 할 때 | `login_attempts.py` 마지막 문단 | 콘솔 | ⬜ 조건부 |

### E. 로그 — 이 문서 밖

draft 카드 `fix: 앱 로그가 어디에도 남지 않는 문제`(Hold P3) 가 소유한다. 콘솔에서 운영 로그를 보는 것은 🚫 (§6).
그 카드가 풀려야 B2 가 선다.

---

## 5. 순서와 의존

```
── 지금 (DB 변경 허용 — 2026-09-03 사람 결정. 카드마다 두 DB 손 적용) ────────────────────
C2 문구 ✅ → B1 상태 페이지 ✅ → C1 assistant 탭 ✅   (셋 다 DB 없음 · FE/읽기 전용 — 먼저 갔다)
A4 감사 로그 ✅ → A3 계정 관리 ✅ → A2a 회원 조회 ✅ → A2b 복호화·상태 변경 ✅
                                                        (A 트랙. A4 가 앞인 이유는 아래)
남은 A 트랙: A0 결정(사람) → A1 신고 · A3-1 비밀번호 변경 · A4-1 감사 로그 화면 · A5 보존
※ A2a 는 A3 을 기다릴 필요가 없었다 — 나가는 값이 전부 마스킹이라 `READ` 로 열리고,
   `pii:read` 가 실제로 갈려야 하는 것은 A2b 다.

── 운영 DB 적용 뒤 (#131 은 dev 에 머지됨. `main` 에 `07_chats.sql` 이 아직 없다) ──────────
A0 결정(D-) → A1 신고  ─┐
B3 대화 집계           ─┴→ B4 운영 지표 화면
D1 screen 인증 은 스크리닝 파트 일정에 (병렬)

── 조건부 ─────────────────────────────────────────────────────────────────────────────
B2 요청 메타데이터 = 로그 카드(Hold) 해제 + 저장처 결정 뒤.  C4 = 테스트 회원 형태 결정 뒤.  D3 = 워커 늘릴 때.
```

- **A4(감사 로그)가 A 트랙 맨 앞인 이유** — A2 의 복호화 · A3 의 발급 · A1 의 신고 처리가 전부 "누가 했나" 를
  남겨야 하는 일이다. 뒤에 넣으면 세 카드를 다시 연다.
- **A3 이 A2 앞인 이유** — 복호화 화면을 열기 전에 그것을 못 보는 계정(VIEWER · ANALYST)이 실제로 있어야 `pii:read`
  가리기가 검증된다. #207 전까지는 전원 ADMIN 이라 권한 코드가 한 번도 실행되지 않았다.
  **정확히는 A2b 앞이다** — A2a(#211)는 마스킹만 하므로 그 계정이 없어도 성립한다.
- **A1 이 맨 뒤인 이유** — 전제가 셋(#131 · A0 · A4)이고 앱 카드가 동반한다. 그 사이 신고는 메일로 계속 온다 (§3).
- **B1 · C1 · C2 를 먼저 하는 이유** — DB 도 결정도 필요 없고, 09-21 발표 화면에 바로 보인다.
- Iteration 은 3~4일 (`collaboration.md`). 행 하나 = 카드 하나. 크기 M 은 둘로 쪼개서 올린다.

---

## 6. 하지 않기로 한 것 (이유는 번호로)

| 것 | 이유 |
| --- | --- |
| **GCP 전용 콘솔 분기** (다른 메뉴 · 다른 빌드) | 코드 하나 · 배포 두 곳 (§0). 환경 차이는 API 가 알려 주는 것(크롤러 없음 등)으로 화면이 안내한다 — C2 |
| **콘솔에서 운영 로그 보기** | Hold 로그 카드 메모 (2026-08-26 사람) — 운영 로그는 파일, 화면에 띄우면 "무엇을 로그에 넣지 않을 것인가" 가 지금보다 빡빡해진다. 감사 로그(A4)는 로그가 아니라 데이터 |
| **지식 베이스 업로드 UI** | 훈련 RAG 재적재 금지(`training/rag-demo.md`) · 생활 RAG 적재는 GPU/개발 PC (RAG-043 ④ · `deploy/roadmap.md` §2-4). 재개 조건: `deploy/roadmap.md` §7-1(크롤러 · 적재 VM 이전) 뒤 |
| **신고 테이블에 답변 원문 복사** | 원문은 `chat_turns` 한 곳 (D-048). 복사하면 탈퇴 시 명시 삭제 대상이 둘이 된다 |
| **메일 신고를 콘솔에 옮겨 적기** | 2026-09-03 사람 결정 "둘 다 유지" — 콘솔은 API 로 온 것만 |
| **질문 원문 지표** | D-037. B2 는 허용 열만 |
| **콘솔에서 `rag load` · 적재 트리거** | 크롤 카드가 "수집까지만" 인 이유와 같다 (RAG-044 ⑤) — 적재는 사람이 판단하고 GPU 가 필요 |

---

## 7. 열린 결정 (사람 몫)

닫힌 것은 날짜와 함께 남긴다.

- ~~Priority~~ — 2026-09-03 닫힘: **P2**.
- ~~신고 경로~~ — 2026-09-03 닫힘: **둘 다 유지** — 서버 API(A1)를 만들되 메일 경로도 남긴다. 콘솔은 API 로 온 것만 본다.
- ~~09-21 전 DB 변경~~ — 2026-09-03 닫힘: **허용**. 생활 파트(`life/roadmap.md` §4 "DB 무변경")와 다르다 — 대가는
  카드마다 두 DB 손 적용.
- **A0 — 관리자가 회원 대화를 보는 범위** — 신고된 turn 만 / 세션 전체 / Perm / 고지 문구. A1 착수 전에 `D-` 로.
- **회원 목록을 열지** (A2c) — #211 은 검색만 만들었다. 목록은 `Perm.READ` 인 지금 권한으로는 VIEWER 까지
  전 회원의 (가려졌더라도) 개인정보를 넘겨보게 되므로, 열려면 **권한을 같이 정해야 한다**.
- **B2 저장처** — 요청 메타데이터를 테이블에 쌓을지, 파일 로그를 집계할지. 로그 카드(Hold)와 같이 본다.
- **역할 발급** — 팀원 계정을 ADMIN 공유에서 역할별(OPERATOR · CURATOR …)로 나눌지. **화면은 생겼다**(#207) —
  이제 고르기만 하면 된다. 나누면 A2(복호화 가리기)가 실제로 검증되고, 안 나누면 A2 는 지금처럼 전원 통과다.
- **C4 테스트 회원** — 관리자 토큰으로 본인 소유 API 를 못 부르는 구조에서 산책 기록 등을 점검하려면 무엇으로 부를지.
- **GCP 콘솔의 크롤 메뉴** — 숨길지, "트리거 없음" 안내만 할지 (C2 는 안내로 제안).
  #180 뒤로는 **감출 수 있게 됐다** — `GET /admin/status` 의 `crawl` 항목이 워커가 없는 환경을
  `absent` 로 알려 준다(브로커에 `active_queues` 로 묻는다). 고르는 것은 여전히 사람 몫이고,
  감추기로 하면 새 카드가 그 값을 읽어 카드/버튼을 가린다.

"""훈련 RAG 답변의 **근거 충실도**를 잰다 (D-060). 사람 라벨 없이 시작한다.

    collect.py     질문을 훈련 RAG 에 먹여 **청크 본문까지** 덤프한다
    judge.py       루브릭 판정기 — 판정자가 그 청크를 보고 채점한다
    anchors.py     코드가 지은 문항과 기대 판정 — judge 를 재는 자
    __main__.py    check-anchors · score · review · agreement

────────────────────────────────────────────────────────────────────────────
세 번째 잣대다 — 앞의 둘과 섞지 않는다
────────────────────────────────────────────────────────────────────────────
    tools/answer_quality/ (#277)        오케스트레이터 답변률·품질    질문 + 답변만
    daengs_life/…/judge.py (#305)       Life RAG answers_question     질문 + 답변만
    여기                                훈련 RAG 근거 충실도          + **검색된 청크**

`answer_quality/__init__.py` 가 라우팅 골드와 답변 품질을 두고 *"두 잣대를 한 파일에 섞지
않는다"* 고 한 것과 같은 규칙이다. 앞의 둘이 근거를 안 보여주는 것은 각각 이유가 있고
(#277 은 라우팅 정보를 안 주려고, #305 는 덤프에 본문이 없어서), **훈련 RAG 는 반대로
보여줘야 한다** — 재려는 것이 *"답이 그 청크에 붙어 있는가"* 라서다.

────────────────────────────────────────────────────────────────────────────
순서
────────────────────────────────────────────────────────────────────────────
    1. collect      개발 PC 에서 서버 DB 를 보고 돈다 (DAENGS_DB_* 필요)
    2. check-anchors  judge 가 쓸 만한지 먼저 잰다. **통과해야 score 가 돈다**
    3. score        덤프를 채점한다. `decision != ANSWER` 인 행은 뺀다 (D-060 ⑤)
    4. review       사람이 라벨링할 문항을 고른다 — judge 가 자신 없어 하는 자리
    5. agreement    사람 라벨과의 일치율. **이것이 승격의 조건**이다 (RAG-007: 30개)

⚠ **아직 지표가 아니다.** 사람 라벨 30개와의 일치율 전에는 `score` 의 수를 어디에도
승격하지 않는다. Life 쪽이 지금 라벨 4개에서 멈춰 있다 (`RAG-075`).

⚠ **`daengs_training` 은 이 패키지를 모른다.** judge 코드가 서빙 패키지에 안 들어가는
것이 의도다 — 그래서 `tools/` 다. 반대 방향(이쪽이 `daengs_training` 을 부르는 것)만 있다.
"""

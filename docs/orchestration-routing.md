# 오케스트레이터 라우팅 정책

`/assistant/query` 로 들어온 요청이 어느 능력으로 가는지를 정하는 정책 문서입니다.
구조는 [orchestration-architecture.md](orchestration-architecture.md), 계약 모양은
[orchestration-contracts.md](orchestration-contracts.md) 가 원본입니다. 표기는 같습니다
(CONFIRMED / OPEN / PENDING).

## 1. 결정적 라우팅의 경계 (CONFIRMED)

라우팅 경로는 둘입니다: 결정적(deterministic) 경로와 의미(semantic) LLM 경로.
`RoutePlan.router` 가 어느 쪽이 판단했는지를 남깁니다 (contracts §2).

**결정적 라우팅은 기계가 읽는 명시적 신호에만 허용됩니다:**

- `requested_capability` — 클라이언트가 능력을 명시한 경우
- 구조화된 UI/액션 메타데이터 — 어느 화면·버튼에서 온 요청인지
- 명시적 source/action 식별자
- 의미가 모호하지 않은, 이미 구조화된 컨텍스트

**자연어 키워드 하드 라우팅은 결정적 경로가 아닙니다.** "훈련"·"짖음"·"배변" 같은 단어가
보인다고 Training 으로 직행하는 규칙을 만들지 않습니다. "짖음 때문에 산책을 못 가요"는
Training 인지 Walk 인지 둘 다인지 단어로는 갈리지 않습니다 — 자유 자연어의 의미 판단은
**전부 의미 라우팅 경로(§2)로** 갑니다. 키워드 규칙은 처음엔 잘 맞다가 오답을 조용히
쌓고, 규칙 목록이 늘수록 서로 부딪히는데 그 충돌을 심판할 기준이 없습니다.

## 2. 의미 라우팅 — LLM 폴백

결정적 신호가 없으면(대부분의 자연어 입력) LLM 라우터가 질의를 읽고 RoutePlan 을
만듭니다. 스키마 제약 출력(EXECUTE/CLARIFY/HANDOFF + CapabilityRequest 목록)으로 받고,
스키마 검증에 실패한 출력은 그대로 실행하지 않습니다 — 실패 시 처리(재시도 후 CLARIFY 로
떨어뜨릴지 등)는 구현 카드의 몫이되, "검증 실패를 조용히 실행"만은 금지입니다.

라우터는 **분류기이지 답변자가 아닙니다.** 라우터가 도메인 답을 직접 생성하는 순간
"도메인 안전·거절은 능력이 소유한다"(architecture §논리 오케스트레이션)가 깨집니다.

## 3. CLARIFY 와 HANDOFF 의 뜻

| mode | 뜻 | 예 |
| --- | --- | --- |
| EXECUTE | 능력 실행 계획이 섰다 | "우리 개 입질 교육 방법 알려줘" → Training |
| CLARIFY | **실행 없이** 사용자에게 부족한 정보를 되묻는다 | "오늘 산책 어때?" 인데 위치 컨텍스트가 없음 |
| HANDOFF | 대화로 처리하지 않고 전용 플로우로 안내한다 | "피부에 뭐가 났는데 봐줘" → Skin 업로드 UI 로 |

- CLARIFY 는 2026-08-22 멘토링에서 합의된 원칙의 계승입니다 — **도구가 직접 되묻지 않고
  오케스트레이터가 후속 질문을 담당한다.** 능력이 `need_input` 성격의 결과를 내면
  그것을 사용자에게 묻는 것은 그래프의 일입니다.
- HANDOFF 는 실패가 아닙니다. multipart 이미지·영상 워크플로(Skin·Gait)는 전용 API 에
  남는다는 확정 경계(architecture §논리 오케스트레이션)의 라우팅 쪽 표현입니다.
  대상 플로우 식별자를 `RoutePlan.handoff.target` 에 담아 프론트가 이동시킬 수 있게 합니다.

## 4. 라우터 모델 — 미정, 벤치마크로 정한다 (CONFIRMED)

**LLM 라우터의 모델은 의도적으로 미정입니다.** 다음 둘은 선정 이유가 되지 않습니다:

- "DAENGS 가 지금 Gemini 를 쓰니까 Gemini" — 재직 효과(incumbency)는 근거가 아닙니다.
- "예전 다른 생성 실험에서 Qwen 이 빨랐으니까 Qwen" — 무관한 과제의 속도는 근거가 아닙니다.

선정은 나중의 **공정 벤치마크**로 합니다. 측정 항목:

| 항목 | 무엇을 재나 |
| --- | --- |
| 라우팅 정확도 | 단일 능력 질의를 맞는 능력으로 보내는가 |
| 다중 능력 재현율 | 두 능력이 필요한 질의에서 둘 다 잡는가 |
| 스키마 유효성 | 출력이 RoutePlan 스키마를 통과하는 비율 |
| 안전 결정적 오라우팅 | 의료·안전 질의를 엉뚱한 데로 보내는 사고율 |
| CLARIFY 정밀도 | 되물을 때만 되묻는가 (과잉 CLARIFY 는 UX 비용) |
| warm p50/p95 지연 | 라우터는 모든 대화 요청의 앞단에 선다 |
| 자원/비용 특성 | 서버 상주 RAM · API 비용 |

**선정 규칙: 품질·안전 게이트를 통과한 모델들 중에서만 가장 빠른 것을 고릅니다.**
빠른데 게이트를 못 넘는 모델은 후보가 아닙니다. 벤치마크 셋 구성과 게이트 임계값은
벤치마크 카드에서 정합니다 (FOLLOW-UP).

## 5. 능력 가용성과 v1 범위

라우터가 EXECUTE 로 보낼 수 있는 대상은 v1 에서 **Training · Life · Walk** 뿐입니다
(architecture §v1 범위 — 능력별 현실은 그 문서 §능력 현실 표).

- **Skin · Gait 로는 EXECUTE 하지 않습니다.** 대화에서 그 도메인이 감지되면 HANDOFF 입니다.
  배포도 profile 뒤라 기본 꺼져 있습니다 (D-024 · D-029) — 라우팅이 가능해도 실행 대상이
  없는 상태입니다.
- Gait 가 미래에 들어오면 동기 EXECUTE 가 아니라 CapabilityResult 의 PENDING + job
  메타데이터 경로(contracts §4)입니다 — 추론이 분 단위입니다.
- 능력이 일시적으로 죽어 있을 때(예: Training 호스트 프로세스 다운) 라우팅을 어떻게
  degrade 할지는 구현 카드의 몫입니다. 계약상 그 실패는 REFUSED 가 아니라 ERROR 입니다
  (contracts §6 불변식 1).

## 6. 미결 사람 결정 (OPEN)

아래는 **사람이 정하기 전에 구현하면 안 되는** 항목입니다. 저장소 어디에도 승인 기록이
없음을 확인했습니다 (2026-08-30 기준 — LangGraph/`/assistant` 관련 기존 결정 없음).
번호(D-0xx)는 결정된 뒤에만 받습니다 — 선호안이 있다는 이유로 번호를 붙이지 않습니다.

| # | 미결 항목 | 왜 사람 결정인가 |
| --- | --- | --- |
| O-1 | **오케스트레이터에서 Life 를 부르는 정확한 경계** — `/ask` HTTP 재호출인가, 같은 프로세스 함수 호출인가 | D-021 이 미뤄 둔 "`/ask` 를 별도 프로세스로 뗄지"와 얽혀 있어, 여기서 정하면 그 결정을 선점합니다 |
| O-2 | **`/assistant/query` 의 인증 정책** — 앱 회원 전용인가, 관리자도 쓰는가, 익명 데모 경로를 둘 것인가 | Training `/training/chat` 은 지금 관리자 게이트(#30)입니다. 대화 진입점의 대상을 넓히는 것은 보안 경계 변경입니다 |
| O-3 | **Life 의 거절 시맨틱 부재를 어떻게 다룰 것인가** — Life 에 거절 계약을 만들 것인가, 오케스트레이터 단에서 다른 취급을 할 것인가 | Life 는 지금 REFUSED 를 낼 방법 자체가 없습니다. 계약을 지어내는 것은 도메인 소유 원칙 위반입니다 |
| O-4 | **반려견 컨텍스트 정책** — `active_dog_id` 만 넘길 것인가, `dog_profile` 스냅샷인가, 다견 처리 규칙은 | 2026-08-22 멘토링의 "추가 확인 후보"였고 아직 결정 기록이 없습니다 |
| O-5 | **최상위 집계 규칙** — 능력별 status 들을 AssistantResponse.status 하나로 접는 규칙 | contracts §5. 부분 성공의 사용자 표현은 제품 결정입니다 |
| O-6 | **능력별 인가 변경 여부** — 오케스트레이션 도입이 기존 능력의 권한 요건을 바꾸는가 | 기본은 "안 바꾼다"이지만, O-2 의 답에 따라 달라질 수 있습니다 |

구현 카드(Card 1)는 이 표의 답을 받은 뒤에만 해당 부분을 구현합니다. 답이 나오면
이 표의 줄을 지우고 decisions.md 에 D- 번호로 옮깁니다.

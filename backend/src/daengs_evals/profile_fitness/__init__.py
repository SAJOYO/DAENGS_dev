"""개체 적합성 — 같은 질문을 강아지만 바꿔 물어 **답변 쌍**을 채점한다.

`answer_quality`(#277)는 답변 하나를 절대채점하고, 판정기에게 질문과 답변 문장만 준다
(`judge.py` 머리말). 그래서 "이 답이 **이 아이**에게 맞는 답인가" 는 그 판정기로 잴 수 없다 —
옵션이 아니라 설계다. 이 패키지는 그 자리를 잰다.

기획이 차별점이라고 적어 둔 것은 "프로필 + 진료 이력을 매 요청 컨텍스트로 주입" 이고,
앱은 `active_dog_id` 하나만 보낸다 (`AssistantApi.requestBody`). 프로필을 프롬프트에 얹는 것은
전부 서버 몫이라, **질문을 고정한 채 프로필만 바꿔 넣는 실험**이 성립한다.

    profiles.py     프로필 픽스처의 스키마 · 로더 · 배선 게이트

세 조건으로 잰다. 조건 이름은 `pairs.py` 가 쌍에 붙인다.

    N  잡음 바닥   같은 프로필로 두 번   — 안 변해야 한다 (거짓 양성률)
    P  절제       프로필 없음 vs 있음   — 변해야 한다 (민감도)
    S  특이도     다른 프로필 · 제도 질문 — 안 변해야 한다 (특이도)

`N < S ≪ P` 가 성립해야 이 지표가 잡음이 아니라 개인화를 재고 있는 것이다. 자산은
`evals/profile_fitness/` 에 둔다.
"""

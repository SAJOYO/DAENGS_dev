name: journey tests

on:
  pull_request:
    paths:
      - "backend/src/daengs_journey/**"
      - "backend/tests/journey/**"
      - "backend/pyproject.toml"
      - "backend/uv.lock"
      - "docker-compose.yml"
      - "nginx/default.conf"
      - ".env.example"
      - ".github/workflows/deploy.yml"
      - ".github/workflows/journey-tests.yml"
  push:
    branches: [dev]
    paths:
      - "backend/src/daengs_journey/**"
      - "backend/tests/journey/**"
      - "backend/pyproject.toml"
      - "backend/uv.lock"
      - "docker-compose.yml"
      - "nginx/default.conf"
      - ".env.example"
      - ".github/workflows/deploy.yml"
      - ".github/workflows/journey-tests.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - name: Compose 설정
        working-directory: ${{ github.workspace }}
        env:
          DAENGS_TRAINING_RAG_BASE_URL: http://host.docker.internal:8010
          REDIS_PASSWORD: ci-only-password
          # 서버 폴더 가드(`:?`, RAG-050). CI 는 크롤러를 띄우지 않으니 렌더만 통과하면 된다.
          DAENGS_CORPUS_DIR: ./data
        run: docker compose config --quiet
      - name: nginx 설정
        working-directory: ${{ github.workspace }}
        run: >-
          docker run --rm
          --add-host host.docker.internal:host-gateway
          -v "${{ github.workspace }}/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro"
          nginx:1.31.1 nginx -t
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: "backend/uv.lock"
      - name: 의존성
        run: uv sync --frozen
      - name: 테스트
        run: uv run pytest -q tests/journey
      - name: 린트
        run: uv run ruff check src/daengs_journey tests/journey

# 생활 파트 로드맵 — ① 제도·문서 RAG (`/life/ask`) · ② 실시간 산책 (`/life/walk-conditions`)

> **living doc.** "지금 무엇이 끝났고 무엇이 열려 있나"만 말한다. 결정의 *왜*는 여기 없다 —
> `RAG-` / `RT-` / `D-` 번호가 가리키는 곳에 있고, 여기서 다시 쓰지 않는다 (같은 결정의 두 번째 주장을
> 만들지 않기 위해서다. `README.md` 가 저쪽 `workflow.md` 를 안 가져온 이유와 같다).
>
> **갱신 규칙은 `docs/collaboration.md` §4 "living doc 갱신" 에 있다** — 여기 복사하지 않는다.
> 요약하면: 카드를 머지하면 해당 행을 고치고, **그 행을 인용하는 자리(요약 · 표 · 순서도)를 grep 으로
> 같이 본다.** 다른 저장소 작업은 카드 번호까지 적되 상태는 옮겨 적지 않는다.
>
> 작성 2026-08-30 (#81) · 현행화 2026-09-02 (#151) · 재점검 2026-09-03 (#164) · 대조 2026-09-04 (#202)
> · 갱신 2026-09-04 (#208) — D5 가 닫히고 `DAENGS_APP#112` 가 끝나 **09-21 전 묶음이 다 닫혔다** (§4).
> · 갱신 2026-09-05 (#236) — D1 종류별 슬라이스. 지표를 읽는 자가 바뀌었다 (§3 D · §1).
> · **갱신 2026-09-05 (#240)** — D5d · D8 을 라벨/문항으로 닫았다. **lap22** (§1 · §3 D).
> · **갱신 2026-09-06 (#268)** — `F1` ✅ · `F4`-ⓐ ◐ → **`#270` 이 닫았다 ✅** (§3 F · §4 의 4·5번 · §6). 코퍼스 9,451 → **10,304청크**.
>   **정찰이 계획을 바꾼 카드다** — `F0`(#256)는 "문서가 있는가"를 맞게 봤지만 "그 문서가 우리 질문에
>   답하는가"는 안 봤고, 사료 법령·고시는 `반려동물` 0회의 사업자 규제였다. **고시 2.64MB 를 뺐고**
>   값은 `nias-pet` 해설 3장이 가져왔다. **lap24 · lap25** (§1).
> · **갱신 2026-09-07 (#286 · #287 · #288)** — **셋 다 코퍼스를 한 행도 안 건드렸다.**
>   `D12` 가 채점기 버그를 걷어(`[1, 2]` 를 못 읽었다 — 저장된 랩 전체 90건) **모든 옛 랩의
>   `grounded` 를 소급으로 올렸고**, `D13` 이 `D5b`(§4 의 8번)를 열려다 **그 전제가 없다는 것을
>   찾아** 어휘 한 줄로 `Q3`·`B1` 을 열었다(21랩 만이다). **KPI 를 두 축으로 가른다** — 조 번호가
>   없는 소스 13문항은 `cited` 가 구조적으로 0이고 그것은 실패가 아니다. `D5b`·`D5e` 를 🚫 로 내리고
>   `D14`(부정 표현)를 세웠다. `pets.registered` 는 컬럼만 먼저 내 오늘 스냅샷에 태운다.
>   **lap29 · lap30** (§1 · §3 D · §4 의 8·10·10b·11·12·14번).
> · **갱신 2026-09-07 (#311)** — **캘리브레이션 1차: 일치 3/4** (RAG-075). 🔴 **`RAG-072` 의 `I4`
>   읽기를 정정했다** — 사람도 「답함」이었다(검색 사실 ③④⑤ 는 그대로이고 `D2` 근거도 그대로다).
>   그리고 **judge 는 무르지 않았다** — 오답이 **유일한 「못함」 판정**(`T3`)이라 이 랩에서 이 축이
>   잡은 진짜는 **0건**이다. `RAG-070` ②(청크를 보고 질의를 짐작) · `RAG-072` ⑦(순위를 잘못 셈)에
>   이어 **셋째 모양**이다 — 답변 하나를 보고 「이건 답이 아니다」를 혼자 정했다.
> · **갱신 2026-09-07 (#305)** — **`D15` 가 judge 를 세웠고, 그 judge 가 `I4` 를 통과시켰다** (RAG-074).
>   `rag judge <랩>` · 축 하나(`answers_question`) · 랩 파일만 있으면 돈다. **사람이 `RAG-007` 을 한 칸 더
>   조였다** — *"급 분리"*(flash → pro)는 같은 계열의 위아래라 self-preference 가 남으므로 **계열까지 갈랐다**
>   (생성 Gemini → judge OpenAI `gpt-5.4-2026-03-05`). ⚠ **카드의 전제가 흔들렸다** — 여는 근거였던 `I4` 가
>   안 걸렸고, 판정이 32/33 이라 기저율이 97%다. 잡은 것은 `T3` 하나. **지표 승격은 사람 라벨 뒤**이고
>   그것이 §6 에 열려 있다.
> · **갱신 2026-09-07 (#303)** — **`D5b` 를 닫았다** (사람 · RAG-073). §6 에 마지막으로 열려 있던 줄이다.
>   *"66행이 공통 머리를 공유한다"* 는 관찰은 **사실이었는데**(쌍 코사인 0.7999) **그 머리를 없애면
>   12문항 중 11이 나빠진다** — 머리가 잡음이 아니라 `동물보호법`·`과태료의 부과기준` 을 지고 있던
>   유일한 문장이었다. **파일은 하나도 안 고쳤다.** 소득은 딴 데 있었다 — 크게 실패한 둘이 각각
>   **어휘**(`입양`·`명의 변경`, §6 에 열어 뒀다)와 **참조 확장**이었다. `D5`-계열 다섯이 다 닫혔다.
> · **재조정 2026-09-07 (#298)** — **사람이 축을 다시 세웠다** (§6). ⓐ 발표(09-21)를 순서의 축에서 빼
>   §0 의 목표 문장에서 기한과 *"발표 자리에서"* 를 지웠고 **두 축이 나란해졌다** — 그래서 `D2` 가
>   §4 로 올라왔다(미룬 유일한 이유가 시연 지연이었다). ⓑ **LLM judge 를 다시 열었다**(`D15`) —
>   §5 가 닫아 둔 자리이고, **지금 지표는 답이 맞는지를 아무도 안 잰다**(`RAG-072` 의 `I4`).
>   ⓒ **GCP 크롤~적재 이전**을 `deploy/roadmap.md` §7-1 에서 끌어올려 §4 의 13번에 두었다.
>   **번호는 자리이지 주소가 아니다** — 열린 행을 다시 세워 `D2`·`D4`·`F4`-ⓑ·`F5` 의 번호가 바뀌었고,
>   카드 이름은 하나도 안 바꿨다.
> · **갱신 2026-09-07 (#296)** — **`D14` 를 닫았다.** 부정이 지워지는 것은 실물인데(`않` 이 든 청크
>   **0/9,838**) 고쳐도 `I4` 가 안 열린다. 정찰이 더 큰 것을 찾았다 — **렉시컬은 dense 후보를 재정렬할 뿐
>   후보를 못 들여온다**(렉시컬 1위 0.0049 < dense 100위 0.0063). 그래서 **순위 카드로 남은 것은 `D2` 하나**다.
>   **파일은 하나도 안 고쳤다** — 게이트에서 멈췄다 (RAG-072).
> · **갱신 2026-09-07 (#292)** — **재는 도구가 섰다.** `D6` 이 랩 대조를 총계가 아니라 **문항 단위**로 내고
>   잡음 띠를 옆에 붙인다 — `lap29 → lap30` 에서 뒤집힌 9문항 중 **두 축이 같이 움직인 `Q3`·`B1` 둘**을
>   짚는데, 그것이 RAG-070 ④ 가 손으로 귀속한 답과 같다. **임계값은 안 만들었다**(효과가 잡음보다 작다).
>   `D5e` 를 사람이 닫고 `D9` 도 같이 닫았다 — 랩 30개가 되며 총계 표의 마지막 줄이 `lap9-age` 였다.
>   §0 · §1 의 코퍼스 수를 두 DB 실측으로 맞췄다 (**9,838** · `insurance` 4,675 · `policy` 4,636 · `food` 272 · `travel` 255).
> · **재작성 2026-09-05 (#254)** — **트랙 축에서 목표 축으로 다시 그렸다.** 카드 번호는 하나도 안 바꿨다
>   (`decisions-rag.md` 와 머지된 PR 본문들이 그 번호를 주소로 쓴다). 바뀐 것은 **배열과 우선순위**다:
>   §0 에 목표 문장이 서고, §1 의 지표가 모집단 셋으로 갈리고, §3 의 모든 행이 "어느 축을 올리는가"를
>   달고, §4 가 순서 목록이 됐다. 계기는 사람의 "목표가 흐릿해진 느낌"(2026-09-05)이고,
>   전제 하나가 같이 풀렸다 — **재적재 비용 해제**(같은 날). 그래서 A1 이 게이트에서 내려온다.
> #202 의 대조는 §3 트랙 표는 맞았고 §0 · §1 · §2 · §4 가 하루 뒤처져 있었다는 것이었다 (#177 이 자기 행만 고치고 갔다).
> 2026-09-03 재점검은 앱 채팅 화면이
> `/assistant/query` 를 쓰고, `daengs_walk`(D-045~047)가 생기고, 발표 09-21 이 확정된 뒤의 순서 재조정.
> 사람 결정 다섯(페이로드 위치 · A4 · A8 · A3 분할 · B4 범위)이 이날 닫혔다 (§6).
> 전제: 오케스트레이션 문서 세트 #80 (사람 승인 O-1~O-14).

---

## 0. 한 장 요약

> ### 목표 — 이 파트가 하려는 것 하나
>
> **사람이 실제로 물을 질문에, 열리는 링크와 조 번호를 달아 답한다.**
>
> KPI 는 처음부터 **"출처 링크 + 조항 번호 인용"** 이었고 바뀌지 않았다 (`decisions-rag.md` 의 RAG-004 ·
> 007 · 011 이 같은 문장을 근거로 쓴다). 2026-09-05 에 더한 것은 **그 앞의 한정어**다 — *사람이 실제로
> 물을* 질문.
>
> **2026-09-07 에 바뀐 것은 기한과 저울이다** (사람 — §6). 앞선 판은 이 문장이
> ***"09-21 까지 … 발표 자리에서"*** 로 시작했고, 거기서 **커버리지 우선**이라는 순서 규칙이 나왔다.
> 근거 셋 중 ⓐ·ⓑ 는 그대로 사실이다 — ⓐ 조 번호가 있는 문항에서는 이미 `cited` 가 높고
> ⓑ 코퍼스의 절반이 지자체별이다. **힘을 잃은 것은 ⓒ 다** — *"실사용 신호가 8턴이라 정밀도를 더
> 깎아도 그것을 볼 사람이 아직 없다"* 는 **발표가 첫 청중이라는 전제** 위에 서 있었는데,
> 발표를 기한에서 빼면 그 전제가 없다. **당시 사실이라 지우지 않고, 순서를 정하는 힘만 뗀다.**
>
> **이 문장이 §3 의 모든 행에 "축" 열을 준다** — 커버리지 / 인용 정확도 / 운영.
> **두 축은 이제 나란하다** — 어느 쪽이 위라고 미리 정하지 않고, §4 의 순서는
> **의존과 값**으로 정한다 (재는 도구가 먼저, 전제가 선 것이 그 다음).

- **답변 API 는 끝났다.** `/life/ask` 9단계 전부 · `/life/walk-conditions` 전부 구현·배포. 코퍼스 **9,838문서**
  (2026-09-06 #268 이 음식·주거를 더해 8,990 → 9,838. 2026-09-07 두 DB 실측 — `#289` 로 GCP 도 같다).
  **기권·거절 신호도 섰다** (A3a ✅ #177) — 골든셋 **41문항**(#217 이 장례 FW3 을 더했다), lap14 와 같은 22문항에서 grounded 13 → 15.
- **08-30 에 세운 묶음은 2026-09-04 에 다 닫혔다** — 그 뒤 09-05 에 목표를 다시 세웠고, **남은 일의 순서가 §4 에 있다**. A0 ✅ → A5 ✅ → A4 ✅ → A3a ✅ → B4 ✅ → 앱의 `active_dog_id` ✅(`DAENGS_APP#112`).
  마지막으로 **D5(검색 순위)는 상수를 안 바꾸고 닫혔다** (#208 · RAG-057) — 다 훑었고 남은 것이 상수가 아니었다. §3 D 참고.
- **수집은 닫혔다.** 문서형 시드 23 중 16 수집, 남은 7 은 robots·WAF·SPA·문서 부재로 우리 손 밖 (`data-sources.md` §0).
- **운영 뼈대는 섰다** — #65(코퍼스 서버 이관) · #66(개정 감지) · #67(고시공고) 전부 2026-08-31 머지. 운영에 남은 것은 C5(워커 예열 · `crawl_runs` 잔존 행 · 알림 · HNSW) 뿐이다.
- **툴로서는 이미 섰다.** #80 에서 이 파트는 에이전트가 아니라 **능력(capability)** 이고, Life·Walk 둘 다 v1 실행 대상.
  통합 첫날 실제로 도는 능력은 이 둘뿐이다 (§2). **Card 1 은 2026-08-31 에 착륙했고(#102) B 어댑터는 그 안에 들어갔다.**
  **A 트랙은 2026-09-06 에 다 닫혔다** (`A1` ✅ #266 · `A2` ✅ #262 · `A7` ✅ #271) — 툴 축은 끝났고
  남은 것은 커버리지 축이다. 열려 있는 것은 `A3b` 하나인데 **라우터 담당 소유**라 이 파트가 순서를 못 정한다 (§4).
- **사용자에게 닿는 길은 어댑터 하나다.** 앱(DAENGS_APP) ChatScreen 은 자유 텍스트를 전부 `POST /assistant/query` 로 보낸다. 직접 `/life/ask`·`/life/walk-conditions` 는
  콘솔 점검 도구로 남았다 (§1). 그래서 A 트랙의 값은 "어댑터를 지나 사용자에게 보이는가"로 잰다 — A4 축소 · A8 verbose 가 🚫 된 이유다.
- **재적재 제약이 풀렸다** (2026-09-05 사람 — §6). 55분 + GCP 덤프·복원은 그대로지만 **감당하기로 했다.**
  대신 **✔ 카드를 연달아 열면 GCP 동기화는 묶어서 한 번** 낸다 (§4).
  이 해제가 바꾸는 것 하나 — **`A2`(지역 필터)가 `A1` 앞으로 온다.** A2 는 메타만 바꿔 55분을 애초에 안 내는데
  A1 뒤에 적혀 있었다. **`A1` 자체는 순서에 남는다** — 그 아래 **넷**이 재인코딩을 동반하므로,
  아래가 길수록 A1 이 앞에 있을 값이 커진다 (§4 의 3번). (2026-09-06 정정 — 다섯이 아니라 넷이다.
  `A7` 은 `category` 만 바꿔 재인코딩이 0이다.)
- **§4 는 날짜가 아니라 순서다.** 무엇 다음에 무엇인가만 정하고, **카드를 몇 장으로 쪼갤지와 Iteration 배정은
  카드를 여는 시점에 사람이 정한다** (`collaboration.md` §1 · §3). 기간이 짧으면 목록에서 덜 내려갈 뿐이다.
- **Walk 는 둘이다.** 이 문서의 Walk 는 Life 의 산책 *적합도*(능력 이름 `walk`)이고, `daengs_walk`(D-045~047)는 산책 *기록*·Capsule 이다 (§2).
- **범주 확장(`food` 등)은 경계 신호가 먼저다** (§3 F) — 그 전제 `A3a` 는 ✅ #177 로 섰으므로 **지금 열린다.**
  경계는 주제가 아니라 **근거의 종류**로 긋는다 (§2).
- **지표 총계가 세 모집단이 섞인 수였고, `D10` ✅ #261 이 고쳤다** (RAG-062). 경계 문항을 `cited`/`grounded`
  에서 빼고 **조 번호 있음/없음** 축을 세웠다. **lap22 `19·18/33` → `17/28 · 18/28`** — `cited` 가 내려간 것은
  회귀가 아니라 **부풀림이 걷힌 것**이다(내려간 둘이 `B3` 놓친 기권과 `B6` 옳은 거절이다).
  **이 커밋 앞뒤로 `cited` 를 비교하지 말 것 — 자가 바뀌었다.**

---

## 1. 지금 상태 (2026-08-30)

| 축 | 상태 | 근거 |
| --- | --- | --- |
| 크롤 | 소스 모듈 15 + Beat(KST 04:00 due 선별). **서버 워커는 `up -d` 에 같이 뜬다** — #65 가 profile 을 뗐고 `raw/`·로그 정본은 2026-08-31 부터 서버 | RAG-044 · 047 · 050 |
| 파스 → 청크 | HTML · XML · PDF(PyMuPDF, 레이아웃 경계) → **10,304청크** (2026-09-06 #268 이 +853) | RAG-018 · 036 · 041 · 048 · 051 · **065** |
| 임베딩 | `qwen3-embedding-0.6b` 승자. **소스 하나 늘면 전량 재인코딩 55분** | RAG-024 · 043 ④ · 045 ⑧ |
| 적재 | `load --prune` — 행 단위 upsert + 옛 행 청소. **HNSW 인덱스 미생성(전수 스캔 = 정확한 최근접)** — GCP 운영에서 142ms 라 아직 병목이 아니다. 켜는 것과 **적재 증분화**가 `D16`(§4 의 13b) | RAG-025 · 045 |
| 검색 | 하이브리드(dense + Kiwi FTS, RRF 0.3) + 인용 확장(홉) + 교통 배제 + **지역 필터**(`org` 사전필터, 질의를 형태소로 읽는다 — RAG-063 ✅ #262) | RAG-035 · 040 · 052 · 003 · **063** |
| 골든셋·평가 | **41문항** — 법령 Q7 · easylaw QA8 · 경계 B6 · 조례 S5 · 교통 T5 · 보험 I5 · 장례 FW3 · 프로필 DP2 (hand 33 · easylaw 8 / answer 36 · abstain 2 · refuse 3). `score-laps` 가 `expect` 까지 채점하고 **`--by` 로 문서 종류별 슬라이스**를 낸다 (RAG-060). **judge·회귀 게이트 없음** | RAG-022 · 029 · 049 · 055 · 056 · 059 · 060 · 007 |
| `/life/ask` 서빙 | `POST /life/ask` — 근거 0건 404 · **약한 근거 기권 404**(`covered+selfreport`) · **경계 거절 422**(`medical`·`emergency`). 응답에 `content` 전문 | RAG-028 · 055 · D-021 |
| `/life/walk-conditions` | 결정적 · T+24h 타임라인 · `sources` 노출 · 판정 불가는 `unknown`(503). **특보는 구 단위까지 잡는다** (#175) | RT-001 ⑥ · RT-003 |
| 화면 | **앱 ChatScreen → `POST /assistant/query`** (DAENGS_APP `ui/chat/ChatScreen.kt`). 웹 콘솔은 생활 RAG 탭이 직접 `/life/ask`·`/life/walk-conditions` 를, **어시스턴트 탭**(`assistant-inspect.tsx`)이 `/assistant/query` 를 부른다. `/console/status` 도 생겼다. 앱이 Life 를 직접 부르는 코드는 없다 | #115 · #36 · #180 · 2026-09-04 실측 |
| 코퍼스 분포 | `insurance` 4,675 · `policy` 4,636 · `food` 272 · `travel` 255 (합 9,838) — **`A7`(#271)이 insurance 를 policy 에서 갈랐고 `F1`(#268)이 food 를 채웠다** | 집 서버 · GCP DB 실측 (2026-09-07) |

랩 추이 (같은 22문항은 lap12 부터, `cited`·`grounded`): lap12 16·13 → lap13 16·12 → lap14 17·13 →
**lap18 15·15** (A3a) → lap19 17·15 → lap20 18·16 → lap21 18·15(33문항) → **lap22 19·18**(#240).
⚠️ **이 줄은 전부 옛 표기(경계 문항 포함)다** — RAG-062 앞의 자로 잰 수이고, 그래서 그대로 둔다.
새 자로는 lap22 가 `17/28 · 18/28` 이다. 두 표기의 대조는 `score-laps` 총계 표의 `옛 표기` 열에 있다.

**새 자로 잰 추이는 여기서 잇는다** — lap22 `17/28 · 18/28` → **lap23 `16/28 · 20/28`** (A2 · #262).
문항 단위로 귀속하면 **이 카드의 것은 `S3` 하나**이고(`ordinance-search` 칸 2/2 → 2/3), 나머지
넷(T2 · T3 · I1 · I2)은 **지역 신호가 안 뜨는 문항이라 SQL 이 동일**하므로 비결정성이다 (RAG-059 ③).
`S3` 는 `grounded` 만 잡히고 `cited` 는 여전히 `[]` 다 — 답이 정확한데 본문에 "제8조"를 안 썼다.
**검색은 고쳤고 남은 것은 생성이다** (RAG-063 ⑧).
**→ lap25 `16/33 · 22/33`** (F1 · F4-ⓐ · #268).
⚠️ **lap24·lap25 는 지역 필터가 죽은 상태에서 쟀다** (RAG-066 ① · #270). `#268` 의 `rag load` 가
A2 의 `org` 백필 2,592행을 지웠고 `S3` 가 그때 죽었다. **아래 문단의 새 문항 이야기는 그대로
유효하고**(지역 신호가 없는 문항이다) **총계 비교만 틀렸다.** 복구 뒤의 수는 RAG-066 ④ 에 있다.
**→ lap27 `18/33 · 23/33`**(회귀 복구 · 어휘 다리 전 대조) **→ lap26 `20/33 · 26/33`**(D11 · #270)
**→ lap28 `18/33 · 26/33`**(A7 · D3 · #271 · #285) **→ lap29 `16/33 · 29/33`**(D12 · #286)
**→ lap30 `22/33 · 30/33`**(D13 · #287).
🔴 **`lap29` **앞**의 `grounded` 는 전부 눌린 값이다** (RAG-069 ③ · #286).
(`lap29`·`lap30` 은 고친 자로 잰 값이라 해당 없다.) 채점기가 `[1, 2]` 처럼 한
괄호에 여럿을 쓴 근거 표기를 **통째로 놓치고 있었다** — 저장된 랩 전체에서 90건이다. 채점기만
고치면 랩 파일을 안 건드리고 소급된다: `lap22` 18→**20** · `lap26` 26→**27** · `lap27` 23→**25** ·
`lap28` 26→**28**. 그러니 `lap29` 의 29 를 위 수들과 나란히 놓으면 **`D12` 의 몫이 부풀어 보인다** —
같은 자로 재면 `28 → 29` 이고, 그 +1 이 `I1` 이다.
⚠️ **`lap30` 의 `cited` +6 을 `D13` 의 것으로 읽으면 안 된다** (RAG-070 ④). 바뀐 문항이 10개인데
어휘가 붙는 것은 **`Q3`·`B1` 둘뿐**이고 나머지 여덟은 잡음이다 — 이 카드의 몫은 `cited +2 ·
grounded +2` 다. **그 랩의 잡음이 ±4 였다**(아래 문단은 ±2~3 이라고 적는다). 총계 한 줄로
카드의 성패를 말하면 안 된다는 근거이고, **`D6`(#292)이 선 이유다** — 이제 `score-laps --against`
가 문항 단위로 대조하고 잡음 띠를 옆에 붙인다 (RAG-071).
🟢 **KPI 는 이제 두 축으로 낸다** (RAG-070 ③ · `score-laps` 의 `KPI` 줄). `lap30` 기준
**조 번호가 있는 소스 18/20 (90%) · 없는 소스는 근거 13/13 (100%)** 이다 — 뒤 13문항은
**문서에 조 번호가 없어서** 인용 칸이 0이고 그것은 실패가 아니다. 총계 칸은 안 건드렸다.
둘을 갈라 잰 이유는 `org` 복구의 몫과 어휘 다리의 몫을 섞지 않기 위해서다 — 복구가 `cited +2 ·
grounded +1`, 어휘 다리가 `cited +2 · grounded +3` 이다. **문항 단위로 귀속하면 어휘 다리의 것은
`HS1`·`FW3`·`DP1` 셋**이고, 나머지 둘(`S1` −1 · `S5` +1)은 별칭이 안 붙는 문항이라 비결정성이다.
**lap28 은 `D3`(#285) 뒤인데 `grounded` 가 26 그대로다**(옛 자) —
⚠️ 고친 자로는 `lap26` 27 → `lap28` 28 이라 **+1 인데, 그것도 잡음 안이다** (RAG-069 ③). 결론은 안 바뀐다 — 그 카드가 고친 것은 보험 약관의 절 이름이고, **보험 문항의 `must` 는 KB·농협을 안 가리킨다**(RAG-068 ④). 지표가 안 움직인 것이 예상 밖이었고 그 어긋남 자체가 그 기록의 내용이다. **분모가 28 → 33 으로 는 랩이라 분자만 보면 안 된다** —
새 문항 다섯(`FD1`·`FD2`·`FD3`·`FD4`·`HS1`)이 들어왔고 그중 **넷이 `grounded`, 하나(`FD3`)가 `cited` 까지**
간다. 실패는 `HS1` 하나이고 원인이 순위가 아니라 **어휘**다 (RAG-065 ⑨ · §4 의 15번).
기대 채점은 **통과 36/38 · 경계 아닌데 거절 0** 이다 — lap24 에서 `FD1`("초콜릿 줘도 되나요")이
`medical` 로 거절돼 `35/38 · 1` 이었고, 프롬프트 한 줄로 풀었다. `B4`·`B5`·`B6` 의 거절은 그대로다.
⚠️ 같은 두 랩에서 `cited`·`grounded` 는 `18/33 · 23/33` → `16/33 · 22/33` 으로 내려갔는데
**잡음이다** — 코퍼스도 문항도 같고, 그 한 줄이 노린 축은 `boundary` 뿐이다 (RAG-065 ⑩).
⚠️ **lap22 의 `grounded` +3 은 세 갈래다** — #217 의 라벨 2(FW1·FW2, 검색은 그대로인데 `tier` 만 바뀌었다) · #240 의 1(I5) · 비결정성 0(Q4 +1, S5 −1). **`cited` +1 도 비결정성 +3 과 #240 의 −2 가 합쳐진 수**이고, 그 −2 는 **의도한 것**이다 (RAG-061 ④ — 부풀림이 사라졌다).
**종류별로 쪼개면 총계가 감추는 것이 보인다** (RAG-060 · #236) — `--by source_id` 로 lap21 을 읽으면
`benefit24-services` 는 21개 랩 내내 `cited` 0(답은 맞다)이고 `knia-disclosure` 는 `cited` 2 · `grounded` 0 이다.
**눌림과 부풀림이 총계에서 상쇄**돼 `18·15` 한 줄로는 어느 쪽도 안 보인다.

#### ⚠️ 총계 `19·18` 은 **세 모집단이 섞인 수**다 (2026-09-05 실측 — #254)

`lap22.jsonl` 을 `score.grounded_from_dump` 로 문항마다 채점하고 골든셋 `must` 라벨의 소스로 묶은 결과다.

| 모집단 | 문항 | `cited` | `grounded` | 이 칸을 어떻게 읽나 |
| --- | --- | --- | --- | --- |
| **조 번호가 있는 문항** — `must` 앵커에 `제N조`·`별표` 가 있다 | 18 | **13 (72%)** | 10 (56%) | **여기가 실력이다.** KPI 두 축이 다 성립하는 유일한 자리 |
| **조 번호가 없는 문항** — 항공 · 보조금24 · knia · 철도 · easylaw note | 10 | 4 (40%) | 8 (80%) | `cited` 는 **영영 낮다.** 요구 근거에 인용할 조가 없다 (RAG-029 의 알려진 누수 · D4) |
| **경계 문항** — 기권 2 · 거절 3 (`must` 없음) | 5 | **2** | 0 | **뜻이 없는데 총계 분모·분자에 다 들어간다** |

**마지막 줄이 지금 지표의 실제 병이다.** `B6`(초콜릿)은 거절을 옳게 했는데 **거절문에 "제10조·제6조"가
들어가** `cited` 로 잡히고, **`B3`(펫보험 몇 살까지)은 기권해야 하는데 답해 버린 실패인데 총계에서는
성공으로 센다.** 같은 랩의 기대 채점표에는 그것이 `놓친 기권 2/2` 로 정직하게 찍혀 있어 —
**한 랩의 두 표가 서로 반대를 말한다.** ✅ **`D10`(#261 · RAG-062)이 고쳤다** — 경계 문항을 총계에서 빼고
조 번호 축을 세웠다. **lap22 는 이제 `17/28 · 18/28`** 이고, 옛 수는 `score-laps` 의 `옛 표기` 열에 남는다.

> ⚠ **위 표의 축은 `must` 앵커로 정한다** (RAG-062 ③) — 소스가 대체로 어떤가가 아니라 **이 문항의 요구
> 근거가 무엇인가**다. #254 가 소스 묶음으로 손 계산했을 때는 `14/12 · 3/6` 이었는데, 앵커로 읽으면
> 네 문항이 옮겨간다: `S5`(`#제4조` 를 가짐)·`I1`(`특별약관 제3조`)은 조 있음, `Q7`·`FW2`(`#note-1` 꼴)는 조 없음.

**코퍼스 질량과 평가 관심이 반대로 붙어 있다** (같은 날 서버 DB `192.168.0.22` 실측):
각 손해보험사 4,563(51%) + 법제처 계열 3,716(41%) = **92%** 인데, **조 번호가 없는 소스는 406청크(4.5%)뿐이면서
골든셋 문항의 36%를 진다.** 그래서 총계는 코퍼스가 아니라 골든셋의 편성을 재고 있다.

**링크 축은 건강하다 — 여기를 의심하지 말 것** (같은 날 실측). `documents.source_url` 은 법령·조례에서
`OC=***` 로 마스킹된 XML API 주소라 사람이 못 여는데, **답변에 실리는 것은 `metadata.citation_url`** 이고
그쪽은 `law.go.kr/법령/...` 사람이 여는 주소다 (`law_drf_api.py:219`). **8,990/8,990 보유 · 마스킹 0.**
RAG-019 가 두 필드를 나눈 것이 이걸 막았다. #254 가 `source_url` 만 보고 "KPI 절반이 깨졌다"고
의심했다가 확인해서 아니었고, 그 확인을 여기 남긴다.

**실사용 신호는 사실상 0이다** (같은 날 실측) — `chat_turns` 8행 · `chat_sessions` 3행 ·
`answer_reports` 1행. **품질 루프 전체가 손으로 쓴 33문항 위에 서 있다.**
이것이 §0 의 목표 문장에서 "정밀도가 아니라 커버리지"의 근거 ⓒ였다.
⚠️ **2026-09-07 부터 이 줄은 순서를 정하지 않는다** (§0 · §6). *"09-21 발표가 첫 실제 청중"* 이라는
전제 위에 서 있던 논거인데 발표가 축에서 빠졌다. **실사용이 적다는 사실은 그대로이고**, 그것이
이제 가리키는 것은 순서가 아니라 **`D15`(judge)** 다 — 사람이 안 물어 주면 답이 맞는지를 잴 자가
골든셋뿐이고, 그 골든셋을 사람 라벨 없이 넓히는 것이 그 카드다.
(랩은 골든셋 41문항 중 손으로 쓴 33만 돈다 — easylaw `QA1`~`QA8` 은 랩에 없다.)

⚠️ **랩 간 잡음이 ±2~3 이다** — lap21 은 검색이 안 바뀐 30문항 부분집합에서 lap20 대비 셋을 잃었고
그 셋은 전부 Gemini 비결정성이다 (RAG-059 ③). **총계만 보면 노이즈를 성과로 읽는다** — 비교는 문항
단위 귀속까지 가야 뜻이 있다. `cited` 가 준 둘(S5·T2)은 답이 맞고 소스에 조 번호가 없는 것이라 RAG-029 의 알려진
누수다. **기존 소스를 더 긁어 올릴 여지는 소진**됐고(RAG-040 이 실증), 남은 여지는 순위(D5 계열)와
**새 범주**(F)다.

⚠️ **그 잡음이 순위 카드의 자리를 정한다** — 순위 카드(`D5b` · `D5e` · `D2`)의 기대 효과는 문항 1~2개인데
**랩 잡음이 ±2~3이라 효과가 잡음보다 작다.** D5(#208)가 "상수로는 안 된다"를, D5d(#240)가 "효과 0"을
각각 정직하게 확인한 것이 그 증거다. **못 고칠 것이라서가 아니라 못 잴 것이라서** `D10`·`D6` 뒤에 둔다 (§4).
**넷 다 그 뒤 닫혔다** — `D5e`(#292)는 전제가 사라져서, `D14`(#296)·`D5b`(#303)는 **전제는 맞았는데
고쳐도 값이 없어서**다. ⚠ `D2` 는 안 닫혔다 — **미룬 이유가 사라져 §4 의 12번으로 올라갔다**(#298). 재는 쪽은 `D10`(#261)에 이어 **`D6`(#292)이 서면서 갖춰졌다.**
남은 `D2` 는 못 재서가 아니라 **지연 때문에** 뒤이고, `D14` 의 정찰이 **그 자리가 정면임을 수로 보였다**
(RAG-072 ⑤ — 정답 면책 조가 dense 89·153위다).

### 다른 파트와의 관계 — 통합 시점 (2026-08-30 dev 기준)

| 파트 | dev 상태 | 이 로드맵에 미치는 것 |
| --- | --- | --- |
| Place 검색 (병원·약국·동반시설) | **2026-09-04 갱신** — profile 이 떨어져 기본 `up -d` 로 뜨고, `place` 가 **실행 능력**이 됐다 (#196 · #198). 다만 `ExecuteName` 은 아직 세 능력이라 명시 신호로만 들어간다 | 장소 데이터는 **저쪽 소유** — 이쪽은 넣지 않는다. 핸드오프가 아니라 능력이 됐으므로 A3b 를 다시 써야 한다 |
| 훈련 RAG | `daengs_training` 모듈로 backend 프로세스에 통합 | 별도 PGVector 유지, 향후 측정 결과에 따라 추출 가능 |
| 피부 스크리닝 · 보행 분석 | profile 뒤, 배포에 안 붙음 (D-022 · D-029) | v1 은 HANDOFF 만 (#80) |
| 오케스트레이션 | **2026-09-04 갱신** — v1 실행 코어(#102) · 시맨틱 라우터(#113) · `/assistant/query`(#115) · closeout(#118) · Place 능력(#196 · #198) | Life·Walk 어댑터는 **이미 그 안에서 돈다**(B2 · B3). 능력 쪽 신호 셋(A0 · A3a · A5)은 **다 섰다**. 이 파트에 남은 것은 B4 뿐이고, place 목적지 선택(A3b)은 라우터 소유 |

---

## 2. 오케스트레이션 안에서 이 파트의 자리

계약·상태·라우팅은 `docs/orchestration/contracts.md` · `docs/orchestration/routing.md`(#80) 가 원본이다. 여기서는 **이 파트가 그 틀에
꽂히려면 무엇을 내야 하는가**만 적는다.

```
/assistant/query → LangGraph
   라우터(분류기) → RoutePlan{requests[], handoffs[], clarify}
   → LifeCapabilityAdapter.run(LifePayload)  ─in-process→ daengs_life.app.services.ask.ask()
     WalkCapabilityAdapter.run(WalkPayload)  ─in-process→ daengs_life.app.services.walk.walk()
   → CapabilityResult{OK | ABSTAINED | REFUSED | PENDING | ERROR | TIMEOUT}
   → 결정적 집계 → AssistantResponse
```

**용어 — Walk 가 둘이다.** 위 `WalkCapabilityAdapter` 와 능력 이름 `walk` 는 Life 의 **산책 적합도**(`daengs_life.realtime`, RT-)다.
`backend/src/daengs_walk/`(D-045 · D-046 · D-047)는 GPS 산책 **기록**·Cellophane·Capsule 이고 `/app/walks` 로 나가며, 오케스트레이션의
실행 대상이 아니다. 이 문서에서 "Walk" 는 전자다. HTTP 경로는 A4(#176)로 `/life/walk-conditions` 가 됐지만 **능력 이름 `walk` 는 09-21 전에
바꾸지 않는다** — 벤치마크 골드 80문항 · 앱이 읽는 `results[].capability` 가 그 값에 묶여 있다 (§5).

### 경계 — 주제가 아니라 **근거의 종류**로 긋는다

"생활"은 주제로는 끝이 없다. 이 파트의 실제 경계는 처음부터 하나였다: **출처를 인용할 수 있는 답만 한다** (KPI · `/life/ask` 의 404 ·
RAG-008 ③ 이 `care`·`emergency` 를 뺀 이유).

| Life 가 답한다 | 근거의 종류 |
| --- | --- |
| 규정 · 제도 · 절차 · 조건 · 요금 · 기한 | 법령 · 조례 · 고시 · 약관 · 기관 공식 안내 (`trust_level`: `law` > `official` > `guideline` — **`guideline` 은 아직 0건이다**, RAG-060 ⑥) |
| 지금/오늘의 조건 판정 | 공공 API 직조회 — `/life/walk-conditions`, (앞으로) 분실 조회 |

| Life 가 답하지 않는다 | 이유 | 어떻게 거절·넘기나 (2026-09-03 계약 실물에 맞춤) |
| --- | --- | --- |
| **이 개의 몸**에 대한 판단 — 증상 · 진단 · 용량 · 응급 | 문서가 아니라 판단 (RAG-008 ③) | **Life 가 REFUSED** — `refusal.code` = `medical_boundary` / `emergency_boundary` (A3a). 능력은 handoff 를 낼 수 없다 — `CapabilityResult` 에 그 필드가 없다 (contracts §4) |
| **이 개의 행동**을 바꾸는 계획 — 짖음 · 배변 · 입질 교정 | 〃 | 라우터가 `training` 을 EXECUTE 로 같이 고른다(다중 선택). Life 는 근거 없으면 `no_evidence` 기권 — **별도 핸드오프 불필요** |
| **어디** — 병원 · 카페 · 숙소 위치 | 장소 데이터는 place 소유 (D-026) | **라우터가 `place` 를 EXECUTE 로 고른다** (A3b). #196 이 능력·어댑터·planner 를 넣었지만 `ExecuteName` 은 아직 `training`·`life`·`walk` 라, 자연어 place 질문은 여전히 빈 선택 → 최상위 FAILED 다 |
| 근거 문서가 코퍼스에 없는 것 | — | 기권 `no_evidence` (넘길 곳 없음) |

이렇게 잡으면 ① 경계가 코드로 강제되고(코퍼스에 있는 것 = 경계) ② 골든셋 `expect: abstain` · `expect: refuse` 로 **테스트가 되며**
③ Life 를 라우터의 **기본 능력**으로 둘 수 있다 — 실패 방식이 정직(근거 없으면 기권)해서다. 핸드오프 식별자는 라우터 소유라
`place` 추가는 라우터 카드(A3b)다 — 이 파트는 필요를 적고 조율한다.

### 이 파트가 내야 할 신호 — 현재 / 목표

| 신호 | Life 현재 | Life 목표 | Walk 현재 | Walk 목표 |
| --- | --- | --- | --- | --- |
| OK | 200 | ✅ **어댑터가 축소한다** — answer · citations(label·url·title) · quality (`adapters/life.py`, O-9). 직접 `/life/ask` 는 전문 유지 (A4 · §5) | 200 | 200 |
| ABSTAINED | ✅ 근거 0건 404 **+ 약한 근거 기권** — 생성이 낸 `covered` + 답변의 자기보고 (`covered+selfreport`, RAG-055) | 그대로. 오기권 셋은 검색 순위 구멍인데 **D5 가 상수로는 못 고침을 확인했다**(RAG-057) — Q3·B1 은 D5b(별표 청킹)·D5d(라벨), S3 는 D5e — ⚠ **셋 다 그 길로는 안 풀렸다**: `S3` 는 `A2`(#262)의 지역 필터가, `Q3`·`B1` 은 `D13`(#287)의 어휘 다리가 열었고 `D5e`(#292)·`D5b`(#303)는 닫혔다 | HTTP 는 `unknown` → 503, **어댑터가 ABSTAINED 로 옮긴다** (#102) | ✅ 확정됨 — A6 |
| REFUSED | ✅ **생성이 `boundary` 를 낸다** — `medical` · `emergency` → 422 + `code`, 어댑터가 REFUSED 로 (RAG-055). 문장도 Life 가 만든다 (불변식 3) | `place` 는 라우터 몫(A3b), `training` 은 다중 EXECUTE 라 불필요 | — | — |
| ERROR / TIMEOUT | 502 / 504 | 그대로 | 503(예산 초과) | 그대로 |
| 컨텍스트 입력 | ✅ `question` + **반려견 견종·나이** (`LifePayload.dog`, B4 · RAG-056) | ✅ **앱이 `active_dog_id` 를 보낸다** (`DAENGS_APP#112` 완료, 2026-09-04) — 사용자 경로에서 켜져 있다. + 지역 (A2). 등록 여부는 pets 에 없다 → F5 전제 | `lat` `lon` | 그대로 — 앱이 `location` 을 매 요청 보낸다 (contracts §8). 프로필 기본 위치는 🚫 (§5) |

---

## 3. 트랙

각 행: 무엇 · 왜(번호) · 전제 · 크기(Project Size 기준) · 상태. **모든 행은 번호나 PR 을 하나 이상 가리킨다** — 근거 없는 항목은 넣지 않는다.

> **트랙은 "어디를 고치나"의 축이다 — "무엇을 위한 것인가"가 아니다.** 그래서 2026-09-05(#254)부터
> 열린 행마다 **축**을 하나 붙인다. §0 의 목표 문장이 그 축을 정의한다:
>
> | 축 | 뜻 | §4 순서에서 |
> | --- | --- | --- |
> | 🟢 **커버리지** | 사람이 물을 법한 질문에 **답이 아예 나오는가** | 🔵 와 **나란하다** (2026-09-07) |
> | 🔵 **인용 정확도** | 나온 답의 조 번호·링크가 **맞는가** | 🟢 와 **나란하다.** `D10`·`D6` 이 서서 잴 수 있게 됐고, **답이 맞는지는 아직 아무도 안 잰다** — `D15`(judge)가 그 자리다 |
> | ⚪ **운영·부채** | 위 둘을 **계속 낼 수 있는가** | **재는 도구는 맨 앞**(`D10` ✅ · `D6` ✅ · `D15`). 나머지는 위 둘의 **비용을 낮추는 순서**로 (GCP 이전이 그것이다) |
>
> ⚠ **2026-09-07 이전에는 이 표가 "🟢 가 위로 온다"고 적고 있었다.** 그 근거는 §0 의 ⓒ 였고,
> 발표를 기한에서 빼며 같이 사라졌다. **축의 뜻은 안 바뀌었다 — 저울만 바뀌었다.**
>
> 트랙 글자(A~F)는 그대로 두는데, `decisions-rag.md` 와 머지된
> PR 본문들이 `A1`·`D5b` 같은 번호를 주소로 쓰기 때문이다 — **번호를 바꾸면 그 인용이 전부 끊긴다.**

### A. 툴로서의 완성 — **툴 축은 닫혔고 남은 셋은 코퍼스 카드다**

> 2026-09-05(#254): `A0`·`A3a`·`A4`·`A5`·`A6`·`B4` 가 다 ✅ 라 "툴로서의 완성"은 끝났다. 트랙 이름은
> 번호 주소를 지키려고 그대로 두지만, **열려 있는 것은 `A7` 하나**다 (`A1` ✅ #266 · `A2` ✅ #262).

| # | 무엇 | 왜 | 전제 | 크기 | 상태 |
| --- | --- | --- | --- | --- | --- |
| A1 | **문서 단위 증분 임베딩** — parquet 을 `(chunk_id, content)` 로 대조해 바뀐 것만 인코딩 | 지금은 소스 하나에 55분 전량 (RAG-043 ④ · 045 ⑧ · 047). 뒤의 소스 카드 **넷**이 이 비용을 낸다 | 없음 — `embed.py` 안에서 끝남 | L | ✅ **#266 (2026-09-06)** — RAG-064. 행별 `content_sha256` 으로 판정한다(`chars` 는 `restamp` 가 스스로 "대리 검사"라 부르는 값이라 못 쓴다). 옛 parquet 은 **전역 지문을 증명서 삼아 재인코딩 없이** 채운다. **왕복 증분 검증**(`tools/a1_roundtrip_verify.py`)이 전량 62분을 안 내고 **재사용 9,143/9,143 비트 동일 · 다시 만든 308행 최대 7.7e-07** 을 확인했다. **곁가지로 `rag embed` 기본을 3종 → 서빙 모델 하나**로 바꿨다(⑥) |
| A2 | **지역 필터** — `org` 을 `chunks`·`METADATA` 로 잇고, 질의에서 지자체를 읽어 사전 필터 | 코퍼스 절반이 지자체별(조례 208 · 보조금24 37)인데 "우리 동네"를 못 가름. RAG-003 · **RAG-033 ⑥**(org 사전필터/BM25/되묻기 택1 미결) · 034 | `db/migrations/` 1장. **재임베딩 불필요(메타만)** | M | ✅ **#262 (2026-09-05)** — RAG-063. **ⓐ `org` 사전필터로 확정.** ⓑ(하이브리드)는 RAG-035 로 들어왔는데 S3 가 **13랩 연속 실패**했고 D5 가 순위 상수로도 못 고침을 증명해, ⑥ 의 "ⓑ 가 들어오면 ⓐ 가 필요 없어질 수 있다"는 **아니었다**. 배관은 `META_FIELDS` 보다 한 단계 앞(`chunk.py`)에서도 끊겨 있었다. **`org` 없는 문서는 남긴다**(법령에 `org` 이 없다). **질의는 부분 문자열이 아니라 형태소 토큰으로 읽는다** — `고양이`가 `고양시`로 잡히던 오탐(QA7). 결과: **S1~S5 must 전부 1위**(전에는 S3 가 top-8 밖), 잃은 문항 0. lap23 `16/28 · 20/28` 이고 문항 귀속으로 **이 카드의 것은 S3 하나**다 |
| A0 | **GCP 에서 `/assistant/query` 경유 Life 스모크** — 앱 계정으로 제도 질문 하나, 기대 OK + citations | `docs/orchestration/architecture.md` 가 "배포 인프라에서 실제 Life 능력 스모크"를 미완 후속으로 남겼다. Walk 는 앱의 CAUTION 문구 버그(859a691)로 GCP 경로가 검증됐지만 Life 는 아니다. `ml` 그룹이 빠지면 503→ERROR 인데 앱에는 "실패" 한 줄뿐이라 뒤 카드가 전부 헛돈다 | GCP 접근 | XS | ✅ #169 (2026-09-03) **인프라 PASS** — 4/4 200, 예열 503 없음, O-9 축소 확인, 의미 라우팅이 `life` 선택. 답변 품질에서 A3a 근거 둘: 산문 물러섬이 OK 로 통과(`cited == []`), `no_evidence` 기권이 사실상 안 남. 기록은 `assistant-life-gcp-smoke.md` §3 |
| A3a ✅ | **Life 경계 신호 — REFUSED** — `services/ask.py` 가 `medical`·`emergency` 를 감지해 REFUSED 로 내고(`refusal.code` 보존), 골든셋에 `must` OR 목록 · `expect: abstain` · `expect: refuse` 문항 | RAG-049 ④ (Q2·T1·I1·I5 가 검증 문항) · D-035 "Life 안전/거절 분류 신설은 별도 카드" · §2 의 경계를 테스트로. **A0 실측**(`assistant-life-gcp-smoke.md` §3): 목줄 과태료 질문이 "자료에 없다" 산문 + OK 로 나가고(`cited == []`), 근거 0건 문장도 검색이 항상 k 건을 돌려줘 `no_evidence` 가 안 난다 — **약한 근거 기권도 이 카드 범위** | A0 ✅ | M | ✅ **#177 (2026-09-03)** — 신호는 **생성이 낸다**(방식 a, RAG-055 ②): `boundary` 3/3 정확 · 오거절 0 (lap16·17·18 모두), 기권은 `covered+selfreport` 로 놓친 기권 0/2. 골든셋은 `must` OR · `expect` 로 늘렸고(36문항) `score-laps` 가 그것을 채점한다. 지표는 lap14 와 같은 22문항에서 grounded 13 → 15, cited 17 → 15(S5·T2 — 답은 맞고 소스에 조 번호가 없다, RAG-029 의 알려진 누수). 오기권 셋(Q3·S3·B1)은 **검색 순위 구멍**이라 D5 로 넘긴다 |
| A3b | **라우터가 `place` 를 자연어로 고르게 한다** — `semantic.py` `ExecuteName` · 프롬프트 목적지 서술 · 골드 문항 · 벤치마크 버전 상승 재실행 | §2 — place 질문이 빈 선택 → FAILED. D-041 | **라우터 담당 조율** | M | 🟢 ⬜ 라우터 카드 — 이 파트 밖. **2026-09-04 정정**: #196 · #198 로 place 가 handoff 후보가 아니라 **실행 능력**이 됐다(`CapabilityName.PLACE` · `adapters/place.py` · `planner.py` 좌표 CLARIFY). 남은 것은 라우터의 목적지 선택뿐이고, 이 행은 **라우터 담당이 다시 쓴다** |
| A4 | **직접 API 이름 정리** — `POST /ask`→`POST /life/ask`, `GET /walk`→`GET /life/walk-conditions`, Swagger 태그 `Life · 제도 Q&A` / `Life · 산책 적합도`. 응답 전문은 **유지**하고 콘솔 관측용임을 DTO docstring 에 명시 | 파트 접두사 통일(`/training/chat` · `/assistant/query` 꼴) · `/life/walk-conditions` 와 `/app/walks` 혼동(`main.py` 의 별칭 경고) · 2026-09-03 사람 확정 | 앱이 직접 안 부름 — DAENGS_APP dev `0290d23` 에서 확인 (§6). 콘솔 점검 탭 호출 경로 · 문서 동반 | S | ✅ #176 (옛 "응답 축소"는 🚫 — §5) |
| A5 | **특보구역명 ↔ 행정구역 매핑표** (`realtime/warning_areas.csv`) | `collect.py` 가 `warning_area=None` — **구 단위 특보를 놓친다.** `/life/walk-conditions` 의 유일한 기능 구멍 (RT-001 ②-a · RT-002 ②-c) | 없음 | S | ✅ #175 (2026-09-03) — RT-003. 출처는 날씨누리(공공데이터포털 파일데이터는 관할 시군구가 없다). **딸려 나온 정정 하나**: 묶음 머리(`서울(서울서남권, …)`)를 매칭해 서초구가 옆 권역 주의보를 자기 것으로 읽던 것 |
| A6 | Walk `unknown` → ABSTAINED 매핑 | #80 계약 표 · RT-001 ⑥ | 없음 | XS | ✅ #102 (`orchestration/adapters/walk.py:52`) |
| A7 | **`insurance` 를 `policy` 에서 독립 category 로** | 4,673/9,836 이 한 칸에 몰려 category 필터의 격리 효과가 없음 | — | S | ✅ **#271 (2026-09-06)** — RAG-067. 재인코딩 0(지문 그대로). **값은 지표 축이다** — 서빙이 `category` 를 안 넘겨 검색 필터 소비자는 아직 없고, 그 층은 라우터 소유다(#80). `score-laps --by category` 에서 보험 5문항이 `policy` 23개에서 갈려 나왔고 **grounded 2/5 로 가장 약한 칸**임이 드러났다 — 고치는 것은 `D3` 몫 |
| A8 | ~~**`/life/walk-conditions?verbose=1`** — 판정 근거(축 · 관측값 · stale) 노출~~ | A6 에서 갈라져 나온 잔여 · RT-001 ⑥ | — | XS | 🚫 2026-09-03 사람 확정 — 근거는 `results[].data.now.axes` 와 aggregate 문장(859a691)으로 이미 나간다. §5 |

### B. 어댑터 — Card 1(#102)에 들어간 것과 남은 것

| # | 무엇 | 왜 | 전제 | 크기 | 상태 |
| --- | --- | --- | --- | --- | --- |
| B1 | 페이로드 — 실물은 `LifePayload(question, dog)` · `WalkPayload(lat, lon)` 이 `daengs_backend/orchestration/contracts.py` 에 있다. **`region` 은 끝까지 안 넣었다** — `A2`(RAG-063 ④)가 지역을 `query.text` 에서 읽기로 해서 페이로드 필드가 필요 없어졌다 | **2026-09-03 위치 결정 닫힘 — 계약 계층 잔류 수용** (§6). 필드 확장은 `contracts.py` 를 고치는 일이라 오케스트레이션 리뷰를 받는다 | — | S | ✅ 위치 결정 · ✅ **`dog` 확장 #202** (B4 가 가져갔다) · ✅ **`region` 은 안 넣기로 닫혔다** (RAG-063 ④ · #262) — 지역은 `query.text` 에서 읽는다 |
| B2 | 어댑터 — `services.ask.ask()` / `services.walk.walk()` in-process 호출, `HTTPException` → status 번역(404→ABSTAINED · 502/503→ERROR · 504→TIMEOUT · `unknown`→ABSTAINED), `elapsed_ms`, 질문 원문 비로깅 | D-035 · #80 불변식 7 · 11 | 없음 — 두 서비스는 이미 HTTP 무관(encoder·conn·client 주입) | S | ✅ #102 |
| B3 | 접점 테스트 갱신 — `daengs_backend → daengs_life` import 를 **어댑터 한 곳만** 허용 | D-018 "세 줄" · D-035 O-11 | B2 | XS | ✅ #102 |
| B4 | 반려견 컨텍스트 소비 — **견종 · 나이만.** `active_dog_id` → pets 조회 → `context["dog"]` → `LifePayload.dog` → 프롬프트 | #88 pets · #80 O-4. pets 에는 견종·성별·중성화·체중·생년월일뿐 — **등록 여부·기본 위치 컬럼은 없다**. 위치는 앱이 `location` 으로 보낸다 | #64 ✅ (fbc1540) · B1 위치 결정 ✅. 소유권 검증은 `services/chat.py` 가 이미 한다 | S~M | ✅ **#202 (2026-09-04)** — RAG-056. **근거 하나를 고쳐 적었다**: 앱 아바타 27종에 맹견 5종이 없어 그쪽은 사용자 경로에서 안 걸리고, 견종이 실제로 답을 가르는 자리는 **단두종(항공 운송)** 이다(퍼그·프렌치불독이 아바타에 있다). 나이는 `birth_date_kind='birthday'` 일 때만 센다 — `family_day` 는 나이가 아니다. **앱이 `active_dog_id` 를 보내야 켜지고, 그것이 `DAENGS_APP#112` 로 끝났다** (2026-09-04). lap19 30문항 cited 17 · grounded 15 · 지어낸 조항 0. 프로필 문항 둘 중 **DP2(나이)는 갈렸고 DP1(단두종)은 안 갈렸다** — 검색이 단두종 청크를 top-5 에 안 올려서다. 프로필은 프롬프트만 바꾸므로 그건 검색 쪽인데, **D5(RAG-057 ④)가 전역 상수로는 안 된다고 확정했다** — DP1 은 w≤0.20 을 요구하고 S3 는 w≥1.35 를 요구한다. 남은 자리는 D5e — ⚠ **그쪽이 아니었다**: `DP1` 은 `D11`(#270)의 어휘 다리가 순위 상수를 안 건드리고 풀었고, 그래서 `D5e` 는 닫혔다(#292) |

### C. 운영 — 다른 날 · 다른 자리

| # | 무엇 | 왜 | 전제 | 상태 |
| --- | --- | --- | --- | --- |
| C1 | #65 코퍼스 서버 이관 + crawler profile 제거 | RAG-050 | **서버 물리 접근** (USB · `.env`) | ✅ #65 (2026-08-31) |
| C2 | #66 법령·약관 개정 감지 | **RAG-054** · RAG-044 ⑤ · data-sources §11 | #61 (머지됨) | ✅ #66 (2026-08-31) |
| C3 | #67 seoul-notice-api 고시공고 | **RAG-053** · data-sources §11 | 서버 `.env` 키 · C1 | ✅ #67 (2026-08-31) |
| C4 | ~~data.go.kr 운영계정 전환 신청~~ | RT-001 ④-e 가 **P0** 로 꼽았던 근본 해결 | — | 🚫 **2026-09-02 사람 확정 — 하지 않는다.** 이유·파장은 §5 |
| C5 | RT-002 워커(캐시 예열) 기동 · `crawl_runs` running 잔존 행 · 알림 채널 (~~HNSW 인덱스~~ → **`D16` 으로 나갔다**, 2026-09-07) | RAG-044 · 047 ⑦ · 045 ⑧ · RT-002 | C1 (끝남) | ⚪ ⬜ **C 트랙에 남은 유일한 행** |

### D. 품질 — **`D10` ✅ 이 섰고 나머지는 틈에**

> 2026-09-05(#254): 순위 카드들은 **효과가 잡음보다 작아** `D10`·`D6` 뒤다 (§4). 예외가 `D10` 이었는데,
> 그것은 품질을 올리는 카드가 아니라 **품질을 재는 자를 고치는 카드**라 커버리지 카드보다 앞에 섰고 **#261 로 끝났다.**
> 남은 도구 카드는 `D6`(§4 의 9번)이고, 그것이 서야 순위 카드의 성패를 말할 수 있다.

| # | 무엇 | 왜 | 상태 |
| --- | --- | --- | --- |
| D1 | 지표를 문서 종류별로 슬라이스 | RAG-046 ⑦ · 052 · RAG-007 원안 — 비법령 소스가 늘수록 `cited` 가 구조적으로 눌림 | ✅ **#236 (2026-09-05)** — RAG-060. `score-laps` 에 `--by {trust_level,source_id,subcategory,category}` · `--laps N`. **주장은 맞았는데 축을 잘못 고르면 안 보인다** — `trust_level` 은 값이 둘뿐이고(`guideline` 0건) 조례(조 번호 있음)와 보조금24(없음)를 `official` 한 칸에 넣어 눌림을 지운다. `--by source_id` 로 보면 **편향이 둘이고 총계에서 상쇄**된다: 눌림(grounded > cited — 보조금24 · 항공 · SRT)과 부풀림(cited > grounded — knia · 법령). 귀속은 랩이 잡은 히트가 아니라 **골든셋 `must` 라벨**로 한다 — 히트로 하면 검색이 성공한 문항만 세어져 분모가 랩마다 흔들린다(착수 실측이 그 함정에 먼저 빠졌다) |
| D2 | 리랭커 재측정 (bge-reranker-v2-m3 / Cohere) | RAG-003 "recall 위에서 다시 잰다" — 하이브리드 뒤 아직 안 잼. 라우터 뒤라 **지연이 대가** | 🔵 ⬜ **전제는 섰고 시점도 왔다** (§4 의 12번). 미룬 유일한 근거가 *"발표 앞에서 시연을 느리게 한다"*(RAG-070 ⑤)였는데 **2026-09-07 에 발표가 순서의 축에서 빠졌다**(§6). 그리고 **`RAG-072` ⑤ 가 여기가 정면임을 수로 보였다** — `I4` 의 정답 면책 조가 dense 89·153위이고, 렉시컬은 dense 후보를 재정렬할 뿐 **후보를 못 들여온다**(③). ⚠ **지연은 그대로 대가다** — 응답 시간을 같이 잰다 |
| D3 | KB·농협 특별약관 레이아웃 경계 · 조 참조가 조 머리로 잡히는 것 | RAG-048 ⑧ · 051 — 재임베딩 동반. **`A1` ✅ #266 이 섰으므로 이제 바뀐 행만 다시 만든다** (§4 의 7번) | ✅ **#285 · #286** — **이 표 아래쪽의 `D3` 행이 실물이다** (같은 카드가 두 줄인 것은 자리표를 남겨 둔 결과다. 번호 주소를 지키려고 안 지운다) |
| D4 | **조 번호가 없는 소스의 인용** (옛 이름 "항·호 단위 인용 대조") | RAG-028 유보 ③ · **RAG-060 ③ 이 실물을 줬다** — `benefit24-services` 는 21개 랩 내내 `cited` 0 인데 답은 맞다. 조 번호가 없는 소스가 `cited` 로는 영영 0이라는 것이 이제 표에 보인다 | 🔵 ⬜ **13문항이고 전부 `grounded` 인데 `cited` 는 1이다** (RAG-070 ③ · `lap29`). `#287` 은 KPI 를 두 축으로 갈라 **수를 정직하게 보이게만** 했다 — 정의는 안 건드렸다. 이 행은 '구제'가 아니라 **무엇을 인용으로 셀 것인가**이고, §6 의 북극성과 저울질한다 |
| D5 | **검색 순위** — RRF 가중치 재측정 · `ts_rank_cd` | RAG-035 · RAG-055 ④(오기권 셋) · RAG-056(DP1) | ✅ **#208 (2026-09-04)** — RAG-057. **상수를 하나도 안 바꿨다.** 25문항·8,990문서로 다시 쟀는데 0.3 이 여전히 최선이고(0.40 이 1 더 얻지만 폭 0.08 짜리 턱, 이득은 문항 하나), 가중치×RRF_K 2차원 전수에서도 최대 19/36 을 못 넘는다. `CANDIDATE_N` 은 20~300 에서 한 칸도 안 움직였다. `ts_rank_cd` 기각 — 24/25 문항에서 `ts_rank` 와 렉시컬 순위가 같고 정규화 플래그는 전부 방향이 반대다. **못 닿는 넷이 셋으로 갈렸다**: S3 는 w≥1.35 · DP1 은 w≤0.20 을 요구해 **전역 상수로는 동시 불가**, Q3·B1 은 **과태료 별표 청킹**이 원인이다(별표 행들이 본문 대부분을 공유해 임베딩이 행을 구별 못 한다). 하네스 `backend/tools/search_sweep.py` 가 남았고 `verify` 가 실물 검색과 25/25 대조한다 |
| D5b | **과태료 별표 재청킹** — 행 청크에 위반행위 이름·근거 법조문을 싣는다 | RAG-057 ⑤ — Q3·B1 의 실제 원인. 답인 `별표 4-2-아`(20·30·50만원)가 dense 106·lex 147 이고 무관한 `4-2-4`(보험 미가입)가 dense 4~8 이다 | 🚫 **전제가 사라졌다** (RAG-070 ② · #287). 처방(*"위반행위 이름·근거 법조문을 싣는다"*)은 **이미 돼 있고**, 공통 머리를 걷어 내도 **질의 벡터에 `안전조치` 가 없어서** 안 올라온다 — `Q3`·`B1` 은 어휘 한 줄로 열렸다(`D13`). 관찰(66행이 머리를 공유한다)은 실재하지만 **원인이 아니라 증상**이었다. 🚫 **닫혔다** (2026-09-07 사람 · **#303** · RAG-073). 남아 있던 관찰(66행이 머리를 공유한다)을 재고 닫았다 — 쌍 코사인 **0.7999**(형제 조문 0.4896)로 **관찰은 실재하는데**, 머리를 걷어 내면 12문항 중 **좋아진 것이 0이고 나빠진 것이 11**이다(top-5 7 → 2). 머리를 행마다 다르게 하는 쪽은 유사도만 내려가고(0.80 → 0.75) **순위는 안 움직인다.** *행끼리의 유사도*와 *옳은 행을 고르는 일*이 다른 것이었다 |
| D5c | **인용 확장의 `법` 해소 + 항 단위 조회** | RAG-057 ⑥ — 시행령 별표는 근거를 `법 제101조제4항제4호` 로 적는데 맨몸 `법` 이 어떤 `document_title` 과도 안 맞고, 조문을 조 단위로만 읽는데 청크는 항 단위다. **별표 층의 확장 기여가 0이었다** | ✅ **#218 (2026-09-04)** — RAG-058. 시행령·시행규칙 안의 맨몸 `법` 을 모법으로 풀고(조례는 안 한다) 조문을 항까지 읽되 조로 폴백한다. **지표는 안 올랐다** — lap19 17·15 → lap20 18·16 인데 늘어난 둘은 DP2(RAG-056 이 예고한 라벨 확장)와 T3(비결정성)이지 이 카드가 아니다. **잃은 문항은 0**이고 거절 3/3 · 기권 지표도 그대로다. 실제 값은 **Q2 하나** — 과태료 주장이 시행령이 아니라 `제101조④` 에 붙었는데 그 청크가 `tier: nice` 라 **지표가 그 개선을 못 잰다**. 지표가 그대로인데 머지한 근거는 RAG-058 ④ 에 적었다 |
| D5c-1 | **확장의 관련성 신호** — 등장 순으로 3개를 고를 뿐 질의와의 관계를 안 본다 | RAG-058 ⑥ — 지금까지는 참조가 대부분 해소 실패로 떨어져 **그것이 우연히 필터 노릇**을 했는데 #218 이 그 우연을 없앴다. lap20 에서는 해가 없었지만 랩 하나의 증거다 | 🔵 ⬜ 잡음이 실제로 물리면 |
| D5d | **골든셋 라벨 보강** — Q3·B1 에 `별표 4-2-아` 를 OR 대안으로 | RAG-057 ⑦ — 가장 좋은 근거가 애초에 라벨에 없었다. RAG-029 가 예고한 자리 | ✅ **#240 (2026-09-05)** — RAG-061 ①. 요구 수는 그대로 두고(`must_total` 70) 주소만 80 → 82. **lap22 에서 효과는 0이고 그것이 예고된 결과다** — 검색이 별표를 top-8 에 안 올린다(RAG-057 ⑤ dense 106위). 라벨은 이제 맞고 **순위만 남아 D5b 를 기다린다** |
| D5e | **질의별 가중치** — S3 와 DP1 이 정반대를 요구한다 | RAG-057 ④ · RAG-056 ④ 가 넘긴 자리 | 🚫 **닫혔다 — 2026-09-07 사람 (#292).** 열 이유였던 *"S3 는 w≥1.35 · DP1 은 w≤0.20 이라 동시 불가"*(RAG-057 ④)가 **양쪽 다 사라졌다**: `A2`(#262)가 `S3` 를 필터로, `D11`(#270)이 `DP1` 을 어휘로 풀었고 **둘 다 순위 상수를 안 건드렸다.** 새로 생긴 순위 문제(`I4`)는 가중치가 아니라 **부정 표현**이라 이름이 다르다 — `D14`(§4 의 10b)가 그 자리를 이어받았다. 권고는 RAG-070 ⑤, 결정은 이 카드다. ⚠ **그 `D14` 도 곧 닫혔다**(#296) — `I4` 의 원인이 순위 상수가 아니라 **dense recall** 이었다 (RAG-072) |
| D6 | RAG-007 축소판 — **랩 대조를 총계가 아니라 문항 단위로**. judge 는 하지 않음 | RAG-007 (제안 상태) · RAG-006 · **RAG-071 (#292)** | ⚪ **#292 (2026-09-07)** — `score-laps --against <기준선랩>`. 뒤집힌 문항을 이름으로 찍고 **잡음 띠**(최근 N랩에서 몇 번 흔들렸나)를 옆에 붙인다. **임계값으로 경고하지 않는다** — 잡음이 ±2~3 인데 순위 카드 효과가 1~2문항이라 임계값을 잡음 위에 두면 아무것도 안 잡히고 아래에 두면 매번 운다. 종료 코드는 **문항 단위**로 낸다 |
| D7 | `rag status` · `BY_SOURCE` 단일화 · `test_evaluate` bge-m3 skip 가드 · **`generate --model` help 문구** | RAG-023 · 042 ⑥ · 052 ⑥ · **RAG-061 (#240)** — help 는 `기본 bge-m3` 라고 적는데 실제 기본은 `config.settings.embedding_model_key`(= `qwen3-embedding-0.6b`)다. 결과는 맞지만 **CLAUDE.md 가 "조용히 틀린다"고 경고하는 자리 바로 옆**이라, 읽는 사람이 `--model` 을 손으로 줄 때 위험하다 | ⚪ ◐ **help 문구만 #292 가 고쳤다** — `search`·`generate` 의 `--model` 을 `load` 와 같은 표기로 맞추고, 같은 줄의 `--category` 에 빠져 있던 `insurance`(#271 이 만든 값)를 넣었다. **남은 셋**(`rag status` · `BY_SOURCE` 단일화 · `test_evaluate` bge-m3 skip 가드)**은 그대로 열려 있다** |
| D8 | **부풀림 쪽 확인** — `knia-disclosure` 2문항이 `cited` 2 · `grounded` 0 이다 | RAG-060 ⑦ — #236 이 보이게만 하고 안 봤다 | ✅ **#240 (2026-09-05)** — RAG-061 ②. **라벨이 좁은 게 아니라 질문이 덜 정해져 있었다** — I5 의 라벨은 삼성화재 표를 요구하는데 lap21 은 KB 표로 답했고 **둘 다 사실**이다(`자기부담금` 을 말하는 청크가 199개). knia 의 `-rN` 은 담보가 달라 FW1 식 형제 행이 아니라, 넓히는 대신 **질문이 회사를 특정하도록 다시 썼다**. lap22 에서 그 칸이 `2/0·2` → **`0/1·2`** — 부풀림이 사라지고 근거를 잡기 시작했다. **I2 는 아직 0이고 남긴다**(검색은 must 를 3위로 올렸는데 답이 지목을 안 한다 — 생성 쪽) |
| D9 | `score-laps` **총계 표의 랩 순서** — 파일명 순이라 `lap10` < `lap2` 다 | RAG-060 ⑦ — 슬라이스 표에서만 숫자 순으로 세웠다. `io.answer_files()` 를 고치면 RAG-029 이후 기록들이 인용하는 표의 순서가 바뀐다 | ⚪ ✅ **#292 (2026-09-07)** — **랩 30개가 되며 실제로 물렸다**: 파일명 순의 마지막 줄이 `lap30` 이 아니라 **`lap9-age`** 였다(저장된 33개 실측). `io.answer_files()` 는 안 건드리고 `cmd_score_laps` 가 `_lap_key` 로 다시 세운다 — **수는 하나도 안 바뀌고 줄 순서만** 바뀐다 |
| **D11** | **질의 어휘 다리** — 사람 말(`아파트`·`화장장`·`비행기`)을 조문 말(`공동주택`·`장묘시설`·`항공`)로 넓혀 **인코딩한다** | **RAG-065 ⑨ 가 연 자리.** `HS1` 의 실패가 순위가 아니라 어휘였다 — 문서도 임베딩도 순위 공식도 멀쩡한데 질의와 조문에 겹치는 낱말이 하나도 없다. `FW3`(화장장/장묘시설)·`DP1`(비행기/항공)도 같은 병이었고, RAG-059 가 `FW3` 를 *"조례가 top-5 를 차지한다"* 로 적은 것은 **조례가 이긴 이유**를 못 본 것이었다 | 🔵 **#270 (2026-09-06)** — RAG-066 ③④. 세부는 그 기록에 |
| **D3** | **약관 절 경계** — 잘린 문장이 제목으로 잡힌다 | `A7`(#271)이 `insurance 3/2·5` 를 드러내며 열린 자리. PDF 가 줄폭으로 문장을 자르는데 정규식이 "줄 끝이 약관 이름"만 본다 | ✅ **#285 (2026-09-06)** — RAG-068. **`D12`(#286)가 닫았다**(④ 의 진단 절반이 채점기 버그였다). 조각 **930 → 0**(농협 589 · KB 341 · 삼성 0). **다만 지표는 안 움직였다** — 보험 문항의 `must` 가 KB·농협을 안 가리킨다(④). 고친 것은 골든셋이 안 재는 축(인용의 읽히는 형태)이다 |
| **D10** | **경계 문항을 `cited`/`grounded` 분모에서 뺀다** + **조 번호 있음/없음** 축 | **#254 실측**(§1) — `B6` 은 거절문의 "제10조"가 `cited` 로 잡히고 **`B3` 은 놓친 기권(실패)인데 총계에서 성공으로 센다.** 같은 랩의 기대 채점표는 `놓친 기권 2/2` 라 **두 표가 서로 반대를 말한다** | ✅ **#261 (2026-09-05)** — RAG-062. `score_rows(rows, ckinds)` 로 경계 문항을 뺐고, **뺀 수를 `boundary` 로 같이 낸다**(분모가 왜 줄었는지가 표에서 보여야 한다). **조 번호 축은 골든셋 `must` 앵커로 정해 코퍼스를 안 본다** — 그래서 "랩 파일만 있으면 돈다"는 총계 표의 약속이 안 깨진다. **`slice_rows` 의 불변식을 다시 썼다**(칸을 지우는 대신). **lap22 `19·18/33` → `17/28 · 18/28`** 이고 내려간 둘이 `B3`·`B6` 이다 — 회귀가 아니라 부풀림이 걷혔다. `옛 표기` 열로 소급 대조 |
| **D12** | **근거 표기 `[1, 2]` 채점** — 한 괄호 안의 여럿을 통째로 놓쳤다 | `D3` 뒤에 답변 본문을 읽다 나왔다. 저장된 랩 전체에서 **90건 · 24종**이고 예외도 경고도 없이 `grounded` 만 낮게 나왔다 | ✅ **#286 (2026-09-07)** — RAG-069. 랩 파일을 안 건드리고 소급된다(`lap28` 26→**28**). 그 수 위에서 `I1`·`I4` 의 질문을 상품 이름까지 좁혀 `I1` 이 열렸다 |
| **D13** | **`목줄` → `안전조치`** + **KPI 두 축** | `D5b`(§4 의 8번)를 열려다 **그 전제가 없다는 것을 찾았다.** 시연 첫 질문이 21랩 동안 안 나왔는데 원인이 청킹이 아니라 어휘였다 | ✅ **#287 (2026-09-07)** — RAG-070. `Q3`·`B1` 이 열렸고(dense 106위 → 1위) 총계 칸은 안 건드린 채 `KPI` 줄만 더했다 |
| **D14** | **부정 표현이 벡터에서 지워진다** | `I4` 하나가 남긴 자리다 (RAG-069 ⑤) — 「지급하지 **않는** 사유」로 물으면 「보험금의 **지급사유**」가 1·2위로 온다. **어휘 다리로는 못 고친다**(낱말을 더하는 도구인데 여기는 지워지는 문제다) | 🚫 **닫혔다 — 2026-09-07 사람 (#296 · RAG-072).** 전제의 **절반만 맞았다**: 부정이 지워지는 것은 실물로 확인했는데(`content_tokens` 에 `않` 이 든 청크 **0/9,838** — Kiwi 가 `않/VX` 를 내는데 **한 글자 필터**가 버린다), **고쳐도 `I4` 는 안 열린다**(`제6조` 최종 110 → 108위). 정찰하다 더 큰 것이 나왔다 — **렉시컬 축은 dense 후보를 재정렬할 뿐 후보를 못 들여온다**(RAG-072 ③). 정면은 dense 이고 그것은 `D2` 다 |
| **D15** | **LLM judge — 골든셋 채점을 "답이 맞는가"까지** | **RAG-007 원안이고 §5 가 닫아 뒀던 자리다.** 닫은 이유는 *"캘리브레이션 비용 대비 22문항"* 이었는데 **전제 둘이 다 바뀌었다** — 문항이 33이고, `D10`(#261)·`D12`(#286)·`D6`(#292)이 서면서 **judge 를 캘리브레이션할 자가 생겼다.** 값의 실물은 `RAG-072` 의 `I4` 다 — 생성이 *"부담보 특별약관이 부가된 경우 지급하지 않습니다"* 라고 답했고 **틀린 말이 아닌데 물은 것의 답이 아닌데**, `cited`·`grounded` 로는 그 구분이 안 난다 | 🔵⚪ ⬜ **2026-09-07 사람이 다시 열었다** (§4 의 11번). ⚠ **#277 과 다른 것이다** — 저쪽은 라우터·어시스턴트의 **답변률과 답변 품질**을 생성 질문으로 재고 라우터 소유다. 여기는 **Life 골든셋 채점**이다. ◐ **#305 (2026-09-07) — RAG-074.** `rag judge` 가 섰고 `lap30` 을 쟀다. **`I4` 는 안 걸렸다** — judge 가 *"한 가지 경우를 직접 제시했다"* 며 통과시켰고, judge 가 무른 것인지 사람이 엄했던 것인지는 **캘리브레이션이 가른다.** 잡은 것은 `T3` 하나(물음의 절반만 채운 답). **캘리브레이션 1차 #311 — 일치 3/4** (RAG-075). `I4` 는 **사람도 「답함」** 이라 `RAG-072` 의 읽기를 정정했고, judge 의 오답은 **유일한 「못함」**(`T3`)이었다 — 무른 게 아니라 **엄한 쪽으로** 틀렸다. **지표 승격은 라벨 30개 뒤**다 |
| **D16** | **적재 증분화 + HNSW 인덱스** | **`C5` 의 `HNSW` 항목이 여기로 나왔다.** 인덱스만 켜면 `rag load` 가 전 행을 upsert 하는 탓에 **매일 인덱스를 통째로 갈아 낸다** — 그래서 적재 증분화가 짝이다. 근거는 `load.py` 의 `UPSERT` 와 `A1`(RAG-064)의 판정이 **적재까지 안 왔다**는 것 | ⚪ ⬜ §4 의 13b. **급하지 않다** — dense 전수 스캔이 142ms 이고, 켜는 이유는 코퍼스가 커질 때다. ⚠ 근사라 **기준선이 이동한다** — `D6` 으로 재고 `ef_search` 를 훑는다 |

### E. `/life/walk-conditions` 검증 부채 — 실측 없이 코드에 들어간 것

| # | 무엇 | 왜 | 상태 |
| --- | --- | --- | --- |
| E1 | **비 오는 날 프로브 재실행** — `1.0~29.9mm` 구간 실수값의 실제 문자열 | RT-001 ②-d-1. RT-004 가 문서에 적힌 표기(`-`·빈 값 포함)는 다 받게 했으나 **실물 확인은 남음** | ⚪ ⬜ 비 오는 날 (범위 줄었음) |
| E2 | ~~LGT(낙뢰) 도메인 확정~~ + 초단기 PTY 5·6·7 | 활용가이드로 확정 — 에너지밀도 0.2~100kA/㎢ 라 `> 0` 이 맞다. PTY 5·6·7 도 같이 닫음 | ✅ RT-004 (#216) |
| E3 | **UV 값 확인 후 표현 확정** | ②-d-3. apihub 403 이라 응답 자체를 못 봄 — 외부 의존 | ⚪ ⬜ 막힘 |
| ~~E3'~~ | ~~체감온도 산식 검산~~ | 여름식·겨울식·Stull 근사 셋 다 공표형과 계수까지 항등 확인 | ✅ RT-004 (#216) |
| ~~E3"~~ | ~~프리페치 N 재역산~~ | C4(운영계정 전환) 🚫 로 전제가 사라짐 | ✅ 닫힘 (§5) |

### F. 범주 확장 — **전제(A3a ✅ #177)가 서서 지금 열린다**

`documents.category` 는 `policy` · `travel` · `food` 이고 `food` 가 비어 있다 (RAG-008 ③ — 나간 팀원 몫이었다). 새 값은 `CHECK` 한 장으로
는다(RAG-008 ③ "제약만 교체"). **넣을지의 기준은 기술이 아니라 §2 의 경계 — 인용할 문서가 있는가.** 소스는 카드 앞에 **정찰 카드로 먼저**
연다 (`data-sources.md` §10 의 교훈 — "verified" 였는데 죽어 있던 URL, robots 로 9곳 중 7곳).

| # | 범주 | 형태 | 소스 | 경계 | 상태 |
| --- | --- | --- | --- | --- | --- |
| F0 | **정찰** — 독성 식품 목록 · 공동주택 표준관리규약 · 재난 시 동반 지침 | — | URL · robots · 본문 유무 | S | ✅ **#256 (2026-09-05)** — 결과는 `data-sources.md` §10 "2026-09-05 정찰". **셋 중 하나만 그대로 열렸다**: 사료 법령·고시 ✅ · 공동주택 **법령** ✅ · 준칙 본문 ❓(법령 DB 에 없다) · 재난 지침 ❓(문서 없음) · 독성 식품 ◐(실물은 있는데 경계와 겹쳐 사람 결정) |
| F1 | **음식 · 사료** (`food`) | RAG | **`nias-pet` 해설 3장 + 사료관리법 3법.** 「사료 등의 기준 및 규격」 고시는 **뺐다** — 별표 1이 초콜릿부산물을 허용 원료로 적어 nias 의 "급여 금지"와 부딪힌다 (RAG-065 ②) | "먹였어요" 는 `none`, "먹었어요" 는 `emergency` REFUSED (A3a·B6) | ✅ **#268 (2026-09-06)** — RAG-065. 독성 식품은 **넣되 증상 문장을 잘랐다**(사람 결정). 12항목 중 4개는 본문이 통째로 비는데 **이름이 곧 답이라 정상**이다. `FD1`·`FD2`·`FD4` 가 lap25 에서 근거를 잡았고 `B6`(초콜릿 먹었어요)은 여전히 거절한다 — 그 문항이 이제야 실제로 시험된다 |
| F2 | **분실 · 유기** | **툴** (`/life/walk-conditions` 형) + 기존 제도(변경신고 30일) | data.go.kr 유기동물 조회 API — 열림, 활용신청 | 없음 | 🟢 ⬜ |
| F3 | **사망 · 장례** | RAG — 골든셋 문항 몇 개 | 장묘업 허가 · 변경신고 조항 **이미 코퍼스에** | 업체 위치는 `place` | ✅ **#217 (2026-09-04)** — RAG-059. 문항 셋(FW1 등록 변경신고 · FW2 업체 허가 확인 · FW3 장묘시설 설치 제한). **셋 중 둘은 답이 옳은데 라벨이 좁아 0점이었고**(FW1 은 해설을 OR 대안으로, FW2 는 `제72조의2`→easylaw 로 고쳤다 — 실제 확인 경로가 **국가동물보호정보시스템**이지 장묘정보시스템이 아니었다) **FW3 은 진짜 실패라 그대로 뒀다**(조례가 top-5 를 차지해 `법#제72조` 가 안 온다 — D5 계열). 넓힌 라벨은 **lap22 부터** 반영된다. 사체 처리(폐기물관리법)는 코퍼스에 없어 뺐다 |
| F4 | 주거 · 이웃 (관리규약 · 소음 민원) | RAG | ⓐ `공동주택관리법` 3법 ✅ **#268** · ⓑ 관리규약 준칙 본문 ❓ **법령 DB 에 없다**(17개 시·도가 각자 PDF/HWP) | 짖음 **교정**은 `training` | ✅ **ⓐ 는 #270 에서 닫혔다.** #268 이 문서를 들였는데 사용자 어휘로 안 잡혔고(조문은 `공동주택`·**`가축`**·`관리주체` 라고 쓴다), `D11`(RAG-066)이 질의를 넓혀 `HS1` 이 시행령 제19조제2항을 **1위로 잡고 조 번호까지 인용**한다. ⓑ 는 §4 의 16번 |
| F5 | **일정 · 알림** — 변경신고 기한 · 접종 주기 · 보험 갱신 · **사망 후 30일 변경신고**(조문 용어는 변경신고, 법제처 해설은 "말소신고" — RAG-059 ④) | proactive (제도 지식 + 프로필 + Beat) | 코퍼스 + #64 + **`pets.registered` 컬럼(신규 — DB 카드, 두 DB 손 적용, 앱 입력 화면 동반)**. `farewell_on` 은 이미 있다 | — | 🟢 ⬜ Card 1 뒤. 등록 여부는 2026-09-03 B4 에서 여기로 옮김 — 그 값의 첫 소비자가 이 행이라서 |

---

## 4. 순서

**위에서 아래로 한 줄씩 내려간다.** 날짜로 묶지 않고, Iteration 도 여기서 배정하지 않는다 —
**무엇 다음에 무엇인가**만 정한다. 카드를 몇 장으로 쪼갤지와 어느 Iteration 에 넣을지는
카드를 여는 시점에 사람이 정한다 (`collaboration.md` §1 · §3).

> 2026-09-05 정정 — 앞선 판(#254)은 이 자리를 **Iteration 네 칸 표**로 그리고 09-21 앞뒤로 갈랐다.
> 그러면 **칸 수가 로드맵이 담을 양을 정해 버린다.** `collaboration.md` §3 의 "카드 하나 = PR 하나"는
> 카드를 **쪼갤 때** 쓰는 규칙이지 **로드맵의 상한**이 아니다. 기간이 짧으면 아래에서 덜 내려갈 뿐이고,
> 그 판단은 순서를 아는 사람이 그때 한다.

| # | 카드 | 축 | 재적재 | 왜 이 자리인가 · 전제 |
| --- | --- | --- | --- | --- |
| ~~1~~ | ✅ **`D10`** 경계 문항을 지표 분모에서 뺀다 (**#261** · RAG-062) | ⚪ | — | **측정 도구가 먼저였다.** 이제 총계가 `B3`(놓친 기권 = 실패)를 성공으로 세지 않는다. 아래 카드들의 "올랐다/내렸다"는 이 자 위에서 읽는다 — **다만 이 커밋 앞뒤로 `cited` 를 비교하면 안 된다** |
| ~~2~~ | ✅ **`A2`** 지역 필터 (**#262** · RAG-063) | 🟢 | — (메타만) | **커버리지가 가장 크게 늘 자리였고 실제로 늘었다** — 조례 문항 S1~S5 의 must 가 전부 1위가 됐다(전에는 S3 가 top-8 밖). `db/migrations/` 1장은 **두 DB 에 각각** 적용한다. 재임베딩이 없어 GCP 는 덤프·복원이 아니라 같은 파일 재적용으로 끝난다 |
| ~~3~~ | ✅ **`A1`** 문서 단위 증분 임베딩 (**#266** · RAG-064) | ⚪ | — | **아래 넷(4·5·7·8)이 재인코딩을 동반한다.** 이제 소스 하나가 바뀌면 **바뀐 행만** 만든다 — 조례 3청크 수정이 55분에서 몇 초가 됐다. ⚠ `A7`(6번)은 `category` 만 바꾸므로 재인코딩이 0이다 |
| ~~4~~ | ✅ **`F1`** 음식 · 사료 (`food`) (**#268** · RAG-065) | 🟢 | 증분 | **정찰이 계획을 바꿨다.** 사료 3법·고시는 사업자 규제였고(`반려동물` 0회), **고시 2.64MB 는 뺐다** — 별표 1이 초콜릿부산물을 허용 원료로 적어 nias 의 "급여 금지"와 부딪힌다. 값은 `nias-pet` 해설 3장이 가져왔다(37장 중 7장만 받고 있었다). 독성 식품은 **넣되 증상 문장을 잘랐다**(사람 결정). `FD1`·`FD2`·`FD4` 가 lap25 에서 답한다 |
| ~~5~~ | ✅ **`F4`-ⓐ** 공동주택관리법 3법 (**#268** · RAG-065 → **#270** 이 닫음) | 🟢 | 증분 | #268 이 문서를 들였을 때는 **사용자 어휘로 안 잡혔다** — 조문은 `공동주택`·**`가축`**·`관리주체` 라고 쓰는데 사람은 `아파트`·`반려견`·`관리사무소` 라고 친다. **`D11`(#270)이 질의를 넓혀 닫았다** — `HS1` 이 시행령 제19조제2항을 1위로 잡고 조 번호까지 인용한다. ⚠ 2026-09-06 에 이 행이 `◐` 인 채 "남은 일은 15번"을 가리키고 있어 **사람이 5번을 남은 것으로 읽었다** — 15번도 그때 이미 닫혀 있었다 (#285 가 정정) |
| ~~6~~ | ✅ **`A7`** `insurance` 를 `policy` 에서 분리 (**#271** · RAG-067) | 🟢 | 재인코딩 0 | **코퍼스의 47.5%가 한 칸에 있었다** — policy 9,309 → insurance 4,673 · policy 4,636. ⚠ **"적재만"이 아니었다** — `category` 는 크롤 시점에 `.meta.json` 에 박히므로 클래스만 고쳐서는 안 바뀐다(RAG-067 ②). 값은 **지표 축**이다 — 서빙이 `category` 를 안 넘겨 검색 필터 쪽 소비자는 아직 없다(①). 갈리자마자 `insurance 3/2·5` 가 보였다 |
| ~~7~~ | ✅ **`D3`** 농협·KB 약관 절 경계 (**#285** · RAG-068) → **`D12`** 가 닫음 (**#286**) | 🔵 | 증분 1,113행 | **결함은 고쳤고 지표는 못 고쳤다.** 잘린 문장이 약관 제목으로 잡혀 인용이 문장 중간에서 시작하던 것을 **930 → 0** 으로 없앴다(농협 589 · KB 341 · 삼성 0). 그런데 **`insurance` 다섯 문항의 `must` 가 하나도 KB·농협을 안 가리킨다** — `hits`(검색이 물어 온 것)를 보고 `must`(정답)를 짐작한 것이 틀렸다. 지표는 `3/2·5 → 2/2·5` 로 안 움직였다. 남은 원인은 **생성**(`I5` 는 must 가 1위인데 안 지목)과 **삼성·knia 쪽 검색**이고 둘 다 별도 카드다 (RAG-068 ④⑥). ⚠ **그 진단도 절반이 틀렸다** — `D12`(#286)가 채점기 버그를 걷어내니 `I5` 는 원래 `grounded` 였다. `I1` 은 질문이 회사를 안 가리켜서였고(문항을 좁혀 열렸다), 남은 `I4` 하나만 진짜 검색 문제다 (RAG-069 ⑤) |
| ~~7b~~ | ✅ **`D12`** 근거 표기 `[1, 2]` 채점 (**#286** · RAG-069) | 🔵 | — | **21랩 동안 `grounded` 가 눌려 있었다.** 정규식이 한 괄호 안의 여럿을 못 읽어 90건을 통째로 놓쳤다 — 예외도 경고도 없이 수만 낮게 나온다. 랩 파일을 안 건드리고 소급된다(③). 그 고쳐진 수 위에서 `I1`·`I4` 의 질문을 **상품 이름까지 좁혔고**(RAG-061 ② 의 전례 — 후보가 36개·109개라 OR 로는 못 담는다) `I1` 이 열렸다. `insurance` **3 → 4/5**. `I4` 는 남았는데 **원인이 바뀌었다** — 히트 5개가 전부 옳은 상품인데 「보험금의 **지급사유**」가 「지급하지 **않는** 사유」 질의의 1·2위다 (RAG-069 ⑤) |
| ~~8~~ | 🚫 **`D5b`** 과태료 별표 재청킹 — **닫혔다** (2026-09-07 사람 · **#303** · RAG-073) | 🔵 | — | **관찰은 맞고 처방이 안 섰다.** 66행이 머리를 공유하는 것은 실재한다 — 쌍 코사인 **0.7999** 로 형제 조문(0.4896)·무작위 쌍(0.3909)보다 훨씬 가깝다. 그런데 **그 머리를 걷어 내면 전부 나빠진다**: 행을 겨냥한 질의 12개에서 **좋아진 문항 0 · 나빠진 문항 11**, top-5 가 7 → 2 다(목줄 3위 → 98위). 머리는 잡음이 아니라 **`동물보호법`·`과태료의 부과기준` 을 지고 있던 유일한 문장**이었다 — 본문에 그 낱말이 없다. 머리를 **행마다 다르게** 만드는 강한 해석도 무승부다 — 유사도는 0.80 → 0.75 로 내려가는데 **top-5·top-20 이 한 칸도 안 움직인다.** ⚠ **진짜 소득은 ⑤ 다** — 크게 실패한 둘이 **어휘**(`입양`·`명의 변경` → `소유권 이전`, 1,475위)와 **참조 확장**(맹견 출입금지는 이미 1~4위인데 과태료 행만 42위)이었고 둘 다 청킹이 아니다. `D5`-계열 다섯이 다 같은 모양으로 끝났다 |
| ~~9~~ | ✅ **`D6`** 랩 대조를 문항 단위로 (**#292** · RAG-071) | ⚪ | — | **이제 순위 카드를 잴 수 있다.** `score-laps --against` 가 뒤집힌 문항을 이름으로 찍고 잡음 띠를 옆에 붙인다. 합격 기준이던 `lap29 → lap30` 재현이 됐다 — 뒤집힌 9문항 중 **두 축이 같이 움직인 것이 `Q3`·`B1` 둘**이고, 그것이 RAG-070 ④ 가 손으로 귀속한 답과 같다. **임계값은 안 만들었다** — 효과(1~2문항)가 잡음(±2~3)보다 작아서 임계값이라는 도구 자체가 안 맞는다 |
| ~~10~~ | 🚫 **`D5e`** 질의별 가중치 — **닫혔다** (2026-09-07 사람 · **#292**) | 🔵 | — | 열 이유였던 *"S3 는 w≥1.35 · DP1 은 w≤0.20 이라 동시 불가"*(RAG-057 ④)가 **양쪽 다 사라졌다** — `A2`(#262)가 `S3` 를, `D11`(#270)이 `DP1` 을 풀었고 **둘 다 순위 상수를 안 건드렸다.** 새 순위 문제(`I4`)는 이름이 달라 아래 10b 가 이어받는다. 권고는 RAG-070 ⑤ 였고 결정은 사람이 했다 |
| ~~10b~~ | 🚫 **`D14`** 부정 표현이 벡터에서 지워진다 — **닫혔다** (2026-09-07 사람 · **#296** · RAG-072) | 🔵 | — | **전제의 절반만 맞았다.** 부정이 지워지는 것은 실물이다(`content_tokens` 에 `않` 0/9,838). 그런데 **고쳐도 `I4` 는 안 열리고**(최종 110 → 108위), `N`×`w` 아홉 칸 어디서도 top-5 에 못 든다. 원인은 **dense 가 면책 조를 89·153위로 본다**는 것이고 RRF 상수로 못 넘는다 — 정면은 `D2`(11번)다. **파일은 하나도 안 고쳤다**: 카드가 세운 게이트에서 멈췄다 (`D5b`·`D5`·`D5d` 와 같은 모양) |
| ~~11~~ | ◐ **`D15`** LLM judge — **judge 는 섰고 지표는 아직이다** (**#305** · RAG-074) | 🔵⚪ | — | `rag judge <랩>` 이 랩 하나를 채점한다. 축은 **하나**(`answers_question`)이고 `question`·`text` 만 봐서 **랩 파일만 있으면 돈다.** ⚠ **결과가 예상과 반대다** — 카드를 여는 근거였던 `I4` 를 **judge 가 통과시켰다**(*"지급하지 않는 한 가지 경우를 직접 제시하고 있다"*). judge 가 무른 것인지 `RAG-072` 의 사람 판단이 엄했던 것인지 **지금은 못 정한다** — 그것을 가르는 것이 캘리브레이션이고, `RAG-007` 이 사람 라벨 30개를 요구한 이유가 실물로 나왔다. **판정 32/33 이라 기저율이 97%** 여서 이 축은 아직 문항을 못 가른다. **진짜로 잡은 것은 `T3` 하나** — 기차 무게 제한을 물었는데 SRT 만 답했고, `grounded` 는 그걸 성공으로 센다. 🔴 **프롬프트를 `I4` 가 걸리게 고치지 않았다** — 그러면 `RAG-070` ② 와 같은 실수다(판정을 보고 프롬프트를 짐작). **캘리브레이션 1차(#311 · RAG-075) — 일치 3/4.** 🔴 **`I4` 는 사람도 「답함」이었다** — `RAG-072` 의 내 읽기가 틀렸고 정정했다(검색 사실 ③④⑤ 는 그대로다). 그리고 **judge 는 무르지 않았다** — 오답이 **유일한 「못함」 판정**(`T3`)이어서, 이 랩에서 이 축이 잡은 진짜는 **0건**이다. 나머지 셋은 전부 `grounded ✗ · judge ✓` 이고 **`D4` 를 가리킨다.** 남은 것은 §6 의 **라벨을 30개까지**이고 그때까지 `score-laps` 에 열을 안 더한다 |
| 12 | **`D2`** 리랭커 재측정 (bge-reranker-v2-m3 / Cohere) | 🔵 | — | **미룰 이유가 사라졌다.** 유일한 근거가 *"발표 시연을 느리게 한다"*(RAG-070 ⑤)였는데 발표가 순서의 축에서 빠졌다. 전제는 이미 다 서 있었고(하이브리드 RAG-035 · 지역 필터 `A2` ✅ #262), **`RAG-072` ⑤ 가 여기가 정면임을 수로 보였다** — `I4` 의 정답 면책 조가 dense **89·153위**이고 RRF 상수로는 못 넘는다(렉시컬은 dense 후보를 재정렬할 뿐 **후보를 못 들여온다**). ⚠ **지연은 그대로 대가다** — 라우터 뒤에 붙으므로 응답 시간을 같이 잰다. ⚠ **전수 스캔 위에서 잰다** — 지금 dense 는 인덱스가 없어 **정확한 최근접**이고, 그것이 리랭커의 바닥선이다. 근사 인덱스(HNSW)를 먼저 켜면 **후보 recall 이 깎인 자리에서 재는 것**이 되어 리랭커의 몫과 인덱스의 손해가 섞인다 (`13b` 참고) |
| 13 | **GCP 크롤~적재 이전** — `docs/deploy/roadmap.md` §7-1 | ⚪ | — | **위 둘과 병렬이고, 아래 🟢 의 비용을 낮춘다.** 지금 코퍼스 갱신이 개발 PC 에 묶여 있고 GCP 반영이 손작업이다 — 그것이 이 표의 `✔ 재적재` 표시가 실제로 뜻하는 값이다. **내용은 저쪽에 있고 여기서 다시 적지 않는다**(같은 결정의 두 번째 주장을 만들지 않는다). 2026-09-07 에 사람이 그 문서의 *"9/21 이후"* 칸에서 끌어올렸다. ⚠ `#290` 이 문서로 만든 수동 절차는 **이 카드까지의 임시**다 |
| 13b | **`D16`** 적재 증분화 + HNSW 인덱스 | ⚪ | — | **둘을 한 카드로 묶는 이유가 있다 — 순서가 반대면 손해만 본다.** ⏱ **두 반쪽의 시점이 다르다**: 적재 증분화는 **13번과 함께 필요하고**(매일 도는 순간 값이 난다), HNSW 켜기는 **16번 뒤라도 된다**(아래 ④). 한 카드에 두는 것은 **HNSW 를 먼저 켜면 안 되기 때문**이다. ① `rag load` 는 지금 **전 행을 upsert 한다**(`ON CONFLICT DO UPDATE SET embedding …`). `A1`(#266)이 증분으로 만든 것은 **임베딩 단계**이고 적재는 재사용한 벡터까지 다시 쓴다. HNSW 가 걸린 뒤에도 그러면 **매일 세 줄이 바뀌어도 9,838개 항목이 그래프에서 전부 갈린다** (UPDATE = 인덱스에서 삭제+삽입, 죽은 항목은 VACUUM 전까지 남는다). ② 그래서 **적재를 진짜 증분으로 만드는 것이 먼저**다 — `content_sha256` 판정이 `A1` 에 이미 있으니 그것을 적재까지 끌어오는 일이다. ③ **HNSW 는 IVFFlat 과 달리 지웠다 다시 만들 필요가 없다** — 학습 단계가 없고 증분으로 갱신한다(`db/indexes.sql` 의 "적재 뒤 수동"은 HNSW 에 대해서는 **속도** 이야기다). 2026-09-07 실측(트랜잭션 안에서 만들고 롤백) — **9,838행에 빌드 2.4초 · 인덱스 77MB**(테이블 46MB). ④ ⚠ **서두를 이유가 없다** — GCP 운영에서 dense 전수 스캔이 **142ms**(전부 캐시 히트)다. 켜는 진짜 이유는 **코퍼스가 커질 때**이고 그것이 16번(`F4`-ⓑ) 뒤다. ⑤ ⚠ **HNSW 는 근사라 켜는 순간 결과가 바뀐다** — 랩 30개가 전부 전수 스캔 위에서 잰 수라 **기준선이 통째로 이동한다.** `D6` 의 `--against` 로 재고 `ef_search` 를 같이 훑는다 |
| 14 | **`D4`** 조 번호가 없는 소스의 인용 | 🔵 | — | **작업이 아니라 사람 결정이 먼저다.** `D10`(#261)이 갈라 놓고 나니 13문항이 **전부 `grounded`** 인데 `cited` 는 1이다(RAG-070 ③) — 보조금24 · knia 공시 · 항공사 안내 · SRT 약관 · easylaw/nias 해설에는 **인용할 조 번호가 애초에 없다.** 그래서 '구제'가 아니라 **무엇을 인용으로 셀 것인가**이고, §6 에 열린 항목으로 있다. ⚠ `D15`(11번)가 서면 이 물음의 답이 달라질 수 있다 — judge 는 조 번호 없이도 *"답이 맞나"* 를 잰다 |
| 16 | **`F4`-ⓑ** 관리규약 준칙 17개 시·도 | 🟢 | ✔ | 법령 DB 에 없어 시·도별 PDF·HWP 를 각각 받아야 한다 (#256). **비싼 쪽이라 뒤**이고, ⓐ 가 이미 답하는 질문이 얼마나 되는지 보고 판단한다 |
| 17 | **`F5`** 일정 · 알림 (proactive) | 🟢 | — | ✅ **컬럼은 나갔다** (**#288**, 2026-09-07) — `pets.registered BOOLEAN`(NULL 은 '모름', **기본값 없음**). 집 서버 DB 적용 완료, **GCP 에도 실렸다**(#289 · 2026-09-07). **남은 전제는 앱 입력 화면**(`DAENGS_APP`)이고, 그 값이 채워져야 이 행이 선다 |
| ~~15~~ | ✅ **`D11`** 질의 어휘 다리 (**#270** · RAG-066) | 🔵 | — | **정찰이 예측한 셋이 그대로 살아났다** — `HS1`·`FW3`·`DP1` 이 전부 `grounded` 가 됐고 앞의 둘은 조 번호까지 인용한다. 대조 랩(`lap27`)과 갈라 재서 `org` 복구의 몫과 섞이지 않게 했다. **`FW3` 는 RAG-059 가 순위 문제로 적어 둔 것이 실은 어휘 문제였다.** 사전은 맥락 조건으로 사정거리를 46문항 중 18 → 5 로 좁혔다 (RAG-066 ⑤) |

> ⚠ **번호는 자리이지 주소가 아니다.** 닫힌 행은 번호를 그대로 들고 남으므로(취소선) 열린 행을
> 다시 세울 때 **빈 번호와 뒤섞인 순서가 생긴다** — 지금 `~~15~~`(`D11`, 닫힘)가 16·17 사이에 있는 것이
> 그렇다. **주소로 쓰는 것은 카드 이름**(`D2`·`F4`-ⓑ)이고, 그것은 안 바꾼다.

### 순서에 자리가 없는 것 — 조건이 오면 그때

| 카드 | 기다리는 조건 |
| --- | --- |
| `F2` 분실 · 유기 (툴) | **독립이라 언제 끼워도 된다.** RAG 가 아니라 `/life/walk-conditions` 형 툴이라 재적재와 무관. data.go.kr **활용신청**이 먼저 |
| `C5` 워커 예열 · `crawl_runs` 잔존 행 · 알림 | **서버 PC 앞에 있는 날.** ~~HNSW~~ 는 **`D16`**(§4 의 13b)으로 나갔다 — 적재 증분화와 짝이라 "틈에" 할 일이 아니다 |
| `D7` 잡동사니 · `D9` 랩 순서 | XS. 다른 카드를 열다 손이 닿을 때 같이 |
| `D5c-1` 확장의 관련성 신호 | **잡음이 실제로 물리면.** #218 이 우연한 필터를 없앴는데 lap20 에서는 해가 없었다 |
| `E1` 비 오는 날 프로브 | **비 오는 날** (RT-004 로 범위는 줄었다) |
| `E3` UV 값 확인 | apihub 403 — **외부 의존이라 막혀 있다** |
| `A3b` 라우터의 place 목적지 선택 | **라우터 담당 소유** — 이 파트가 순서를 못 정한다. 병렬로 조율 |

### 재적재와 GCP 동기화 — 위 표의 ✔ 에 붙는 값

재적재는 **집 서버 DB 만** 바꾼다. GCP 는 그 순간 안 바뀐다 — 적재도 성공하고 스모크도 통과하는데
앱에만 새 문서가 안 보이는 모양으로 만난다 (`docs/deploy/roadmap.md` §2-5).
반영하려면 **`documents` 테이블만** 갈아 끼운다 — 절차는 `runbook.md` §6 의 "Life 코퍼스만 동기화 (GCP)"
이고 `#290` 이 적었다. 🔴 **통째 덤프·복원으로 하지 말 것** — `vectordb` 한 DB 에 운영 테이블이 같이 있어
사용자 데이터를 덮어쓴다.

- **✔ 카드를 연달아 열면 GCP 동기화는 묶어서 한 번 낸다.** 카드마다 내면 손작업이 그 수만큼 늘어난다.
- `db/migrations/` 도 **두 DB 에 각각 손으로** 적용한다. 버전 테이블이 없어 어디까지 적용했는지는 사람이 안다.
- **구조적 해소가 §4 의 13번이다** (`docs/deploy/roadmap.md` §7-1 — 크롤러·코퍼스·적재 이전).
  2026-09-07 에 사람이 그 문서의 *"9/21 이후"* 칸에서 끌어올렸다. **위 손작업 전부가 그 카드까지의
  임시**이고, 그것이 이 표의 `✔` 표시가 실제로 뜻하는 값이다.

### 이미 선 것 · 닫힌 것

```
A0 ✅ #169 → A5 ✅ #175 → A4 ✅ #176 → A3a ✅ #177 → B4 ✅ #202
  → 앱 active_dog_id ✅ DAENGS_APP#112 → D5 ✅ #208
B1 ✅ → B2 ✅ → B3 ✅ → [Card 1 ✅ #102 · A6 ✅].   A8 🚫 · C4 🚫 (§5)
D1 ✅ #236 · D5c ✅ #218 (지표는 안 움직였다 — RAG-058 ④) · D5d · D8 ✅ #240
F0 ✅ #256 (정찰 — F1 은 열리고 F4 는 반으로 갈렸다) · 로드맵 재작성 ✅ #254
D10 ✅ #261 · A2 ✅ #262 · A1 ✅ #266 · F1 ✅ · F4-ⓐ ◐ #268 (lap24 · lap25)
D11 ✅ #270 (lap26 · lap27) — F4-ⓐ 의 ◐ 가 여기서 닫혔다. `D5e` 의 전제도 같이 사라졌다
A7 ✅ #271 (랩 없음 — category 는 서빙 검색에 안 쓰인다) · D3 ✅ #285 (lap28)
D12 ✅ #286 (lap29) — 채점기를 고쳤더니 **위 모든 랩의 `grounded` 가 소급으로 올랐다**
D13 ✅ #287 (lap30) — `D5b`(8번)를 열려다 **그 전제가 없다는 것을 찾았다**. 어휘 한 줄로 닫혔다
D6 ✅ #292 · D9 ✅ #292 · D7 ◐ #292 (help 문구만) — **`D5e` 는 사람이 닫았다**(랩 없음: 도구 카드다)
D14 🚫 #296 (랩 없음 — **파일을 안 고치고 닫았다**). 정찰이 `I4` 의 원인을 dense 로 옮겼다 (RAG-072)
```

- **D5 는 동작을 안 바꾸고 닫혔다** — 상수를 다 훑었더니 남은 것이 상수가 아니었고, 그 자리가 D5b~D5e 다.
- **Priority 는 사람이 정한다** (`collaboration.md` §1 · §2). 위 순서는 **의존과 값의 제안**이지 우선순위 등급이 아니다.

---

## 5. 하지 않기로 한 것 (이유는 번호로)

| 것 | 이유 |
| --- | --- |
| 막힌 소스 허가 문의 — 검역본부 2 · 항공 7 · 현대해상 | 회신을 기다릴 수 없어 하지 않기로 (RAG-046 · 2026-08-30 사람) |
| 남은 보험 4사 — 메리츠 · 카카오페이(SPA) · 현대해상(robots) · DB손보 | RAG-048 ① (DB손보는 사람이 뺌). knia 수치로만 답한다 |
| `care` · `emergency` · 건강 상식 확장 | RAG-008 ③ — 진단 성격은 다른 에이전트. 접종 스케줄은 `policy`+`guideline` 까지만 |
| 동반시설 · 숙소 · 병원 위치 데이터 | place-search 소유 (D-026). **다른 파트가 붙기 전에 그쪽 데이터를 대신 채우지 않는다** |
| 멀티턴 · checkpointer | #80 v1 은 stateless CLARIFY (D-034). 제품 증거가 생기면 별도 카드 |
| 자연어 키워드 하드 라우팅 | #80 routing §1 — 의미 판단은 LLM 라우터. `transport.exclusions()` 같은 **능력 안의** 필터와는 다르다 |
| ~~LLM judge (RAG-007 원안)~~ | ~~캘리브레이션 비용 대비 22문항. D6 축소판까지만~~ — **2026-09-07 에 다시 열렸다** (`D15` · §4 의 11번). 닫은 이유의 전제 둘이 다 바뀌었다: 문항이 33이고, `D6`(#292)이 서면서 judge 를 캘리브레이션할 자가 생겼다 |
| 임베딩 모델 프로세스 분리 · AWS 이전 | D-021 재개 조건 ⓐ~ⓓ 미발동 · RAG-043 ⑤ |
| `RAG-005` · `006` 의 확정 | 계획이지 결정이 아니었다 — 헤더는 제안으로 둔다 (2026-08-30 정정) |
| **data.go.kr 운영계정 전환 신청 (C4)** | 2026-09-02 사람 확정. 그래서 **개발계정 일 1,000회가 상수로 남는다** — RT-001 ④-e 가 "근본 해결"로 꼽았던 4번이 빠지고, 남은 1~3(AWS 로 초단기실황 대체 · 프리페치 상한 `N` · provider 별 일 예산 차단)이 **유일한 대응**이 된다. ④-f 의 "운영계정(10만/일)이 승인되면 `N` 을 수백 단위로 재역산" 갈래도 함께 닫힌다 |
| **직접 `/life/ask` 응답 축소 (옛 A4)** | 2026-09-03 사람 확정. 어댑터가 O-9 축소를 이미 한다(`adapters/life.py`). 직접 `/life/ask` 의 유일한 소비자인 콘솔 점검 탭은 `content` 전문이 있어야 검문소④를 본다 (RAG-028 ②). A4 는 이름 정리 카드로 바뀌었다 |
| **`/life/walk-conditions?verbose=1` (A8)** | 2026-09-03 사람 확정. 앱은 `/life/walk-conditions` 를 직접 안 부르고, 근거는 `results[].data.now` 와 aggregate 의 같은 등급 축 문장(859a691)으로 이미 나간다. 콘솔에 산책 점검 탭이 생기면 그때 다시 본다 |
| **Life → `training` 핸드오프** | training 은 EXECUTE 능력이라 라우터가 다중 선택으로 잡는다. 능력은 handoff 를 낼 수도 없다 (contracts §4 — `CapabilityResult` 에 필드 없음) |
| **능력 이름 `walk` 변경** | 벤치마크 골드 80문항 · 앱의 `results[].capability` 파싱이 그 값에 묶여 있다. A4 는 HTTP 경로만 바꾼다. **묶인 것이 풀려야 여는 것이고 날짜와 무관하다** — 라우터 담당과 조율한다 (2026-09-07 에 *"09-21 뒤"* 라는 표현을 뺐다: 발표가 순서의 축이 아니다) |
| **프로필 "기본 위치" 컬럼** | 앱이 `location` 을 매 요청 보낸다 (contracts §8). 컬럼을 두면 원천이 둘이 된다 |

### 순서에서 **뒤에 두는** 것 (안 하는 것과 다르다 — 2026-09-05 #254 · 정정 #257)

`🚫` 는 "안 한다"이고 아래는 **"아직 차례가 아니다"** 다. 값은 그대로 남아 있고 **자리만 뒤**이며,
그 자리는 §4 의 표에 번호로 적혀 있다.

| 것 | 뒤에 두는 이유 |
| --- | --- |
| ~~순위 튜닝~~ | **이 줄은 2026-09-07 에 비었다.** `D5e`(#292)·`D14`(#296)·`D5b`(#303)가 다 닫혔고, 남은 `D2` 는 **§4 의 12번으로 올라갔다** — 미룬 유일한 이유가 *"발표 시연 지연"* 이었는데 발표가 순서의 축에서 빠졌다. 원래 이 줄의 논거였던 *"효과가 잡음보다 작다"* 는 **`D10`(#261)·`D6`(#292)이 서면서 해소됐다** — 이제 문항 단위로 잰다 |
| **`F4`-ⓑ 관리규약 준칙 17개 시·도** | 법령 DB 에 없어 시·도별 PDF·HWP 를 각각 받아야 한다(#256). **ⓐ(5번)가 이미 답하는 질문이 얼마나 되는지 보고** 판단한다 |
| **`D4` 항·호 단위 인용** | **못 재서가 아니라 사람 결정이 먼저다.** `D10`(#261)이 갈라 놓고 나니 10문항이 아니라 **13문항이고 전부 `grounded`** 인데 `cited` 는 1이다(RAG-070 ③) — 그 소스들에는 **인용할 조 번호가 애초에 없다.** 그래서 '구제'가 아니라 **무엇을 인용으로 셀 것인가**이고, §6 에 열린 항목으로 있다. ⚠ `D15`(judge)가 서면 답이 달라질 수 있다 |

---

## 6. 열린 결정 (사람 몫)

**2026-09-05 에 닫힌 셋 (#254 — 사람이 "목표가 흐릿하다"고 한 자리에서 나왔다)**

- ~~**북극성 축**~~ — **닫힘: "사람이 실제로 물을 법한 질문에 답한다"(커버리지 우선).** 대안이던 "인용
  정확도 심화"를 고르지 않은 이유는 §1 의 실측이다 — 조 번호가 있는 문항에서는 이미 `cited` 13/18 이고,
  **실사용이 8턴**이라 정밀도를 더 깎아도 볼 사람이 아직 없다. 이 문장이 §3 의 "축" 열을 정의한다.
- ~~**재적재 비용**~~ — **닫힘: 감당한다.** 그래서 `A2` 가 `A1` 앞으로 왔다 (§4) — A2 는 메타만 바꿔
  55분을 애초에 안 내는데 A1 뒤에 적혀 있었다. 대신 **✔ 카드를 연달아 열면 GCP 동기화는 묶어서 한 번** 낸다.
- ~~**코퍼스 확장 범위**~~ — **닫힘: `F1` 음식·사료 + `F4` 주거·이웃 둘.** **`F0` 정찰이 하드 게이트**였고
  **✅ #256 (2026-09-05) 으로 끝났다** — F1 은 그대로 열리고(§4 의 4번), F4 는 접히는 대신 **반으로 갈렸다**
  (법령 ⓐ ✅ = 5번 / 준칙 ⓑ ❓ = **16번**). 게이트를 세운 값은 실제로 나왔다: 준칙을 `ordin` 에서 찾으면
  **"준칙 심의위원회 조례"가 이름만 맞게 잡힌다.**
- ~~**지표 총계**~~ — **닫힘: 고친다 → ✅ #261 로 끝났다** (RAG-062). 경계 문항을 `cited`/`grounded` 분모에서
  빼고 조 번호 있음/없음을 갈라 센다. 예고한 대가(**옛 기록이 인용하는 헤드라인이 바뀐다**)는 실제로 났고,
  **옛 기록은 고치지 않고** `score-laps` 총계 표의 `옛 표기` 열로 대조하게 했다 — 당시 사실이기 때문이다.

**2026-09-07 에 닫힌 셋 (사람 — #298)**

- ~~**발표(09-21)를 순서의 축으로 쓸 것인가**~~ — **닫힘: 쓰지 않는다.** §0 의 목표 문장에서
  *"09-21 까지"* 와 *"발표 자리에서"* 를 뺐고, 거기서 나오던 **커버리지 우선**도 같이 내렸다.
  **두 축(🟢·🔵)은 이제 나란하고**, §4 의 순서는 **의존과 값**으로 정한다 — 재는 도구가 먼저,
  전제가 선 것이 그 다음이다. ⚠ 근거 ⓐ·ⓑ 는 그대로 사실이고 **ⓒ 만 힘을 잃었다**(§0).
  **가장 크게 움직인 것은 `D2`** — 미룬 유일한 이유가 *"발표 시연을 느리게 한다"*(RAG-070 ⑤)였다.
- ~~**LLM judge 를 다시 열 것인가**~~ — **닫힘: 연다** (`D15` · §4 의 11번). §5 가 닫아 둔 이유는
  *"캘리브레이션 비용 대비 22문항"* 이었는데 **전제 둘이 다 바뀌었다** — 문항이 33이고,
  `D10`(#261)·`D12`(#286)·`D6`(#292)이 서면서 judge 를 캘리브레이션할 자가 생겼다.
  ⚠ **#277 과 다른 것이다** — 저쪽은 라우터·어시스턴트의 답변률·답변 품질이고 라우터 소유다.
  여는 이유의 실물은 `RAG-072` 의 `I4` 이고, **지금 지표는 답이 맞는지를 아무도 안 잰다.**
- ~~**GCP 크롤~적재 이전을 언제 할 것인가**~~ — **닫힘: 지금 순서에 넣는다** (§4 의 13번).
  `docs/deploy/roadmap.md` §7-1 에 이미 적혀 있던 것을 그 문서의 *"9/21 이후"* 칸에서 끌어올렸다.
  **내용은 저쪽이 원본이고 여기서 다시 적지 않는다.** 이것이 없애는 것이 이 문서의 `✔ 재적재`
  표시가 실제로 뜻하는 손작업 전부다.

- ~~**`D5b`(§4 의 8번)를 닫을 것인가**~~ — **닫힘: 닫는다** (`#303` · RAG-073).
  남아 있던 것은 *"66행이 공통 머리를 공유한다"* 는 관찰 하나였고 **그것은 사실이었다**
  (쌍 코사인 0.7999 대 형제 조문 0.4896). 그런데 **그 관찰이 가리키는 두 처방이 각각
  해롭고 무승부**다 — 머리를 걷어 내면 12문항 중 나빠진 것이 11이고(top-5 7 → 2),
  머리를 행마다 다르게 하면 유사도만 내려가고(0.80 → 0.75) 순위가 안 움직인다.
  **파일은 하나도 안 고쳤다** — `D5e`·`D14` 와 같은 모양이다.

**아직 열려 있는 것**

- **judge 를 지표로 승격할 것인가 — 라벨을 30개까지** (`D15` · RAG-075).
  **1차 4개는 했다** — 엇갈린 문항 넷이고 **일치 3/4** 다(`rag judge lap30 --show --against
  lap30__human`). ⚠ **남은 26개는 「두 자가 일치하는 문항」에서 골라야 한다** — 엇갈린 자리가
  넷뿐이라 더 없다. 그건 사람 시간이고, 그래서 이 줄이 열려 있다.
  ⚠ **1차가 이미 하나를 말했다** — judge 의 오답이 **유일한 「못함」 판정**이라, 이 랩에서
  이 축이 잡은 진짜는 **0건**이다. 30개를 채웠는데도 그렇다면 물음은 *"judge 를 고칠까"* 가
  아니라 ***"이 골든셋에 이 축이 잴 것이 있나"*** 가 된다.

- **`referenced_indices` 를 `[조례 제26조]` 같은 표기까지 넓힐 것인가** (RAG-075 ⑤).
  `S1` 의 답변이 `must`(검색 1위)를 **이름으로 정확히 지목했는데** 대괄호 안이 숫자가 아니라
  `grounded ✗` 로 세진다 — `D12`(#286)가 `[1, 2]` 를 고친 것과 같은 종류다.
  🔴 **넓히면 소급 변경 세 번째**이고, `RAG-070` ③ 이 *"옛 기록의 대조선이 또 끊긴다"* 로
  막아 둔 자리다. 값과 값을 저울질할 사람 몫이다.

- **`입양`·`명의 변경` 을 어휘에 넣을 것인가** — `RAG-073` ⑤ⓐ 가 찾았다. 사람이
  *"강아지를 입양받고 명의 변경을 안 하면"* 이라고 묻는데 조문은 *"소유권을 이전받은 날부터
  30일"* 이라고 쓴다. `vocabulary.expand` 가 **한 글자도 안 더해** 정답이 **1,475위**이고,
  dense 1~4위가 **보험 「계좌번호가 변경된 경우」** 다. `D13`(`목줄 → 안전조치`)과 같은 종류인데,
  **사정거리를 안 쟀다** — `RAG-066` 의 *"값을 보인 낱말만 넣는다"* 규칙대로 재고 정한다.

- ~~**독성 식품 목록을 `F1` 에 넣을 것인가**~~ — **닫힘: 넣되 증상 문장은 자른다** (2026-09-06 사람,
  ✅ #268 · RAG-065 ⑤). 「급여해서는 안되는 음식」의 설명에서 증상 어휘가 든 **문장을 통째로** 뺀다 —
  절 단위로 자르면 연결어미로 끝나는 조각이 남고, 청크는 생성기가 그대로 인용하는 재료다.
  `초콜릿 → "좋지 않다"` 는 남고 `구토·설사·부정맥` 은 사라진다. **12항목 중 4개는 본문이 통째로
  비는데 그것이 정상이다** — 절 제목과 항목 이름이 남아 "포도 먹여도 되나요"에는 답한다.
  빈 자리를 채우려고 문장을 지어내지 않는다.
  예고한 대가 ①(조 번호가 없어 `cited` 가 0)은 실제로 났고 D10 의 "조 번호 없음" 버킷에서 읽는다.
  대가 ②(`medical` 과 충돌)도 **lap24 에서 실제로 났다** — `FD1`("초콜릿 줘도 되나요")이 `medical` 로
  거절됐다. 프롬프트에 *"먹여도 되나요는 `none`, 먹었어요는 `emergency`"* 한 줄을 더해 풀었고,
  lap25 에서 `FD1` 은 답하고 `B4`·`B5`·`B6` 은 그대로 거절한다.

- ~~**`insurance` 독립 category 의 시점**~~ — **닫힘: 뺐다** (✅ #271 · RAG-067, 2026-09-06).
  2026-08-30 "나중에 뺀다"의 그 나중이다. 재인코딩 0이고 지문이 안 움직였다.
  **다만 값의 성격이 예상과 달랐다** — 로드맵은 "category 필터의 격리 효과"를 근거로 잡았는데,
  확인해 보니 **서빙이 `category` 를 아예 안 넘긴다**(`app/services/ask.py` → `generate.ask` 에 인자가
  없다). 그 층을 잇는 것은 라우터 소유다(#80). 그래서 지금 얻은 것은 **지표 축**이고, 갈리자마자
  보험 5문항이 `insurance 3/2·5` 로 드러났다 — `policy` 23개 안에 묻혀 있던 **가장 약한 칸**이다.
  검색 필터는 소비자가 생겼을 때 쓴다.
- ~~**지역 필터의 형태**~~ — **닫힘: ⓐ `org` 사전필터** (RAG-063 · #262, 2026-09-05). ⓑ(하이브리드)는 이미 들어와
  있었는데 S3 를 13랩 동안 못 고쳤고, D5 가 순위 상수로도 안 됨을 증명했다. ⓒ(되묻기)는 지금도 하고 있어 새로 안 한다.
- **핸드오프 식별자** — 2026-09-03 정리: `place` 만 라우터 HANDOFF 대상 후보(A3b — 확정은 라우터 담당).
  `medical` · `emergency` 는 핸드오프가 아니라 Life 의 `refusal.code` (A3a). `training` 은 제거 (§5).
  남은 사람 몫은 **A3b 의 시점** — 라우터 벤치마크를 다시 돌리는 값이라 **라우터 담당이 본다.** 이 파트는 순서를 못 정한다 (§4).
- ~~**A4 착수 시점**~~ — 2026-09-03 닫힘. DAENGS_APP dev(`0290d23`, origin 과 동일)의 API 경로 전부를 훑어 `/life/ask`·`/life/walk-conditions` 직접 호출이
  없음을 확인했다. 앱이 읽는 것은 `/assistant/query` 응답의 `results[].capability == "walk"`(`assistant/WalkVerdict.kt`)뿐이라,
  **HTTP 경로는 바꿔도 능력 이름은 못 바꾼다**는 §5 의 판단이 코드로도 확인됐다.
- ~~**페이로드를 누가 소유하는가**~~ — **2026-09-03 닫힘: 계약 계층 잔류 수용.** 이유: `LifePayload` 를
  `daengs_life.app.dto` 로 옮기면 `contracts.py` 가 `daengs_life` 를 import 하게 되어 D-035 "어댑터 한 곳만" 규칙을 깬다.
  #80 불변식 6 은 "페이로드의 *의미*는 능력이, *타입*은 계약 계층이 소유한다"로 문구 개정을 제안한다 —
  `docs/orchestration/contracts.md` 는 오케스트레이션 소유라 그쪽 카드에서. 앞으로 필드 확장(B4 · A2)은 `contracts.py`
  수정이라 오케스트레이션 리뷰를 받는다. (#151 이 올리고 #164 가 닫음)

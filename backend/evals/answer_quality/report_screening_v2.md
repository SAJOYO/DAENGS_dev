# 경과일은 갈래를 가르는가 — 표본 28로 다시 (#318)

#314 는 14문항 · 판정 조합 하나(`abnormal:3`)로 돌았고, #319 가 그 결론 하나를 무효화했습니다.
이 카드는 표본을 28로 늘리고 조합을 넷으로 늘려 다시 쟀습니다.

2026-09-07, dev `6fb0752` 기준. 질문 `questions_screening_v2.jsonl`(28문항),
`--life-temperature 0`, `--adapters real`.

> ⚠️ **2026-09-08 정정 (#328).** ② 의 거절을 **읽어 보니 거절이 아니었습니다.** 다섯 조건의 Life
> `REFUSED` 15건 중 **14건이 본문에 `[N]` 표기를 단 제도 답변**입니다 — 조항을 정확히 짚고 진료
> 권함을 덧붙인 뒤 생성이 `boundary` 를 `medical`/`emergency` 로 냈고, `services/ask.py` 가 그
> 답변을 422 로 감싸면서 `hits` 를 안 실어 어댑터의 `data` 가 `None` 이 됐습니다. **사용자는
> `[1][3]` 을 보는데 그 번호가 가리킬 출처 목록이 없습니다.** 그래서 이 리포트의 두 문장이
> 틀렸습니다 — ① 의 *"다름은 전부 0 대 5(한쪽이 답을 안 함)"* 는 **답은 했고 `REFUSED` 라 `data`
> 가 없어진 것**이고, ② 의 *"최근 이상 소견이면 더 조심스러워진다"* 는 **같은 답을 하면서 라벨만
> 바뀌고 인용을 잃은 것**입니다. 고친 자리는 RAG-077 — Life 가 422 에도 근거를 싣고 어댑터가
> `REFUSED` 에도 `data.citations` 를 채웁니다. 이 수집은 그 이전 것이라 ① 의 분모(15~17)에 그
> 문항들이 빠져 있습니다. 아래 본문은 그대로 두고 틀린 자리마다 표시했습니다.
> **RAG-077 뒤에 같은 28문항을 다시 수집한 수치는 ④ 입니다** (#330).
>
> | 조건 | `REFUSED` | 그중 본문에 `[N]` 있음 |
> | --- | --- | --- |
> | `v2_off` | 4 | **4** |
> | `v2_off_ctl` | 4 | **4** |
> | `v2_abn3` | 5 | **4** (`noisy_02` 만 표기 없음) |
> | `v2_abn200` | 1 | **1** |
> | `v2_normal3` | 1 | **1** |

## 결론 셋

1. **인용 조항은 절대 안 갈립니다.** 양쪽이 답한 경우 조항 집합이 다른 사례가 **28문항 ×
   5조건에서 0건**입니다. 코퍼스 갈래 가설은 **반증됐습니다.**
2. **대신 거절 경계가 갈립니다.** ~~최근 이상 소견이면 Life 가 더 자주 거절하고, 오래됐거나
   정상이면 더 답합니다.~~ **갈리는 것은 생성의 `boundary` 라벨이지 답변이 아닙니다** — 라벨이
   바뀐 문항도 같은 조항으로 답했고 인용만 잃었습니다 (2026-09-08 정정, #328 · 위 표).
   대조쌍이 문항 ID까지 완전히 같아 **노이즈 플로어가 0** 입니다.
3. **#314 의 "자격 단정 2 → 0" 은 재현되지 않습니다.** n=28 에서 `off` 와 `abnormal:3` 이
   똑같이 4건입니다.

## ① 인용 조항 — 다섯 비교 전부 100% 동일

**양쪽 다 인용이 있는 문항만** 셉니다 (`cited_diff.compare`). 한쪽이 답을 안 한 자리를
"같다" 로 세면 답을 안 할수록 지표가 좋아지기 때문입니다.

| 비교 | 양쪽 인용 있음 | 그중 동일 |
| --- | --- | --- |
| `v2_off` 대 `v2_off_ctl` (대조쌍) | 17 | **17** |
| `v2_off` 대 `v2_abn3` | 15 | **15** |
| **`v2_abn3` 대 `v2_abn200`** | 16 | **16** |
| `v2_abn3` 대 `v2_normal3` | 16 | **16** |
| `v2_off` 대 `v2_normal3` | 17 | **17** |

집합 비교에서 나온 "다름" 은 **전부 `0 대 5`**(한쪽 0건, 교집합 0) 였습니다 — 조항이 달라진
것이 아니라 ~~한쪽이 답을 안 한 것입니다~~ **한쪽이 답을 하고도 `REFUSED` 라 `data` 를 잃은
것입니다** (2026-09-08 정정, #328). `noisy_01` · `no_location_01` 의 `abnormal:3` 답변은 `off` 와
같은 조항을 짚고 `[1][3]` 을 달았는데, 422 경로에 `hits` 가 없어 `citations` 가 0 으로 셌습니다.
RAG-077 뒤에 같은 질문을 다시 수집하면 이 두 문항이 비교에 **들어오고**, 위 표의 분모가
늡니다.

**왜 이렇게 되는지는 #283 설계에서 예측됩니다.** 판정은 검색 질의에 안 들어갑니다(B4 의
프로필과 같은 이유 — 질의를 바꾸면 lap 비교 축이 흔들립니다). 그래서 top-k 가 같고, 생성도
같은 top-k 안에서 인용합니다. `SCREENING_BLOCK` 은 **무엇을 인용할지가 아니라 어떻게 말할지**
를 바꿉니다.

## ② 거절 경계 — 경과일이 여기서 작동합니다

`multi_intent` 제외, 문항 24건 기준 Life 능력의 결과 상태:

| 조건 | OK | ABSTAINED | REFUSED |
| --- | --- | --- | --- |
| `v2_off` (판정 없음) | 17 | 3 | 4 |
| `v2_off_ctl` (동일 조건 재실행) | 17 | 3 | **4 — 문항 ID까지 같음** |
| `v2_abn3` (3일 전 이상 소견) | 16 | 3 | **5** |
| `v2_abn200` (200일 전 이상 소견) | 18 | 5 | **1** |
| `v2_normal3` (3일 전 특이 소견 없음) | 20 | 3 | **1** |

**대조쌍이 거절 문항 4개의 id 까지 정확히 같습니다** — `polite_02` · `casual_03` ·
`noisy_02` · `noisy_03`. 노이즈 플로어가 0 이므로 조건별 차이는 전부 판정 때문입니다.

~~방향이 말이 됩니다 — 최근 이상 소견이 있으면 더 조심스러워지고(거절 5), 오래됐거나
정상이면 더 답합니다(거절 1). 경과일이 실제로 작동하는데, 이 카드가 찾으려던 자리
(인용 조항)가 아니라 답하느냐 마느냐에서 나타납니다.~~

**2026-09-08 정정 (#328) — "답하느냐 마느냐" 가 아니었습니다.** 15건의 거절 중 14건이
답을 했습니다. `abnormal:3` 에서만 거절된 `noisy_01` · `no_location_01` 은 `off` 와 **거의 같은
본문**에 진료 권함 한 문장을 더한 것이고, 라벨이 `medical_boundary` 로 바뀌면서 인용만
잃었습니다. 경과일이 움직이는 것은 **생성의 `boundary` 라벨**이지 답변의 내용이 아닙니다.
`noisy_03` 은 더 분명합니다 — 네 조건 전부 같은 조항을 짚는데 `off` 는 `medical`, 판정 블록이
붙은 셋은(**정상 판정이어도**) `emergency` 입니다. 라벨이 질문이 아니라 **문맥에 달려 있습니다.**
그 분석은 RAG-077 ④, 고치는 카드는 로드맵 `D17` 입니다.

⚠️ **이것이 좋은 동작인지는 이 리포트가 답하지 않습니다.** "최근 이상 소견이니 진료로
넘긴다" 는 옳은 조심일 수도 있고, 제도 질문에 답할 수 있는데 안 답한 과잉일 수도 있습니다.
거절 문구가 무엇이었는지를 사람이 읽어야 합니다 — ~~다음 카드감입니다~~ **#328 이 읽었습니다.
셋 다 아니었습니다 — 답은 했고 인용만 잃었습니다** (위 정정).

## ③ #314 의 자격 단정 결론이 재현되지 않습니다

앵커 게이트를 통과한 변형 A 로 채점했습니다 (#319).

| 조건 | `usable` | 자격 단정 |
| --- | --- | --- |
| `v2_off` | 24/28 (0.857) | **4** |
| `v2_abn3` | 24/28 (0.857) | **4** |
| `v2_abn200` | 22/28 (0.786) | 6 |
| `v2_normal3` | 22/28 (0.786) | 6 |

**`off` 와 `abnormal:3` 이 똑같습니다.** #314 는 n=14 에서 `2 → 0` 이었는데, n=28 에서는
차이가 없습니다. 2건이 움직인 것을 효과로 읽었던 것이고, 표본을 늘리니 사라졌습니다.

오래된 판정과 정상 판정에서 자격 단정이 **조금 늘어난 것**(4 → 6)은 ② 와 같은 방향입니다 —
덜 조심스러워지면 단정도 는다는 것이라 일관됩니다. 다만 2건 차이라 이것도 크기는 못 말합니다.

**표본을 늘린 것이 이 카드의 제일 큰 성과입니다.** #314 의 두 결론 중 하나는 #319 가
(판정기 일치율로), 다른 하나는 이 카드가 (표본으로) 무효화했습니다.

## ④ RAG-077 뒤 같은 28문항 재수집 — v3 (#330, 2026-09-08)

dev `8fcf9bc`(#328 머지 커밋) 기준. 같은 질문 파일(`questions_sha256` `39ce3c95…`), `--life-temperature 0`,
`--adapters real`, 같은 다섯 조건. 라벨은 `v3_*` 이고 `answers_v2_*.jsonl` 은 그대로 둡니다. 수집 5회 **10분 40초**.

**축이 서 있었는지 먼저.** v2 와 v3 사이에 바뀐 것이 RAG-077 하나여야 이 절이 성립합니다.

| 축 | 확인 |
| --- | --- |
| 코퍼스 | `documents` 9,838행, 마지막 갱신 **9월 6일 23:05 KST** — v2 수집(9월 7일 20:46 KST) 이후 적재 없음 |
| 생성·검색·채점 코드 | `generate.py` · `config.py` · `search.py` · `score.py` 의 `6fb0752..8fcf9bc` diff **없음** |
| 실행 조건 | 다섯 meta 행 전부 `dev_source_sha` `8fcf9bc` · `source_dirty_files` `[]` · 온도 0 · `gemini-3.1-flash-lite` |
| ⚠ 라우터 프롬프트 | **`semantic-router-ko-v9` → `v10`** (`bf2e1a5`, D-061 · #323 — 도메인 밖 요청이 place 로 새던 것). Life 생성과 무관하고, 아래 표대로 Life 상태 분포 · 거절 문항 ID · 거절 코드가 v2 와 **문항 단위로 전부 같아** 이 축에는 영향이 없었습니다 |

### 거절이 인용을 되찾았습니다

`multi_intent` 제외 24문항. Life 상태 분포는 v2 와 **같고**, 거절의 `data.citations` 만 0 에서 전부로 바뀝니다.

| 조건 | OK | ABSTAINED | REFUSED | 거절 중 `data.citations` 있음 (v2 → v3) | 거절 문항 ID · 코드 |
| --- | --- | --- | --- | --- | --- |
| `off` / `off_ctl` | 17 | 3 | 4 | **0 → 4** | v2 와 같음 |
| `abn3` | 16 | 3 | 5 | **0 → 5** | v2 와 같음 |
| `abn200` | 18 | 5 | 1 | **0 → 1** | v2 와 같음 |
| `normal3` | 20 | 3 | 1 | **0 → 1** | v2 와 같음 |

거절마다 5~6건(`noisy_03` 은 6 — 인용 확장 RAG-040)이고, 본문의 `[N]` 이 그 안에서 풀립니다.

### 인용 비교 — 분모가 늘고, 갈린 조항은 여전히 0

`cited_diff.compare` 의 `comparable`(한쪽이라도 인용 있음) · `identical` · `different` 를 그대로 적습니다.
① 의 v2 표는 "양쪽 인용 있음" 칸에 실제로는 `identical` 을 적었던 것이라(그래서 `0 대 5` 가 표 밖에
있었습니다), 여기서는 세 수를 다 씁니다.

| 비교 | v2 `comparable` / `identical` | **v3 `comparable` / `identical`** | v3 `different` 의 정체 |
| --- | --- | --- | --- |
| `off` 대 `off_ctl` | 17 / 17 | **21 / 21** | — |
| `off` 대 `abn3` | 18 / 15 | **21 / 21** | — (v2 의 `0 대 5` 셋이 전부 `identical` 로) |
| `abn3` 대 `abn200` | 18 / 16 | **21 / 19** | `casual_03` · `noisy_02` — `abn3` 거절(5) ↔ `abn200` **기권**(0) |
| `abn3` 대 `normal3` | 20 / 16 | **22 / 20** | `casual_03` — 거절 ↔ 기권(0) · `abbrev_typo_04` — 기권(0) ↔ 답(5) |
| `off` 대 `normal3` | 20 / 17 | **22 / 20** | 위와 같은 둘 |

읽는 법 세 가지입니다.

1. **`0 대 5` 가 사라졌습니다.** v2 에서 "한쪽이 답을 안 한 것" 으로 읽었던 `noisy_01` · `no_location_01`
   (`off` 대 `abn3`)이 v3 에서는 **같은 조항 집합**입니다. 답은 같았고 라벨만 달랐다는 #328 의 판독이
   수치로 확인됐습니다.
2. **조항 집합이 갈리는 사례는 여전히 0건입니다.** 남은 `different` 는 전부 한쪽이 **기권**(`ABSTAINED`,
   인용 없음)이라 교집합 0 인 자리입니다 — 거절 ↔ 기권, 기권 ↔ 답. 그것은 기권 축의 이야기지 인용 축이
   아닙니다. 결론 1(*"인용 조항은 안 갈린다"*)은 분모가 는 뒤에도 그대로입니다.
3. **거절 라벨은 그대로입니다** — D17 전이라 그래야 정상이고, 라벨이 움직였다면 축이 깨진 것이었습니다.

### 남는 것

- `casual_03` · `noisy_02` 가 `abn3` 에서는 거절, `abn200`/`normal3` 에서는 **기권**입니다. 같은 top-k 를 두고
  판정 블록이 `covered` 판단까지 흔든다는 뜻인데, 이 절의 지표(인용)로는 안 보입니다. D17 이 라벨을 볼 때
  같이 볼 자리입니다
- 재현: `#330` 본문 C 절의 PowerShell 블록 그대로. 비교는 `cited_diff.compare` 를 다섯 쌍에 돌리면 됩니다

## 실측 — 시간

| 단계 | 시간 |
| --- | --- |
| 질문 생성 28문항 | 20초 |
| 수집 ×5 (28문항, 온도 0) | **624초** (회당 약 120초, 문항당 4.3초) |
| `eligibility_claim` 채점 ×4 | 약 150초 |
| 인용 비교 | 수초 |
| **합계** | **약 14분** |

## ⚠️ 재현 — 함정 셋

`report_screening_v1.md` 의 재현 절차에 **두 개가 더 붙습니다.**

1. **`POSTGRES_IP` 를 셸에 올릴 것.** 안 하면 전건 `OperationalError`(59초 타임아웃).
   `rag` 는 `POSTGRES_*` 를 읽는데 서버 주소는 `backend/.env` 의 `DAENGS_DB_HOST` 에만
   있습니다. **둘을 통일하지 마세요** — 루트 CLAUDE.md 가 막아 둔 자리입니다.
2. **`uv sync --group ml` 을 할 것.** 안 하면 전건 `life_http_503`
   (`ModuleNotFoundError: sentence_transformers`). 이 카드에서 실제로 28건 중 23건을
   그렇게 날렸습니다(49초). 루트 CLAUDE.md 가 적어 둔 함정 그대로입니다.
3. **`--life-temperature 0` 은 Walk 를 결정적으로 만들지 못합니다.** 대조쌍의 답변 문장이
   26/28 이었고, 다른 2건은 `walk:ABSTAINED` 대 `walk:OK` 였습니다 — Walk 는 실시간 날씨를
   씁니다. 인용에는 영향이 없었습니다(17/17). **Life 만 재는 지표를 쓰세요.**

## 남은 것

- ~~**거절이 옳은 조심인지 과잉인지** — ② 의 5 대 1 차이가 이 카드의 실질 발견인데, 그 거절이
  무엇을 말했는지는 안 읽었습니다. 거절 문구를 사람이 보는 카드가 다음입니다~~ → #328 이
  읽었고 RAG-077 이 인용을 살렸습니다. ~~RAG-077 뒤에 같은 28문항을 다시 수집해 ① 의 분모를 다시
  재는 일~~ → #330 이 쟀습니다(④). 남은 것은 **경계 라벨 자체**(로드맵 `D17`)입니다
- `branch_selected` 는 여전히 지표에서 빠져 있습니다 (#319). 이 카드는 그것을 되살리지 않고
  `cited` 로 우회했고, **그 우회가 답을 냈습니다** — 되살릴 이유가 줄었습니다
- 표본 28 도 작습니다. 거절 차이가 4~5건이라 n 을 더 늘리면 방향이 더 또렷해집니다
- `multi_intent` 4건은 여전히 place 실패로 인용 비교에서 빠집니다. 라우팅 관찰용으로는
  답변 파일에 남아 있습니다

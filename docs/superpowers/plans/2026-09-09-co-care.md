# 공동 돌봄 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 한 강아지를 여러 보호자(대표 1명 + 돌보미 N명)가 함께 돌보고, 돌봄 기록이 서로에게 정확히 보이게 한다.

**Architecture:** `pets.app_user_id` 를 대표로 남기고 돌보미만 담는 `pet_members` 를 더한다. 구성원은 둘의 합집합이다. 접근 판정은 `repositories/pet.py` 의 `get_accessible` 한 곳으로 모으고, 탈퇴 정리와 대표/돌보미 상호배제는 **트리거**가 보장한다 — `app_users` 행이 탈퇴해도 안 지워져 FK 가 안 돌기 때문이다.

**Tech Stack:** FastAPI · SQLAlchemy 2.0 async(asyncpg) · PostgreSQL(plpgsql 트리거) · pytest · uv

**Spec:** `docs/co-care.md` — 이 계획은 그 문서에서 논증한다. 실행자는 **둘 다** 읽는다.

## Global Constraints

- **`app_users` 행은 탈퇴해도 안 지워진다.** `services/app_auth.py:322` 의 `withdraw()` 가 `status='withdrawn'` 으로 바꾸고 암호문만 NULL 로 만든다. **`app_users` 를 가리키는 FK 의 `ON DELETE CASCADE`·`SET NULL` 은 영영 안 돈다.** 정리는 트리거나 명시 삭제로 한다.
- **`delete_all_for_owner`(파기)는 대표 기준을 유지한다.** 구성원으로 바꾸면 돌보미 탈퇴가 남의 강아지를 지운다.
- Python 은 **3.12 고정**. 모든 명령은 `backend/` 에서 `uv run` 을 거친다.
- 의존성 추가는 `uv add` 로만. 이 계획은 **새 의존성이 없다.**
- 기본 DB 스키마 원본은 `db/init/*.sql` 이고 **Alembic 을 쓰지 않는다.** `models/` 가 SQL 을 따라간다.
- `db/migrations/` 의 SQL 은 **버전 테이블이 없어** 여러 번 돌려도 안전해야 한다.
- 브랜치는 `docs/co-care-design`. 한 PR 로 간다.
- `MAX_MEMBERS_PER_PET = 5`, 초대 유효 개수 상한 **3**, 초대 만료 **24시간**, `MEDICATION_CONFIRM_WINDOW = timedelta(hours=6)`, `CONFIRM_KINDS = frozenset({"medication"})`.

---

## 파일 구조

| 파일 | 책임 |
| --- | --- |
| `db/init/24_pet_members.sql` (신규) | 두 표 · 두 트리거의 원본 |
| `db/migrations/2026-09-09_pet_members.sql` (신규) | 이미 도는 DB 용. 재실행 안전 |
| `backend/src/daengs_backend/models/pet_member.py` (신규) | `PetMember` |
| `backend/src/daengs_backend/models/pet_invite.py` (신규) | `PetInvite` |
| `backend/src/daengs_backend/models/care_event.py` (수정) | `app_user_id` → `actor_app_user_id`, nullable |
| `backend/src/daengs_backend/repositories/pet_member.py` (신규) | 구성원·초대 쿼리만 |
| `backend/src/daengs_backend/repositories/pet.py` (수정) | `get_accessible` · `count_accessible` 추가 |
| `backend/src/daengs_backend/services/pet_member.py` (신규) | 초대·수락·퇴장·승계 규칙과 트랜잭션 |
| `backend/src/daengs_backend/routers/pet_member.py` (신규) | HTTP 경계만 |
| `backend/src/daengs_backend/schemas/pet_member.py` (신규) | 요청·응답 모양 |
| `backend/tests/test_pet_members.py` (신규) | 층 1 — 규칙 |
| `backend/tests/test_pet_membership_postgres.py` (신규) | 층 2 — 트리거·인덱스·마이그레이션 재실행 |
| `.github/workflows/backend-tests.yml` (수정) | postgres 서비스 |

---

## Task 1: 스키마 · 모델 · 마이그레이션

**Files:**
- Create: `db/init/24_pet_members.sql`, `db/migrations/2026-09-09_pet_members.sql`
- Create: `backend/src/daengs_backend/models/pet_member.py`, `backend/src/daengs_backend/models/pet_invite.py`
- Modify: `backend/src/daengs_backend/models/care_event.py`, `backend/src/daengs_backend/models/__init__.py`
- Modify: `backend/src/daengs_backend/services/care_event.py` (개명 따라가기), `backend/tests/test_care_events.py` (`FakeCareEvent` 필드명)
- Test: `backend/tests/test_pet_membership_postgres.py`

**Interfaces:**
- Produces: `PetMember(pet_id, app_user_id, joined_at)`, `PetInvite(id, pet_id, invited_by, token_hash, expires_at, created_at)`, `CareEvent.actor_app_user_id: uuid.UUID | None`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`backend/tests/test_pet_membership_postgres.py` 를 만든다. `_postgres_or_skip` 은 `test_release_fk_postgres.py` 의 것과 같은 규칙(loopback 만, 없으면 skip)이다.

```python
"""트리거·인덱스가 진짜 PostgreSQL 에서 도는지의 증명 (docs/co-care.md §5).

가짜 리포지토리로는 증명할 수 없다 — 그것이 트리거를 고른 이유다. 모든 쓰기는 한
트랜잭션에서 검사한 뒤 rollback 한다. loopback DB 만 만진다.
"""

from __future__ import annotations

import os
import uuid
from urllib.parse import urlparse

import psycopg
import pytest


def _postgres_or_skip():
    dsn = os.environ.get(
        "DAENGS_TEST_DATABASE_URL",
        "postgresql://postgres:postgres@127.0.0.1:5432/vectordb",
    )
    parsed = urlparse(dsn)
    if parsed.hostname not in {"127.0.0.1", "localhost", "::1"}:
        pytest.fail("공동 돌봄 증명은 loopback 이 아닌 DB 를 거부한다")
    try:
        conn = psycopg.connect(dsn, connect_timeout=3)
    except psycopg.Error as exc:
        pytest.skip(f"로컬 PostgreSQL 없음: {type(exc).__name__}")
    with conn.cursor() as cur:
        cur.execute("SELECT to_regclass('public.pet_members'), to_regclass('public.pet_invites')")
        if any(v is None for v in cur.fetchone()):
            conn.close()
            pytest.skip("로컬 PostgreSQL 에 공동 돌봄 스키마가 없다")
    return conn


def _seed(cur) -> tuple[uuid.UUID, uuid.UUID, uuid.UUID]:
    """대표 · 돌보미 · 강아지 하나씩. kakao_id 는 충돌을 피해 난수로."""
    owner = uuid.uuid4()
    carer = uuid.uuid4()
    pet = uuid.uuid4()
    cur.execute(
        "INSERT INTO app_users (id, kakao_id, status) VALUES (%s, %s, 'active'), (%s, %s, 'active')",
        (owner, uuid.uuid4().int % 10**12, carer, uuid.uuid4().int % 10**12),
    )
    cur.execute(
        "INSERT INTO pets (id, app_user_id, name, breed) VALUES (%s, %s, '맥스', '믹스')",
        (pet, owner),
    )
    cur.execute(
        "INSERT INTO pet_members (pet_id, app_user_id) VALUES (%s, %s)", (pet, carer)
    )
    return owner, carer, pet


def test_withdrawal_deletes_membership():
    """탈퇴는 UPDATE 다. FK 는 안 돌고 트리거 ① 만이 돌보미 행을 지운다."""
    conn = _postgres_or_skip()
    try:
        with conn.cursor() as cur:
            owner, carer, pet = _seed(cur)
            cur.execute(
                "INSERT INTO pet_invites (pet_id, invited_by, token_hash, expires_at)"
                " VALUES (%s, %s, %s, NOW() + INTERVAL '1 day')",
                (pet, owner, "a" * 64),
            )

            cur.execute("UPDATE app_users SET status='withdrawn' WHERE id=%s", (carer,))
            cur.execute("SELECT count(*) FROM pet_members WHERE app_user_id=%s", (carer,))
            assert cur.fetchone()[0] == 0, "탈퇴한 돌보미가 유령으로 남았다"

            cur.execute("UPDATE app_users SET status='withdrawn' WHERE id=%s", (owner,))
            cur.execute("SELECT count(*) FROM pet_invites WHERE invited_by=%s", (owner,))
            assert cur.fetchone()[0] == 0, "탈퇴한 대표의 초대가 남았다"
    finally:
        conn.rollback()
        conn.close()


def test_owner_cannot_be_carer():
    """트리거 ② — 대표를 pet_members 에 넣으면 거절한다."""
    conn = _postgres_or_skip()
    try:
        with conn.cursor() as cur:
            owner, _carer, pet = _seed(cur)
            with pytest.raises(psycopg.errors.RaiseException):
                cur.execute(
                    "INSERT INTO pet_members (pet_id, app_user_id) VALUES (%s, %s)",
                    (pet, owner),
                )
    finally:
        conn.rollback()
        conn.close()


def test_member_lookup_index_exists():
    """app_user_id 인덱스. 마이그레이션에서 누락되면 여기서 걸린다."""
    conn = _postgres_or_skip()
    try:
        with conn.cursor() as cur:
            cur.execute(
                "SELECT indexdef FROM pg_indexes"
                " WHERE tablename='pet_members' AND indexname='idx_pet_members_app_user'"
            )
            row = cur.fetchone()
            assert row is not None, "idx_pet_members_app_user 가 없다"
            assert "app_user_id" in row[0]
    finally:
        conn.rollback()
        conn.close()


def test_care_event_actor_is_nullable():
    """actor 는 '소유자' 가 아니라 '챙긴 사람' 이라 비어 있을 수 있어야 한다."""
    conn = _postgres_or_skip()
    try:
        with conn.cursor() as cur:
            cur.execute(
                "SELECT is_nullable FROM information_schema.columns"
                " WHERE table_name='care_events' AND column_name='actor_app_user_id'"
            )
            row = cur.fetchone()
            assert row is not None, "actor_app_user_id 컬럼이 없다 (개명 누락)"
            assert row[0] == "YES"
    finally:
        conn.rollback()
        conn.close()


def test_migration_is_rerunnable():
    """버전 테이블이 없으므로 두 번 돌려도 안전해야 한다."""
    conn = _postgres_or_skip()
    try:
        sql = open("../db/migrations/2026-09-09_pet_members.sql", encoding="utf-8").read()
        with conn.cursor() as cur:
            cur.execute(sql)
            cur.execute(sql)
    finally:
        conn.rollback()
        conn.close()
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd backend && uv run pytest tests/test_pet_membership_postgres.py -v`
Expected: 전부 **SKIP**("공동 돌봄 스키마가 없다") — 표가 아직 없으니 정상이다. 로컬 postgres 가 없으면 "로컬 PostgreSQL 없음" 으로 skip 된다. **둘 중 어느 쪽이든 이 단계는 통과다.** 초록 PASS 가 나오면 잘못 만든 것이다.

- [ ] **Step 3: `db/init/24_pet_members.sql` 을 쓴다**

```sql
-- ---------------------------------------------------------------------
-- pet_members / pet_invites : 공동 돌봄 (docs/co-care.md)
-- ---------------------------------------------------------------------
-- Order: … -> 03_auth -> 05_pets -> 23_care_events -> 24_pet_members
--
-- 대표는 여기 없다. `pets.app_user_id` 가 대표이고 이 표는 돌보미만 담는다.
-- 구성원 = 대표 ∪ 돌보미. role 칸을 두지 않은 이유가 이것이다 — 한 사람이 양쪽에
-- 동시에 있을 수 없으니 어긋날 자리가 없다.
-- ---------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS pet_members (
    pet_id      UUID NOT NULL REFERENCES pets(id)      ON DELETE CASCADE,

    -- ⚠️ 이 CASCADE 는 **안 돈다.** 탈퇴가 app_users 행을 안 지우기 때문이다
    --    (services/app_auth.py 의 withdraw 는 status 만 바꾼다). 탈퇴한 돌보미를 지우는
    --    것은 아래 트리거다. 여기에 기대면 유령 돌보미가 남는다.
    app_user_id UUID NOT NULL REFERENCES app_users(id) ON DELETE CASCADE,

    joined_at   TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    PRIMARY KEY (pet_id, app_user_id)
);

-- PK 가 (pet_id, …) 라 "내가 돌보는 강아지 전부" 를 못 탄다. 그 조회가 제일 잦다
-- (앱을 켤 때마다). 지우지 말 것.
CREATE INDEX IF NOT EXISTS idx_pet_members_app_user ON pet_members (app_user_id);

CREATE TABLE IF NOT EXISTS pet_invites (
    id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    pet_id     UUID NOT NULL REFERENCES pets(id) ON DELETE CASCADE,

    -- 대표가 바뀌면 이전 대표가 뿌린 초대를 무효로 보는 데 쓴다. 이 CASCADE 도 위와
    -- 같은 이유로 안 돈다 — 트리거가 지운다.
    invited_by UUID NOT NULL REFERENCES app_users(id) ON DELETE CASCADE,

    -- 평문은 저장하지 않는다 (refresh_tokens 와 같은 규칙). core/token.py 의
    -- hash_refresh_token 이 만드는 sha256 hex 64자다.
    token_hash CHAR(64) NOT NULL UNIQUE,

    expires_at TIMESTAMPTZ NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_pet_invites_pet ON pet_invites (pet_id);

-- accepted_at 칸이 없다. 수락하면 행을 지운다 — 그것만으로 일회용이 되고, "이미 쓴 초대"와
-- "없는 토큰"이 같은 404 가 되어 정보도 덜 샌다.

-- ① app_users 는 탈퇴해도 살아남으므로 FK 로는 절대 안 지워진다.
--   21_activity_game.sql 의 activity_owner_cleanup 과 같은 선례다.
CREATE OR REPLACE FUNCTION pet_membership_owner_cleanup() RETURNS trigger LANGUAGE plpgsql AS $func$
BEGIN
    IF NEW.status = 'withdrawn' THEN
        DELETE FROM pet_members WHERE app_user_id = NEW.id;
        DELETE FROM pet_invites  WHERE invited_by  = NEW.id;
    END IF;
    RETURN NEW;
END $func$;
DROP TRIGGER IF EXISTS pet_membership_owner_cleanup ON app_users;
CREATE TRIGGER pet_membership_owner_cleanup AFTER UPDATE OF status ON app_users
FOR EACH ROW EXECUTE FUNCTION pet_membership_owner_cleanup();

-- ② 대표가 돌보미로도 들어오는 것을 DB 가 거절한다. 서비스 검증과 겹치지만, 겹치는 것이
--   정합성 제약의 목적이다.
CREATE OR REPLACE FUNCTION pet_members_not_owner() RETURNS trigger LANGUAGE plpgsql AS $func$
BEGIN
    IF NEW.app_user_id = (SELECT app_user_id FROM pets WHERE id = NEW.pet_id) THEN
        RAISE EXCEPTION '대표는 돌보미가 될 수 없다 (pet=%, user=%)', NEW.pet_id, NEW.app_user_id;
    END IF;
    RETURN NEW;
END $func$;
DROP TRIGGER IF EXISTS pet_members_not_owner ON pet_members;
CREATE TRIGGER pet_members_not_owner BEFORE INSERT OR UPDATE ON pet_members
FOR EACH ROW EXECUTE FUNCTION pet_members_not_owner();

COMMENT ON TABLE pet_members IS
    '공동 돌봄의 돌보미. 대표는 pets.app_user_id 에 있다 (docs/co-care.md)';
```

이어서 같은 파일 끝에 `care_events` 개명을 붙인다. `db/init/` 은 빈 볼륨에서 23 → 24 순서로 도므로, 여기서 고치면 새 DB 는 처음부터 새 이름을 갖는다.

```sql
-- care_events.app_user_id 는 이제 '소유자' 가 아니라 '챙긴 사람' 이다. 이름을 안 바꾸면
-- models/care_event.py 의 옛 주석을 믿은 다음 사람이 이것으로 권한을 검사한다.
ALTER TABLE care_events RENAME COLUMN app_user_id TO actor_app_user_id;
ALTER TABLE care_events ALTER COLUMN actor_app_user_id DROP NOT NULL;
ALTER TABLE care_events DROP CONSTRAINT care_events_app_user_id_fkey;
ALTER TABLE care_events ADD CONSTRAINT care_events_actor_fkey
    FOREIGN KEY (actor_app_user_id) REFERENCES app_users(id) ON DELETE SET NULL;
```

- [ ] **Step 4: `db/migrations/2026-09-09_pet_members.sql` 을 쓴다**

`db/init/24_pet_members.sql` 과 같은 내용이되, **두 번 돌려도 안전해야 한다.** `CREATE TABLE IF NOT EXISTS` · `CREATE OR REPLACE FUNCTION` · `DROP TRIGGER IF EXISTS` 는 이미 안전하고, 안전하지 않은 것은 `ALTER … RENAME` 과 제약 교체뿐이다. 그 부분만 감싼다.

```sql
-- 이미 도는 DB 에 손으로 적용한다. 버전 테이블이 없으므로 여러 번 돌려도 안전해야 한다.
-- 위쪽(두 표 · 인덱스 · 두 트리거)은 db/init/24_pet_members.sql 과 글자 그대로 같다 —
-- 복사해 넣는다.

DO $mig$
BEGIN
    IF EXISTS (
        SELECT 1 FROM information_schema.columns
        WHERE table_name = 'care_events' AND column_name = 'app_user_id'
    ) THEN
        ALTER TABLE care_events RENAME COLUMN app_user_id TO actor_app_user_id;
    END IF;

    IF EXISTS (
        SELECT 1 FROM information_schema.columns
        WHERE table_name = 'care_events' AND column_name = 'actor_app_user_id'
          AND is_nullable = 'NO'
    ) THEN
        ALTER TABLE care_events ALTER COLUMN actor_app_user_id DROP NOT NULL;
    END IF;

    IF EXISTS (
        SELECT 1 FROM information_schema.table_constraints
        WHERE table_name = 'care_events' AND constraint_name = 'care_events_app_user_id_fkey'
    ) THEN
        ALTER TABLE care_events DROP CONSTRAINT care_events_app_user_id_fkey;
    END IF;

    IF NOT EXISTS (
        SELECT 1 FROM information_schema.table_constraints
        WHERE table_name = 'care_events' AND constraint_name = 'care_events_actor_fkey'
    ) THEN
        ALTER TABLE care_events ADD CONSTRAINT care_events_actor_fkey
            FOREIGN KEY (actor_app_user_id) REFERENCES app_users(id) ON DELETE SET NULL;
    END IF;
END $mig$;
```

- [ ] **Step 5: 모델 둘을 만든다**

`backend/src/daengs_backend/models/pet_member.py`:

```python
"""공동 돌봄의 돌보미. 원본 스키마는 `db/init/24_pet_members.sql` 입니다.

**대표는 여기 없습니다.** `pets.app_user_id` 가 대표이고 이 표는 돌보미만 담습니다 —
구성원은 둘의 합집합입니다. `role` 칸이 없는 이유가 그것입니다.
"""

import uuid
from datetime import datetime

from sqlalchemy import DateTime, ForeignKey, Uuid, text
from sqlalchemy.orm import Mapped, mapped_column

from daengs_backend.models.base import Base


class PetMember(Base):
    __tablename__ = "pet_members"

    pet_id: Mapped[uuid.UUID] = mapped_column(
        Uuid, ForeignKey("pets.id", ondelete="CASCADE"), primary_key=True
    )

    #: ⚠️ FK 의 CASCADE 는 **안 돕니다** — 탈퇴가 `app_users` 행을 안 지웁니다.
    #: 탈퇴한 돌보미를 지우는 것은 `pet_membership_owner_cleanup` 트리거입니다.
    app_user_id: Mapped[uuid.UUID] = mapped_column(
        Uuid, ForeignKey("app_users.id", ondelete="CASCADE"), primary_key=True
    )

    joined_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=text("NOW()")
    )

    def __repr__(self) -> str:
        return f"<PetMember pet={self.pet_id} user={self.app_user_id}>"
```

`backend/src/daengs_backend/models/pet_invite.py`:

```python
"""공동 돌봄 초대. 원본 스키마는 `db/init/24_pet_members.sql` 입니다.

**평문 토큰이 없습니다** — `token_hash` 만 둡니다 (`refresh_tokens` 와 같은 규칙).
`accepted_at` 도 없습니다: 수락하면 행을 지워서 일회용이 됩니다.
"""

import uuid
from datetime import datetime

from sqlalchemy import CHAR, DateTime, ForeignKey, Uuid, text
from sqlalchemy.orm import Mapped, mapped_column

from daengs_backend.models.base import Base


class PetInvite(Base):
    __tablename__ = "pet_invites"

    id: Mapped[uuid.UUID] = mapped_column(
        Uuid, primary_key=True, server_default=text("gen_random_uuid()")
    )
    pet_id: Mapped[uuid.UUID] = mapped_column(
        Uuid, ForeignKey("pets.id", ondelete="CASCADE")
    )

    #: 초대한 대표. 대표가 바뀌면 이전 대표가 뿌린 초대를 무효로 보는 데 씁니다.
    invited_by: Mapped[uuid.UUID] = mapped_column(
        Uuid, ForeignKey("app_users.id", ondelete="CASCADE")
    )

    token_hash: Mapped[str] = mapped_column(CHAR(64), unique=True)
    expires_at: Mapped[datetime] = mapped_column(DateTime(timezone=True))
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=text("NOW()")
    )

    def __repr__(self) -> str:
        # 토큰 해시는 repr 로 흘리지 않습니다.
        return f"<PetInvite {self.id} pet={self.pet_id}>"
```

- [ ] **Step 6: `care_events` 모델을 개명한다**

`backend/src/daengs_backend/models/care_event.py` 의 `app_user_id` 블록을 통째로 바꾼다. **주석이 핵심이다** — 지금 주석(*"`pets.app_user_id` 와 같은 값"*)이 이 변경으로 거짓이 된다.

```python
    #: **챙긴 사람.** 소유자가 아닙니다 — 이 기록의 주인은 강아지입니다 (docs/co-care.md).
    #:
    #: `None` 은 **탈퇴한 보호자**입니다. 화면에 이름을 낼지는 "지금도 구성원인가" 가 정하고,
    #: 그 규칙은 `services/pet_member.py` 의 `actor_label` 하나입니다.
    actor_app_user_id: Mapped[uuid.UUID | None] = mapped_column(
        Uuid, ForeignKey("app_users.id", ondelete="SET NULL")
    )
```

- [ ] **Step 7: 모델을 등록한다**

`backend/src/daengs_backend/models/__init__.py` 의 import 블록(알파벳 순)에 더하고 `__all__` 에도 넣는다. 모듈 머리말이 *"새 모델은 아래 import 에도 추가하세요"* 라고 적어 둔 자리다.

```python
from daengs_backend.models.pet_invite import PetInvite
from daengs_backend.models.pet_member import PetMember
```

- [ ] **Step 8: 개명을 따라가는 곳을 고친다**

개명하면 기존 코드가 깨진다. `grep -rn "app_user_id" backend/src/daengs_backend/services/care_event.py backend/src/daengs_backend/repositories/care_event.py` 로 찾아, `CareEvent(...)` 생성자와 쿼리 조건의 `app_user_id=` 를 `actor_app_user_id=` 로 바꾼다. `backend/tests/test_care_events.py` 의 `FakeCareEvent` 필드명도 같이 바꾼다.

⚠️ **`care_repo.get_owned(session, app_user_id, event_id)` 의 의미가 바뀐다.** 지금은 "내 기록" 을 소유자로 거르는데, 앞으로 삭제 권한은 Task 6 에서 다시 정한다. 이 단계에서는 **필드명만 바꾸고 동작은 그대로 둔다** (그래야 기존 테스트가 그대로 통과해 개명이 안전했음을 보인다).

- [ ] **Step 9: 로컬 DB 에 스키마를 넣고 테스트가 통과하는지 본다**

로컬 개발용 postgres 가 떠 있어야 한다. **서버 DB 를 쓰지 않는다** — 테스트가 loopback 만 허용한다.

Run: `psql "postgresql://postgres:postgres@127.0.0.1:5432/vectordb" -f db/migrations/2026-09-09_pet_members.sql`
그다음: `cd backend && uv run pytest tests/test_pet_membership_postgres.py -v`
Expected: 5개 PASS. (로컬 postgres 가 없으면 전부 SKIP 이고, 그때는 Task 2 의 CI 가 이것을 대신 증명한다.)

- [ ] **Step 10: 기존 스위트가 안 깨졌는지 본다**

Run: `cd backend && uv run pytest tests/test_care_events.py tests/test_pets.py -q`
Expected: PASS. 실패하면 Step 8 의 개명이 빠진 곳이 있다.

- [ ] **Step 11: 커밋**

```bash
git add db/init/24_pet_members.sql db/migrations/2026-09-09_pet_members.sql \
        backend/src/daengs_backend/models/ backend/src/daengs_backend/services/care_event.py \
        backend/src/daengs_backend/repositories/care_event.py \
        backend/tests/test_pet_membership_postgres.py backend/tests/test_care_events.py
git commit -m "feat: 공동 돌봄 스키마 — pet_members·pet_invites 와 트리거 둘

app_users 가 탈퇴해도 안 지워져 FK 가 안 돈다. 21_activity_game.sql 의 선례대로
status UPDATE 트리거로 정리한다. care_events.app_user_id 는 소유자가 아니라
'챙긴 사람' 이 되므로 actor_app_user_id 로 개명하고 nullable 로 내린다."
```

---

## Task 2: CI 에 PostgreSQL 붙이기

Task 1 의 테스트는 지금 CI 에서 통째로 skip 된다. 개인정보 파기 보장 둘이 거기 걸려 있다.

**Files:**
- Modify: `.github/workflows/backend-tests.yml`

**Interfaces:**
- Consumes: Task 1 의 `db/init/*.sql`
- Produces: CI 에서 `DAENGS_TEST_DATABASE_URL` 이 loopback postgres 를 가리킨다

- [ ] **Step 1: 워크플로에 서비스와 스키마 적재를 더한다**

`jobs.test` 아래 `runs-on` 다음에 `services` 를 넣고, `- name: 의존성` **앞에** 적재 단계를 넣는다.

```yaml
    services:
      postgres:
        # 층 2 테스트(트리거·인덱스·마이그레이션 재실행)는 진짜 PostgreSQL 이 있어야
        # 돈다. 없으면 pytest.skip 으로 조용히 넘어가는데, 그 층이 지고 있는 것이
        # 개인정보 파기 보장이라 조용히 넘어가면 안 된다 (docs/co-care.md §5).
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: vectordb
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s --health-timeout 5s --health-retries 5
```

`steps` 안, `actions/checkout` 다음에:

```yaml
      # db/init/ 은 "볼륨이 빌 때만" 도는 것이라 서비스 컨테이너에서는 자동 실행되지
      # 않는다. 순서대로 직접 먹인다 — 파일 이름의 숫자가 곧 의존 순서다.
      - name: 스키마 적재
        working-directory: .
        run: |
          for f in $(ls db/init/*.sql | sort); do
            echo "== $f"
            PGPASSWORD=postgres psql -h 127.0.0.1 -U postgres -d vectordb -v ON_ERROR_STOP=1 -f "$f"
          done
        env:
          PGPASSWORD: postgres
```

그리고 테스트 단계에 DSN 을 준다:

```yaml
      - name: 테스트
        run: uv run pytest -q
        env:
          DAENGS_TEST_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/vectordb
```

- [ ] **Step 2: 머리 주석을 갱신한다**

파일 머리 주석에 한 문단을 더한다 — 이 워크플로가 "왜 이렇게 생겼나"를 적어 두는 곳이다.

```
# **postgres 서비스가 붙어 있습니다.** 대부분의 테스트는 가짜 리포지토리로 돌지만,
# 트리거·FK·인덱스는 가짜로 증명할 수 없습니다. 그 층(`test_pet_membership_postgres.py` ·
# `test_release_fk_postgres.py`)은 DSN 이 없으면 **조용히 skip** 되므로, 서비스를 떼면
# 검사가 사라진 것을 아무도 모릅니다.
```

- [ ] **Step 3: 되살아난 테스트가 진짜 도는지 본다**

이 단계는 CI 에서만 확인된다. push 한 뒤 Actions 로그에서 다음을 본다:
- `스키마 적재` 단계가 `db/init/*.sql` 을 전부 통과했는가
- `test_pet_membership_postgres.py` 5개가 **SKIP 이 아니라 PASS** 인가
- `test_release_fk_postgres.py` 도 SKIP 에서 PASS 로 바뀌었는가

Expected: 셋 다 그렇다. `test_release_fk_postgres.py` 가 여기서 실패하면 **이 PR 이 만든 문제가 아니라 원래 있던 문제가 드러난 것이다** — 그때는 멈추고 사람에게 알린다.

- [ ] **Step 4: 커밋**

```bash
git add .github/workflows/backend-tests.yml
git commit -m "ci: backend 테스트에 postgres 서비스 — 트리거·FK 층이 skip 되지 않게

test_pet_membership_postgres.py 와 이미 조용히 skip 되고 있던
test_release_fk_postgres.py 가 같이 살아난다."
```

---

## Task 3: 접근 판정 — `get_accessible` · `count_accessible`

**Files:**
- Create: `backend/src/daengs_backend/repositories/pet_member.py`
- Modify: `backend/src/daengs_backend/repositories/pet.py`
- Modify: `backend/tests/fakes.py`
- Test: `backend/tests/test_pet_members.py`

**Interfaces:**
- Produces:
  - `pet_repo.get_accessible(session, app_user_id, pet_id, *, for_update=False) -> Pet | None`
  - `pet_repo.count_accessible(session, app_user_id) -> int`
  - `pet_member_repo.list_members(session, pet_id) -> list[uuid.UUID]` — 대표를 **제외한** 돌보미 id
  - `pet_member_repo.is_member(session, pet_id, app_user_id) -> bool` — 대표도 True
  - `pet_member_repo.add(session, pet_id, app_user_id) -> None`
  - `pet_member_repo.remove(session, pet_id, app_user_id) -> int` — 지운 행 수
  - `pet_member_repo.count_members(session, pet_id) -> int` — 대표 포함

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`backend/tests/test_pet_members.py` 를 만든다. 이 파일은 이후 Task 5~7 이 계속 늘려 쓴다.

```python
"""공동 돌봄 — 구성원 판정과 초대·승계·퇴장의 규칙 (docs/co-care.md).

DB 는 쓰지 않습니다 (`test_care_events.py` 와 같은 규칙). 트리거의 증명은
`test_pet_membership_postgres.py` 에 있습니다 — 여기서 보는 것은 **규칙**입니다.
"""

import uuid

import pytest
from fakes import FakeAdmin, FakePet, Store, install

from daengs_backend.repositories import pet as pet_repo

OWNER = uuid.uuid4()
CARER = uuid.uuid4()
STRANGER = uuid.uuid4()


@pytest.fixture
def store(monkeypatch: pytest.MonkeyPatch) -> Store:
    s = Store(FakeAdmin(login_id="admin", password_hash="x", name="관리자", role="OWNER"))
    install(s, monkeypatch)
    return s


@pytest.fixture
def pet(store: Store) -> FakePet:
    p = FakePet(app_user_id=OWNER, name="맥스", breed="믹스")
    store.pets.append(p)
    return p


async def test_owner_is_accessible(store: Store, pet: FakePet):
    assert await pet_repo.get_accessible(None, OWNER, pet.id) is pet


async def test_carer_is_accessible(store: Store, pet: FakePet):
    store.pet_members.append((pet.id, CARER))
    assert await pet_repo.get_accessible(None, CARER, pet.id) is pet


async def test_stranger_is_not_accessible(store: Store, pet: FakePet):
    assert await pet_repo.get_accessible(None, STRANGER, pet.id) is None


async def test_get_owned_still_owner_only(store: Store, pet: FakePet):
    """돌보미는 get_owned 로는 안 잡혀야 한다 — 수정·삭제가 그것을 쓴다."""
    store.pet_members.append((pet.id, CARER))
    assert await pet_repo.get_owned(None, CARER, pet.id) is None


async def test_count_accessible_includes_carer_pets(store: Store, pet: FakePet):
    """미니룸 상한은 '내 방에 서는 아이 수' 다 — 돌보미로 참여한 아이도 센다."""
    mine = FakePet(app_user_id=CARER, name="네오", breed="푸들")
    store.pets.append(mine)
    store.pet_members.append((pet.id, CARER))
    assert await pet_repo.count_accessible(None, CARER) == 2
    assert await pet_repo.count_for_owner(None, CARER) == 1
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd backend && uv run pytest tests/test_pet_members.py -v`
Expected: FAIL — `AttributeError: module 'daengs_backend.repositories.pet' has no attribute 'get_accessible'` (그리고 `Store` 에 `pet_members` 가 없다는 `AttributeError`).

- [ ] **Step 3: `repositories/pet_member.py` 를 쓴다**

```python
"""pet_members / pet_invites 조회·저장. 쿼리만 있고 판단은 없습니다.

"초대해도 되나"·"상한을 넘었나"·"승계 순서" 는 services/pet_member.py 가 정합니다.
commit 도 하지 않습니다.
"""

import uuid
from datetime import datetime

from sqlalchemy import delete as sql_delete
from sqlalchemy import func, select
from sqlalchemy.ext.asyncio import AsyncSession

from daengs_backend.models import Pet, PetInvite, PetMember

__all__ = [
    "add",
    "add_invite",
    "count_members",
    "count_valid_invites",
    "delete_expired_invites",
    "delete_invite",
    "delete_invites_for_pet",
    "get_invite_by_hash",
    "is_member",
    "list_members",
    "remove",
]


async def list_members(session: AsyncSession, pet_id: uuid.UUID) -> list[uuid.UUID]:
    """그 아이의 **돌보미** id 들, 참여 순서대로. **대표는 안 들어 있습니다.**"""
    stmt = (
        select(PetMember.app_user_id)
        .where(PetMember.pet_id == pet_id)
        .order_by(PetMember.joined_at, PetMember.app_user_id)
    )
    return list(await session.scalars(stmt))


async def is_member(
    session: AsyncSession, pet_id: uuid.UUID, app_user_id: uuid.UUID
) -> bool:
    """구성원인가. **대표도 True 입니다** — 구성원은 대표 ∪ 돌보미입니다."""
    stmt = select(Pet.id).where(Pet.id == pet_id, Pet.app_user_id == app_user_id)
    if await session.scalar(stmt) is not None:
        return True
    member = select(PetMember.pet_id).where(
        PetMember.pet_id == pet_id, PetMember.app_user_id == app_user_id
    )
    return await session.scalar(member) is not None


async def count_members(session: AsyncSession, pet_id: uuid.UUID) -> int:
    """구성원 수. **대표를 포함해서 셉니다** — 상한 검사가 이것을 씁니다."""
    stmt = select(func.count()).select_from(PetMember).where(PetMember.pet_id == pet_id)
    return int(await session.scalar(stmt) or 0) + 1


def add(session: AsyncSession, pet_id: uuid.UUID, app_user_id: uuid.UUID) -> PetMember:
    row = PetMember(pet_id=pet_id, app_user_id=app_user_id)
    session.add(row)
    return row


async def remove(
    session: AsyncSession, pet_id: uuid.UUID, app_user_id: uuid.UUID
) -> int:
    """지운 행 수. 0 이면 애초에 돌보미가 아니었습니다."""
    result = await session.execute(
        sql_delete(PetMember).where(
            PetMember.pet_id == pet_id, PetMember.app_user_id == app_user_id
        )
    )
    return int(result.rowcount or 0)


async def get_invite_by_hash(session: AsyncSession, token_hash: str) -> PetInvite | None:
    return await session.scalar(select(PetInvite).where(PetInvite.token_hash == token_hash))


async def count_valid_invites(
    session: AsyncSession, pet_id: uuid.UUID, now: datetime
) -> int:
    stmt = (
        select(func.count())
        .select_from(PetInvite)
        .where(PetInvite.pet_id == pet_id, PetInvite.expires_at > now)
    )
    return int(await session.scalar(stmt) or 0)


def add_invite(
    session: AsyncSession,
    *,
    pet_id: uuid.UUID,
    invited_by: uuid.UUID,
    token_hash: str,
    expires_at: datetime,
) -> PetInvite:
    row = PetInvite(
        pet_id=pet_id, invited_by=invited_by, token_hash=token_hash, expires_at=expires_at
    )
    session.add(row)
    return row


async def delete_invite(session: AsyncSession, invite_id: uuid.UUID) -> int:
    result = await session.execute(sql_delete(PetInvite).where(PetInvite.id == invite_id))
    return int(result.rowcount or 0)


async def delete_expired_invites(
    session: AsyncSession, pet_id: uuid.UUID, now: datetime
) -> int:
    """만료건 청소. **새 초대를 만들 때 부릅니다** — 아무도 안 누르면 영원히 쌓입니다."""
    result = await session.execute(
        sql_delete(PetInvite).where(PetInvite.pet_id == pet_id, PetInvite.expires_at <= now)
    )
    return int(result.rowcount or 0)


async def delete_invites_for_pet(session: AsyncSession, pet_id: uuid.UUID) -> int:
    """그 아이의 초대 전부. 승계가 부릅니다 — 옛 대표가 뿌린 링크를 죽입니다."""
    result = await session.execute(sql_delete(PetInvite).where(PetInvite.pet_id == pet_id))
    return int(result.rowcount or 0)
```

- [ ] **Step 4: `repositories/pet.py` 에 둘을 더한다**

`__all__` 에 `"count_accessible"`, `"get_accessible"` 을 알파벳 순으로 넣고, `PetMember` 를 import 한다. `get_owned` 바로 아래에 놓는다.

```python
def _is_member(app_user_id: uuid.UUID):
    """구성원 = 대표 ∪ 돌보미. **쓰기·파기에는 쓰지 마세요** — 그쪽은 `get_owned` 입니다."""
    return or_(
        Pet.app_user_id == app_user_id,
        Pet.id.in_(
            select(PetMember.pet_id).where(PetMember.app_user_id == app_user_id)
        ),
    )


async def get_accessible(
    session: AsyncSession,
    app_user_id: uuid.UUID,
    pet_id: uuid.UUID,
    *,
    for_update: bool = False,
) -> Pet | None:
    """**구성원일 때** 돌려줍니다. 읽기와 기록이 이것을 씁니다.

    `get_owned` 와 이름을 갈라 둔 것이 의도입니다 — 프로필 수정·배웅·삭제는 대표만이라
    거기서 이 함수를 부르면 돌보미가 강아지를 지웁니다. 잘못 부른 것이 이름으로 보여야
    합니다.
    """
    stmt = select(Pet).where(Pet.id == pet_id, _is_member(app_user_id))
    if for_update:
        stmt = stmt.with_for_update(of=Pet)
    return await session.scalar(stmt)


async def count_accessible(session: AsyncSession, app_user_id: uuid.UUID) -> int:
    """**미니룸에 서는 아이 수.** 상한 검사가 이것을 씁니다 (docs/co-care.md §2).

    `count_for_owner` 와 다릅니다 — 저건 파기·소유 판단용이고 이건 화면 용량입니다.
    """
    stmt = select(func.count()).select_from(Pet).where(_is_member(app_user_id))
    return int(await session.scalar(stmt) or 0)
```

import 줄에 `or_` 를 더한다: `from sqlalchemy import func, or_, select`. 그리고 `from daengs_backend.models import Pet, PetMember`.

- [ ] **Step 5: 가짜를 확장한다**

`backend/tests/fakes.py` 의 `Store.__init__` 에 더한다:

```python
        #: 공동 돌봄의 돌보미. `(pet_id, app_user_id)` 짝입니다 — 대표는 여기 없고
        #: `FakePet.app_user_id` 가 대표입니다 (docs/co-care.md).
        self.pet_members: list[tuple[uuid.UUID, uuid.UUID]] = []
        #: 초대. `install` 이 만드는 `FakeInvite` 를 담습니다.
        self.pet_invites: list = []
```

`install` 안, `pet_get_owned` 옆에 더하고 `monkeypatch.setattr` 두 줄을 붙인다:

```python
    def _member_pet_ids(user_id):
        return {pid for pid, uid in store.pet_members if uid == user_id}

    async def pet_get_accessible(session, app_user_id, pet_id, *, for_update=False):
        ids = _member_pet_ids(app_user_id)
        return next(
            (
                p
                for p in store.pets
                if p.id == pet_id and (p.app_user_id == app_user_id or p.id in ids)
            ),
            None,
        )

    async def pet_count_accessible(session, app_user_id):
        ids = _member_pet_ids(app_user_id)
        return sum(1 for p in store.pets if p.app_user_id == app_user_id or p.id in ids)
```

```python
    monkeypatch.setattr(pet_repo, "get_accessible", pet_get_accessible)
    monkeypatch.setattr(pet_repo, "count_accessible", pet_count_accessible)
```

- [ ] **Step 6: 테스트가 통과하는지 본다**

Run: `cd backend && uv run pytest tests/test_pet_members.py -v`
Expected: 5개 PASS.

- [ ] **Step 7: 커밋**

```bash
git add backend/src/daengs_backend/repositories/ backend/tests/fakes.py backend/tests/test_pet_members.py
git commit -m "feat: 구성원 판정 — pet_repo.get_accessible / count_accessible

get_owned 는 대표 전용으로 남긴다. 이름이 갈려 있어야 프로필 수정·삭제에서
잘못 부른 것이 눈에 띈다."
```

---

## Task 4: 호출부를 구성원 기준으로 옮기기

**Files:**
- Modify: `backend/src/daengs_backend/services/care_event.py:61`
- Modify: `backend/src/daengs_backend/services/pet.py` (조회 3곳만)
- Modify: `backend/src/daengs_backend/repositories/chat.py:17,26`
- Modify: `backend/src/daengs_backend/repositories/gait_record.py:27`
- Modify: `backend/src/daengs_backend/repositories/walk_entry.py:40`
- Modify: `backend/src/daengs_backend/repositories/territory_claim.py:38`
- Modify: `backend/src/daengs_backend/repositories/walk.py:127`
- Test: `backend/tests/test_pet_members.py`, `backend/tests/test_care_events.py`

**Interfaces:**
- Consumes: Task 3 의 `pet_repo.get_accessible`, `pet_repo._is_member`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`test_pet_members.py` 끝에 더한다.

```python
async def test_carer_can_read_profile_but_not_edit(store: Store, pet: FakePet):
    """돌보미는 보고 기록한다. 수정·배웅·삭제·사진은 대표만이다."""
    from daengs_backend.services import pet as pet_service

    store.pet_members.append((pet.id, CARER))
    assert (await pet_service.get_one(None, CARER, pet.id)).id == pet.id
    with pytest.raises(pet_service.PetNotFoundError):
        await pet_service.delete(None, CARER, pet.id)
```

`test_care_events.py` 끝에 둘을 더한다.

```python
async def test_carer_can_record_and_read(store, pet):
    """돌보미가 기록하고, 대표가 그것을 본다 — 이 기능의 전부다."""
    store.pet_members.append((pet.id, CARER))
    body = {
        "pet_id": str(pet.id), "kind": "meal",
        "occurred_at": "2026-09-09T08:12:00+09:00",
        "client_event_id": str(uuid.uuid4()),
    }
    assert client_as(CARER).post("/app/care-events", json=body).status_code == 201
    seen = client_as(OWNER).get(f"/app/care-events/today?pet_id={pet.id}").json()
    assert seen["meal"] == 1


async def test_day_summary_counts_other_members_walks(store, pet):
    """하루 요약은 아빠의 산책도 센다 — repositories/walk.py 의 소유자 조건을 뺀 결과다."""
    store.pet_members.append((pet.id, CARER))
    store.walks.append(
        FakeWalk(
            app_user_id=CARER,
            started_at=datetime(2026, 9, 9, 8, 0, tzinfo=SEOUL),
            ended_at=datetime(2026, 9, 9, 8, 40, tzinfo=SEOUL),
            pets=[FakeWalkPet(pet_id=pet.id)],
        )
    )
    got = client_as(OWNER).get(f"/app/care-events/today?pet_id={pet.id}&day=2026-09-09").json()
    assert got["walk"] == 1
```

⚠️ `client_as(...)` · `pet` fixture 는 `test_care_events.py` 에 이미 있는 것을 쓴다. 이름이 다르면 그 파일의 기존 헬퍼 이름에 맞춘다 — **새로 만들지 않는다.** `FakeWalk` · `FakeWalkPet` 는 `fakes.py` 에서 import 한다.

- [ ] **Step 2: 실패를 확인한다**

Run: `cd backend && uv run pytest tests/test_pet_members.py tests/test_care_events.py -v -k "carer or other_members"`
Expected: FAIL — 돌보미가 404 를 받는다(아직 `get_owned` 를 쓰고 있다), 산책 수가 0 이다.

- [ ] **Step 3: `services/care_event.py` 를 옮긴다**

`_owned_pet` 을 이름과 내용 모두 바꾼다.

```python
async def _accessible_pet(session: AsyncSession, app_user_id: uuid.UUID, pet_id: uuid.UUID):
    """**구성원이 아니면** 404 감. 배웅한 아이도 통과합니다 — 배웅은 행을 안 지우고,
    있었던 일을 적어 두는 것이라 그 아이의 기록은 계속 보이고 남길 수 있어야 합니다."""
    pet = await pet_repo.get_accessible(session, app_user_id, pet_id)
    if pet is None:
        raise PetNotFoundError
    return pet
```

같은 파일에서 `_owned_pet(` 을 부르는 곳 전부를 `_accessible_pet(` 으로 바꾼다 (`record` · `list_events` · `day_summary`).

- [ ] **Step 4: `services/pet.py` 의 조회 3곳만 옮긴다**

`pet_repo.get_owned(` 를 부르는 곳이 여럿이다. **조회하는 것만** `get_accessible` 로 바꾼다:

- 프로필 한 건 조회 → `get_accessible`
- 돌봄 정보 조회 → `get_accessible`
- 사진 조회 → `get_accessible`

**그대로 두는 것** (`for_update=True` 가 붙은 것들과 삭제·배웅·대표 지정): 프로필 수정 · 배웅 · 삭제 · 사진 업로드/확정 · `set_primary`.

⚠️ 판단 기준은 **"되돌리기 어려운가"** 다. 헷갈리면 `get_owned` 로 둔다 — 틀렸을 때 돌보미가 못 하는 것(불편)이고, 반대로 틀리면 돌보미가 남의 강아지를 지운다(사고).

- [ ] **Step 5: 리포지토리 넷을 옮긴다**

넷 다 `Pet.app_user_id == <caller>` 조건을 `pet_repo._is_member(<caller>)` 로 바꾼다. `from daengs_backend.repositories import pet as pet_repo` 를 import 한다 (순환 import 가 나면 `_is_member` 를 `repositories/pet_member.py` 로 옮기고 양쪽이 그것을 쓴다).

- `repositories/chat.py:17,26` — `.where(Pet.id == pet_id, pet_repo._is_member(app_user_id))`
- `repositories/gait_record.py:27` — `.where(pet_repo._is_member(app_user_id), GaitRecord.deleted_at.is_(None))`
- `repositories/walk_entry.py:40` — `select(Pet.id).where(Pet.id == pet_id, pet_repo._is_member(owner))`
- `repositories/territory_claim.py:38` — `Pet.id.in_(pet_ids), pet_repo._is_member(owner), Pet.farewell_on.is_(None)`

- [ ] **Step 6: `repositories/walk.py:127` 의 한 줄을 뺀다**

`count_for_pet_between` 의 `.where(...)` 에서 `Walk.app_user_id == app_user_id,` 줄을 **삭제한다.** 인자 `app_user_id` 는 시그니처에 남기되(부르는 쪽을 안 고치려고) 안 쓰게 되므로 `_app_user_id` 로 바꾼다. docstring 도 고친다:

```python
    """그 아이가 나간 산책 수 — `started_at` 이 `[start, end)` 인 것.

    **소유자 조건을 걸지 않습니다.** 부르는 쪽(`care_event.day_summary`)이 이미 강아지
    접근 권한을 확인한 뒤라, 여기서 다시 사람으로 거르면 **다른 보호자의 산책만 빠집니다**
    (docs/co-care.md §2). `walk_pets` 조인이 "그 아이가 나간 산책" 을 정확히 집습니다.
    """
```

- [ ] **Step 7: 테스트가 통과하는지 본다**

Run: `cd backend && uv run pytest tests/test_pet_members.py tests/test_care_events.py tests/test_pets.py tests/test_walk_api.py tests/test_chat_api.py -q`
Expected: PASS.

- [ ] **Step 8: 전체 스위트를 돌린다**

Run: `cd backend && uv run pytest -q`
Expected: PASS (약 6분). 접근 판정을 건드렸으니 여기서 한 번 전부 본다.

- [ ] **Step 9: 커밋**

```bash
git add backend/src/daengs_backend/ backend/tests/
git commit -m "feat: 돌보미가 강아지를 읽고 기록한다 — 접근 판정을 구성원 기준으로

수정·배웅·삭제·사진은 대표만 그대로다. walk.count_for_pet_between 의 소유자 조건은
뺀다 — 부르는 쪽이 이미 권한을 봤고, 그 줄 때문에 다른 보호자의 산책이 요약에서 빠졌다."
```

---

## Task 5: 초대 만들기와 수락

**Files:**
- Create: `backend/src/daengs_backend/schemas/pet_member.py`, `backend/src/daengs_backend/services/pet_member.py`, `backend/src/daengs_backend/routers/pet_member.py`
- Modify: `backend/src/daengs_backend/main.py`
- Test: `backend/tests/test_pet_members.py`

**Interfaces:**
- Produces:
  - `services.pet_member.create_invite(session, app_user_id, pet_id) -> tuple[PetInvite, str]` — 두 번째가 **평문 토큰**(이때만 존재)
  - `services.pet_member.accept_invite(session, app_user_id, token) -> Pet`
  - 예외: `InviteNotFoundError` · `InviteExpiredError` · `AlreadyOwnerError` · `MemberLimitError` · `PetLimitError` · `InviteLimitError`
  - 상수: `MAX_MEMBERS_PER_PET = 5` · `MAX_ACTIVE_INVITES = 3` · `INVITE_TTL = timedelta(hours=24)`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`test_pet_members.py` 에 더한다.

```python
def _invite(store, pet, owner=OWNER):
    """대표가 초대를 만들고 평문 토큰을 얻는다."""
    r = client_as(owner).post(f"/app/pets/{pet.id}/invites")
    assert r.status_code == 201, r.text
    return r.json()["token"]


async def test_carer_cannot_invite(store: Store, pet: FakePet):
    store.pet_members.append((pet.id, CARER))
    assert client_as(CARER).post(f"/app/pets/{pet.id}/invites").status_code == 404


async def test_accept_makes_member(store: Store, pet: FakePet):
    token = _invite(store, pet)
    r = client_as(CARER).post("/app/pet-invites/accept", json={"token": token})
    assert r.status_code == 200
    assert (pet.id, CARER) in store.pet_members


async def test_accept_is_idempotent(store: Store, pet: FakePet):
    """카톡 링크는 두 번 눌린다. 두 번째도 200 이어야 한다."""
    token = _invite(store, pet)
    client_as(CARER).post("/app/pet-invites/accept", json={"token": token})
    r = client_as(CARER).post("/app/pet-invites/accept", json={"token": token})
    assert r.status_code == 200


async def test_unknown_token_is_404(store: Store, pet: FakePet):
    r = client_as(CARER).post("/app/pet-invites/accept", json={"token": "nope"})
    assert r.status_code == 404


async def test_expired_token_is_410(store: Store, pet: FakePet):
    token = _invite(store, pet)
    store.pet_invites[0].expires_at = datetime(2020, 1, 1, tzinfo=UTC)
    r = client_as(CARER).post("/app/pet-invites/accept", json={"token": token})
    assert r.status_code == 410


async def test_owner_accepting_own_invite_is_409(store: Store, pet: FakePet):
    """트리거 ② 가 DB 에서도 막지만, 서비스가 먼저 거절해야 500 이 안 난다."""
    token = _invite(store, pet)
    r = client_as(OWNER).post("/app/pet-invites/accept", json={"token": token})
    assert r.status_code == 409


async def test_invite_limit_is_three(store: Store, pet: FakePet):
    for _ in range(3):
        _invite(store, pet)
    assert client_as(OWNER).post(f"/app/pets/{pet.id}/invites").status_code == 409


async def test_creating_invite_clears_expired(store: Store, pet: FakePet):
    _invite(store, pet)
    store.pet_invites[0].expires_at = datetime(2020, 1, 1, tzinfo=UTC)
    _invite(store, pet)
    assert len(store.pet_invites) == 1


async def test_accept_fills_empty_primary_pet(store: Store, pet: FakePet):
    """등록한 강아지가 없는 신규 돌보미는 첫 수락에서 대표 강아지를 얻는다."""
    user = store.app_users[CARER_KAKAO]
    user.primary_pet_id = None
    token = _invite(store, pet)
    client_as(CARER).post("/app/pet-invites/accept", json={"token": token})
    assert user.primary_pet_id == pet.id


async def test_accept_respects_miniroom_limit(store: Store, pet: FakePet):
    """수락자가 이미 5마리면 6번째가 방에 못 선다."""
    for i in range(5):
        store.pets.append(FakePet(app_user_id=CARER, name=f"강아지{i}", breed="믹스"))
    token = _invite(store, pet)
    r = client_as(CARER).post("/app/pet-invites/accept", json={"token": token})
    assert r.status_code == 409
```

⚠️ `client_as` · `CARER_KAKAO` 는 이 파일에 없다. `test_care_events.py` 가 `CurrentAppUser` 를 `dependency_overrides` 로 갈아 끼우는 방식을 **그대로 베껴** 이 파일 위쪽에 만든다. `store.app_users` 는 `kakao_id` 로 키가 걸린 dict 이므로, fixture 에서 `OWNER`·`CARER` 에 대응하는 `FakeAppUser` 를 각각 넣고 그 `kakao_id` 를 상수로 둔다.

- [ ] **Step 2: 실패를 확인한다**

Run: `cd backend && uv run pytest tests/test_pet_members.py -v -k "invite or accept"`
Expected: FAIL — 404 (라우트가 없다).

- [ ] **Step 3: 스키마를 쓴다**

`backend/src/daengs_backend/schemas/pet_member.py`:

```python
"""공동 돌봄 API 의 요청 / 응답 형태 (docs/co-care.md).

앱 짝 PR 과 손으로 맞춥니다. 한쪽만 고치지 마세요.
"""

import uuid
from datetime import datetime

from pydantic import BaseModel, Field


class InviteCreated(BaseModel):
    """**평문 토큰은 여기서 한 번만 나옵니다.** 서버는 해시만 들고 있어 다시 못 보여 줍니다."""

    id: uuid.UUID
    pet_id: uuid.UUID
    token: str
    expires_at: datetime


class InviteAccept(BaseModel):
    #: 초대 링크에 실린 토큰. `pet_id` 를 안 받는 것이 의도입니다 — 수락 전에는 그 강아지에
    #: 아무 권한이 없어서, URL·본문에 실으면 남의 강아지 id 를 넣어 보는 자리가 생깁니다.
    token: str = Field(min_length=1, max_length=200)


class MemberOut(BaseModel):
    app_user_id: uuid.UUID
    #: 지금도 구성원일 때만 이름이 옵니다. 아니면 `None` 이고 앱이 "이전 보호자" 로 그립니다.
    nickname: str | None
    is_owner: bool
    joined_at: datetime | None


class MemberListResponse(BaseModel):
    pet_id: uuid.UUID
    members: list[MemberOut]


class OwnerTransfer(BaseModel):
    app_user_id: uuid.UUID


__all__ = [
    "InviteAccept",
    "InviteCreated",
    "MemberListResponse",
    "MemberOut",
    "OwnerTransfer",
]
```

- [ ] **Step 4: 서비스를 쓴다**

`backend/src/daengs_backend/services/pet_member.py`:

```python
"""공동 돌봄의 규칙. 트랜잭션 경계도 여기입니다 (docs/co-care.md §3).

**락은 `pets` 행에 겁니다.** `pet_members` 를 세고 INSERT 하는 사이에 다른 수락이 끼면
상한을 넘기고, 탈퇴 가드와도 같은 자원을 두고 줄을 서야 합니다.
"""

import logging
import uuid
from datetime import UTC, datetime, timedelta

from sqlalchemy.ext.asyncio import AsyncSession

from daengs_backend.core.token import generate_refresh_token, hash_refresh_token
from daengs_backend.models import Pet, PetInvite
from daengs_backend.repositories import app_user as app_user_repo
from daengs_backend.repositories import pet as pet_repo
from daengs_backend.repositories import pet_member as member_repo
from daengs_backend.services.pet import MAX_PETS_PER_USER, PetNotFoundError

log = logging.getLogger(__name__)

#: 한 강아지의 구성원 수(대표 포함). 가족이 쓰는 기능이라 넉넉하되 무한은 아닙니다.
MAX_MEMBERS_PER_PET = 5

#: 동시에 살아 있을 수 있는 초대. 아빠와 동생을 같이 부르는 것은 되고, 무한 발급은 막습니다.
MAX_ACTIVE_INVITES = 3

#: 초대 유효 기간.
INVITE_TTL = timedelta(hours=24)


class InviteNotFoundError(Exception):
    """없는 토큰. **이미 쓴 초대도 이것입니다** — 수락이 행을 지우기 때문입니다."""


class InviteExpiredError(Exception):
    """만료됐거나, 그새 대표가 바뀌어 무효가 됐습니다."""


class AlreadyOwnerError(Exception):
    """대표가 자기 초대를 수락했습니다. 트리거 ② 가 DB 에서도 막지만 여기서 먼저 거절합니다."""


class MemberLimitError(Exception):
    """구성원 상한."""


class PetLimitError(Exception):
    """수락자의 미니룸 상한."""


class InviteLimitError(Exception):
    """유효 초대 상한."""


async def create_invite(
    session: AsyncSession, app_user_id: uuid.UUID, pet_id: uuid.UUID
) -> tuple[PetInvite, str]:
    """**대표만.** 평문 토큰은 이 반환값에만 있고 DB 에는 해시만 남습니다.

    :returns: (초대 행, 평문 토큰)
    """
    pet = await pet_repo.get_owned(session, app_user_id, pet_id)
    if pet is None:
        raise PetNotFoundError

    now = datetime.now(UTC)
    # 아무도 안 누른 만료건이 영원히 쌓이는 것을 여기서 막습니다.
    await member_repo.delete_expired_invites(session, pet_id, now)

    if await member_repo.count_valid_invites(session, pet_id, now) >= MAX_ACTIVE_INVITES:
        raise InviteLimitError

    token = generate_refresh_token()
    invite = member_repo.add_invite(
        session,
        pet_id=pet_id,
        invited_by=app_user_id,
        token_hash=hash_refresh_token(token),
        expires_at=now + INVITE_TTL,
    )
    await session.commit()
    return invite, token


async def accept_invite(session: AsyncSession, app_user_id: uuid.UUID, token: str) -> Pet:
    """초대 수락. 검증 순서는 docs/co-care.md §3 의 표와 같습니다."""
    invite = await member_repo.get_invite_by_hash(session, hash_refresh_token(token))
    if invite is None:
        raise InviteNotFoundError

    # ⚠️ pets 행을 잠급니다. 동시 수락이 상한을 넘기지 못하게 하고, 아래 3번 검사가
    #    동시 승계와 경쟁하지 않게 합니다.
    pet = await pet_repo.get_by_id_for_update(session, invite.pet_id)
    if pet is None:  # pragma: no cover — FK 가 CASCADE 라 강아지 없이 초대만 남을 수 없습니다
        raise InviteNotFoundError

    now = datetime.now(UTC)
    if invite.expires_at <= now:
        await member_repo.delete_invite(session, invite.id)
        await session.commit()
        raise InviteExpiredError

    if invite.invited_by != pet.app_user_id:
        # 그새 대표가 바뀌었습니다. 옛 대표가 뿌린 링크는 죽습니다.
        raise InviteExpiredError

    if pet.app_user_id == app_user_id:
        raise AlreadyOwnerError

    if await member_repo.is_member(session, pet.id, app_user_id):
        # 멱등입니다 — 카톡 링크는 두 번 눌립니다. 초대는 그대로 두어야 첫 수락의
        # 삭제와 경쟁하지 않습니다.
        return pet

    if await member_repo.count_members(session, pet.id) >= MAX_MEMBERS_PER_PET:
        raise MemberLimitError

    if await pet_repo.count_accessible(session, app_user_id) >= MAX_PETS_PER_USER:
        raise PetLimitError

    member_repo.add(session, pet.id, app_user_id)
    await member_repo.delete_invite(session, invite.id)

    # 등록한 강아지가 없던 사람은 첫 수락에서 대표 강아지를 얻습니다 —
    # 없으면 앱 첫 화면이 빕니다 (`services/pet.py` 의 등록 경로와 같은 규칙).
    user = await app_user_repo.get_by_id(session, app_user_id)
    if user is not None and user.primary_pet_id is None:
        user.primary_pet_id = pet.id

    await session.commit()
    log.info("공동 돌봄 참여 (pet=%s, user=%s)", pet.id, app_user_id)
    return pet
```

`pet_repo.get_by_id_for_update` 가 없으므로 `repositories/pet.py` 에 더한다:

```python
async def get_by_id_for_update(session: AsyncSession, pet_id: uuid.UUID) -> Pet | None:
    """소유자를 안 보고 잠급니다. **수락처럼 "아직 권한이 없는" 경로가 씁니다** —
    권한 판단은 부르는 쪽(services)이 하고, 여기서는 행을 잡는 것만 합니다."""
    stmt = select(Pet).where(Pet.id == pet_id).with_for_update()
    return await session.scalar(stmt)
```

`__all__` 에도 넣는다.

- [ ] **Step 5: 라우터를 쓴다**

`backend/src/daengs_backend/routers/pet_member.py`:

```python
"""공동 돌봄 HTTP 경계 — 서비스의 예외를 상태 코드로 바꿉니다 (docs/co-care.md §3).

판단은 여기 없습니다. 라우터가 `CurrentAppUser` 로 잠겨 있습니다.
"""

import uuid
from typing import Annotated

from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.ext.asyncio import AsyncSession

from daengs_backend.core.database import get_session
from daengs_backend.core.deps import CurrentAppUser
from daengs_backend.schemas.pet_member import InviteAccept, InviteCreated
from daengs_backend.services import pet_member as member_service
from daengs_backend.services.pet import PetNotFoundError

router = APIRouter(prefix="/app", tags=["pet-members"])

Session = Annotated[AsyncSession, Depends(get_session)]

_PET_NOT_FOUND = "강아지를 찾을 수 없습니다."
_INVITE_NOT_FOUND = "초대를 찾을 수 없습니다."
_INVITE_EXPIRED = "만료된 초대입니다. 새 초대를 요청하세요."


@router.post(
    "/pets/{pet_id}/invites",
    response_model=InviteCreated,
    status_code=status.HTTP_201_CREATED,
)
async def create_invite(pet_id: uuid.UUID, user: CurrentAppUser, session: Session) -> InviteCreated:
    """초대 링크 발급. **대표만.** 평문 토큰은 이 응답에만 있습니다.

    내 강아지가 아니면 404 입니다 — 돌보미도 404 라 "대표가 아니다" 가 안 샙니다.
    """
    try:
        invite, token = await member_service.create_invite(session, user.app_user_id, pet_id)
    except PetNotFoundError:
        raise HTTPException(status.HTTP_404_NOT_FOUND, _PET_NOT_FOUND) from None
    except member_service.InviteLimitError:
        raise HTTPException(
            status.HTTP_409_CONFLICT,
            f"살아 있는 초대는 {member_service.MAX_ACTIVE_INVITES}개까지입니다.",
        ) from None
    return InviteCreated(
        id=invite.id, pet_id=invite.pet_id, token=token, expires_at=invite.expires_at
    )


@router.post("/pet-invites/accept", status_code=status.HTTP_200_OK)
async def accept_invite(body: InviteAccept, user: CurrentAppUser, session: Session) -> dict:
    """초대 수락. **같은 링크를 두 번 눌러도 200 입니다.**

    경로에 `pet_id` 가 없는 것이 의도입니다 — 수락 전에는 그 강아지에 아무 권한이 없습니다.
    """
    try:
        pet = await member_service.accept_invite(session, user.app_user_id, body.token)
    except member_service.InviteNotFoundError:
        raise HTTPException(status.HTTP_404_NOT_FOUND, _INVITE_NOT_FOUND) from None
    except member_service.InviteExpiredError:
        raise HTTPException(status.HTTP_410_GONE, _INVITE_EXPIRED) from None
    except member_service.AlreadyOwnerError:
        raise HTTPException(status.HTTP_409_CONFLICT, "이미 이 아이의 대표입니다.") from None
    except member_service.MemberLimitError:
        raise HTTPException(
            status.HTTP_409_CONFLICT,
            f"한 아이의 보호자는 {member_service.MAX_MEMBERS_PER_PET}명까지입니다.",
        ) from None
    except member_service.PetLimitError:
        raise HTTPException(
            status.HTTP_409_CONFLICT, "돌보는 아이가 너무 많습니다."
        ) from None
    return {"pet_id": str(pet.id), "name": pet.name}


__all__ = ["router"]
```

- [ ] **Step 6: 등록한다**

`backend/src/daengs_backend/main.py` 에서 `pet` 라우터 등록(181행 근처) **다음 줄**에 넣는다. import 도 같은 블록에 더한다.

```python
app.include_router(pet_member.router)
```

- [ ] **Step 7: 가짜에 초대·구성원 리포지토리를 더한다**

`fakes.py` 의 `install` 에 `member_repo` 함수들의 대역을 만들고 `monkeypatch.setattr` 로 건다. `FakeInvite` 는 `id · pet_id · invited_by · token_hash · expires_at` 을 가진 dataclass 로 `fakes.py` 에 만든다.

- [ ] **Step 8: 테스트가 통과하는지 본다**

Run: `cd backend && uv run pytest tests/test_pet_members.py -v`
Expected: 전부 PASS.

- [ ] **Step 9: 커밋**

```bash
git add backend/src/daengs_backend/ backend/tests/
git commit -m "feat: 공동 돌봄 초대와 수락 — 링크 토큰, 일회용, 멱등

토큰은 core/token.py 의 짝을 그대로 쓰고 평문은 저장하지 않는다. 수락 경로에
pet_id 가 없는 것은 수락 전에 그 강아지에 권한이 없기 때문이다."
```

---

## Task 6: 구성원 목록 · 퇴장 · 내보내기

**Files:**
- Modify: `backend/src/daengs_backend/services/pet_member.py`, `backend/src/daengs_backend/routers/pet_member.py`
- Test: `backend/tests/test_pet_members.py`

**Interfaces:**
- Produces:
  - `services.pet_member.list_members(session, app_user_id, pet_id) -> list[MemberOut]`
  - `services.pet_member.remove_member(session, app_user_id, pet_id, target_id) -> None`
  - `services.pet_member.actor_label(session, pet_id, app_user_id) -> str | None` — Task 10 이 쓴다
  - 예외: `CannotRemoveOwnerError`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
async def test_owner_can_remove_carer(store: Store, pet: FakePet):
    store.pet_members.append((pet.id, CARER))
    r = client_as(OWNER).delete(f"/app/pets/{pet.id}/members/{CARER}")
    assert r.status_code == 204
    assert (pet.id, CARER) not in store.pet_members


async def test_carer_can_leave(store: Store, pet: FakePet):
    store.pet_members.append((pet.id, CARER))
    assert client_as(CARER).delete(f"/app/pets/{pet.id}/members/{CARER}").status_code == 204


async def test_carer_cannot_remove_another_carer(store: Store, pet: FakePet):
    other = uuid.uuid4()
    store.pet_members.extend([(pet.id, CARER), (pet.id, other)])
    assert client_as(CARER).delete(f"/app/pets/{pet.id}/members/{other}").status_code == 403


async def test_owner_cannot_remove_self(store: Store, pet: FakePet):
    """대표는 승계 엔드포인트로 가야 한다."""
    assert client_as(OWNER).delete(f"/app/pets/{pet.id}/members/{OWNER}").status_code == 409


async def test_leaving_clears_primary_pet(store: Store, pet: FakePet):
    """접근 못 하는 강아지를 primary 로 가리키면 앱 첫 화면이 깨진다."""
    user = store.app_users[CARER_KAKAO]
    store.pet_members.append((pet.id, CARER))
    user.primary_pet_id = pet.id
    mine = FakePet(app_user_id=CARER, name="네오", breed="푸들")
    store.pets.append(mine)

    client_as(CARER).delete(f"/app/pets/{pet.id}/members/{CARER}")
    assert user.primary_pet_id == mine.id


async def test_leaving_last_pet_nulls_primary(store: Store, pet: FakePet):
    user = store.app_users[CARER_KAKAO]
    store.pet_members.append((pet.id, CARER))
    user.primary_pet_id = pet.id
    client_as(CARER).delete(f"/app/pets/{pet.id}/members/{CARER}")
    assert user.primary_pet_id is None


async def test_member_list_shows_owner_and_carer(store: Store, pet: FakePet):
    store.pet_members.append((pet.id, CARER))
    got = client_as(CARER).get(f"/app/pets/{pet.id}/members").json()
    assert [m["is_owner"] for m in got["members"]] == [True, False]
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd backend && uv run pytest tests/test_pet_members.py -v -k "remove or leave or member_list"`
Expected: FAIL — 404 (라우트가 없다).

- [ ] **Step 3: 서비스에 셋을 더한다**

```python
class CannotRemoveOwnerError(Exception):
    """대표는 이 경로로 못 나갑니다. 승계 엔드포인트로 가야 합니다."""


class NotAllowedError(Exception):
    """남을 내보낼 수 있는 것은 대표뿐입니다."""


async def list_members(
    session: AsyncSession, app_user_id: uuid.UUID, pet_id: uuid.UUID
) -> list[MemberOut]:
    """대표를 맨 앞에, 그다음 돌보미를 참여 순으로. **구성원만 볼 수 있습니다.**"""
    pet = await pet_repo.get_accessible(session, app_user_id, pet_id)
    if pet is None:
        raise PetNotFoundError

    carer_ids = await member_repo.list_members(session, pet_id)
    names = await app_user_repo.nicknames_by_ids(session, [pet.app_user_id, *carer_ids])
    out = [
        MemberOut(
            app_user_id=pet.app_user_id,
            nickname=names.get(pet.app_user_id),
            is_owner=True,
            joined_at=None,
        )
    ]
    out += [
        MemberOut(app_user_id=cid, nickname=names.get(cid), is_owner=False, joined_at=None)
        for cid in carer_ids
    ]
    return out


async def remove_member(
    session: AsyncSession,
    app_user_id: uuid.UUID,
    pet_id: uuid.UUID,
    target_id: uuid.UUID,
) -> None:
    """내보내기(대표) 또는 나가기(본인). 같은 경로입니다."""
    pet = await pet_repo.get_accessible(session, app_user_id, pet_id)
    if pet is None:
        raise PetNotFoundError
    if target_id == pet.app_user_id:
        raise CannotRemoveOwnerError
    if app_user_id != pet.app_user_id and app_user_id != target_id:
        raise NotAllowedError

    await member_repo.remove(session, pet_id, target_id)

    # ⚠️ `primary_pet_id` 의 FK 는 ON DELETE SET NULL 이지만 **강아지 행은 안 지워지므로
    #    안 돕니다.** 여기서 명시로 비웁니다 — 안 그러면 접근 못 하는 아이를 가리킵니다.
    user = await app_user_repo.get_by_id(session, target_id)
    if user is not None and user.primary_pet_id == pet_id:
        remaining = await pet_repo.list_accessible(session, target_id)
        user.primary_pet_id = remaining[0].id if remaining else None

    await session.commit()


async def actor_label(
    session: AsyncSession, pet_id: uuid.UUID, app_user_id: uuid.UUID | None
) -> str | None:
    """케어 기록에 이름을 낼지 정합니다 — **지금도 구성원일 때만** 냅니다.

    탈퇴자는 트리거가 `pet_members` 에서 지우므로 자동으로 비구성원이 되고, **재가입해도**
    다시 초대받기 전엔 이름이 안 납니다. 이 규칙이 없으면 옛 기록이 어느 날 갑자기 남의
    현재 닉네임으로 뜹니다 (docs/co-care.md §3).
    """
    if app_user_id is None:
        return None
    if not await member_repo.is_member(session, pet_id, app_user_id):
        return None
    names = await app_user_repo.nicknames_by_ids(session, [app_user_id])
    return names.get(app_user_id)
```

`pet_repo.list_accessible` 과 `app_user_repo.nicknames_by_ids` 가 없으면 만든다.

```python
# repositories/pet.py
async def list_accessible(session: AsyncSession, app_user_id: uuid.UUID) -> list[Pet]:
    """구성원인 강아지 전부, 등록 순서대로. `list_for_owner` 와 같은 정렬입니다."""
    stmt = select(Pet).where(_is_member(app_user_id)).order_by(Pet.created_at, Pet.id)
    return list(await session.scalars(stmt))
```

```python
# repositories/app_user.py
async def nicknames_by_ids(
    session: AsyncSession, app_user_ids: Sequence[uuid.UUID]
) -> dict[uuid.UUID, str | None]:
    """id → 닉네임. **암호화 컬럼이 아니라 평문 nickname 이라 그대로 읽습니다.**"""
    if not app_user_ids:
        return {}
    stmt = select(AppUser.id, AppUser.nickname).where(AppUser.id.in_(list(app_user_ids)))
    return {row.id: row.nickname for row in await session.execute(stmt)}
```

- [ ] **Step 4: 라우터에 둘을 더한다**

```python
@router.get("/pets/{pet_id}/members", response_model=MemberListResponse)
async def list_members(pet_id: uuid.UUID, user: CurrentAppUser, session: Session) -> MemberListResponse:
    """구성원 목록. 대표가 맨 앞입니다. **구성원만 볼 수 있습니다.**"""
    try:
        members = await member_service.list_members(session, user.app_user_id, pet_id)
    except PetNotFoundError:
        raise HTTPException(status.HTTP_404_NOT_FOUND, _PET_NOT_FOUND) from None
    return MemberListResponse(pet_id=pet_id, members=members)


@router.delete("/pets/{pet_id}/members/{target_id}", status_code=status.HTTP_204_NO_CONTENT)
async def remove_member(
    pet_id: uuid.UUID, target_id: uuid.UUID, user: CurrentAppUser, session: Session
) -> None:
    """내보내기(대표) 또는 나가기(본인). **대표는 자기를 못 뺍니다** — 승계로 가야 합니다."""
    try:
        await member_service.remove_member(session, user.app_user_id, pet_id, target_id)
    except PetNotFoundError:
        raise HTTPException(status.HTTP_404_NOT_FOUND, _PET_NOT_FOUND) from None
    except member_service.CannotRemoveOwnerError:
        raise HTTPException(
            status.HTTP_409_CONFLICT,
            "대표는 이 방법으로 나갈 수 없습니다. 다른 보호자에게 대표를 넘기세요.",
        ) from None
    except member_service.NotAllowedError:
        raise HTTPException(
            status.HTTP_403_FORBIDDEN, "다른 보호자를 내보낼 수 있는 것은 대표뿐입니다."
        ) from None
```

- [ ] **Step 5: 테스트가 통과하는지 본다**

Run: `cd backend && uv run pytest tests/test_pet_members.py -v`
Expected: 전부 PASS.

- [ ] **Step 6: 커밋**

```bash
git add backend/src/daengs_backend/ backend/tests/
git commit -m "feat: 구성원 목록과 퇴장·내보내기 — primary_pet_id 도 같이 정리

FK 의 SET NULL 은 강아지 행이 안 지워져서 안 돈다. 나간 사람이 접근 못 하는
아이를 대표로 가리키면 앱 첫 화면이 깨지므로 명시로 비운다."
```

---

## Task 7: 승계

**Files:**
- Modify: `backend/src/daengs_backend/services/pet_member.py`, `backend/src/daengs_backend/routers/pet_member.py`
- Test: `backend/tests/test_pet_members.py`

**Interfaces:**
- Produces: `services.pet_member.transfer_owner(session, app_user_id, pet_id, new_owner_id) -> Pet`, 예외 `NotAMemberError`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
async def test_transfer_swaps_roles(store: Store, pet: FakePet):
    store.pet_members.append((pet.id, CARER))
    r = client_as(OWNER).post(f"/app/pets/{pet.id}/owner", json={"app_user_id": str(CARER)})
    assert r.status_code == 200
    assert pet.app_user_id == CARER
    assert (pet.id, CARER) not in store.pet_members   # 새 대표는 돌보미가 아니다
    assert (pet.id, OWNER) in store.pet_members       # 옛 대표는 돌보미로 남는다


async def test_transfer_keeps_old_owner_primary_pet(store: Store, pet: FakePet):
    """옛 대표는 돌보미로 남아 계속 접근한다 — primary 를 건드리면 안 된다."""
    user = store.app_users[OWNER_KAKAO]
    user.primary_pet_id = pet.id
    store.pet_members.append((pet.id, CARER))
    client_as(OWNER).post(f"/app/pets/{pet.id}/owner", json={"app_user_id": str(CARER)})
    assert user.primary_pet_id == pet.id


async def test_transfer_requires_membership(store: Store, pet: FakePet):
    r = client_as(OWNER).post(f"/app/pets/{pet.id}/owner", json={"app_user_id": str(STRANGER)})
    assert r.status_code == 409


async def test_transfer_respects_new_owner_limit(store: Store, pet: FakePet):
    """새 대표가 이미 5마리면 6마리가 된다."""
    store.pet_members.append((pet.id, CARER))
    for i in range(4):
        store.pets.append(FakePet(app_user_id=CARER, name=f"강아지{i}", breed="믹스"))
    r = client_as(OWNER).post(f"/app/pets/{pet.id}/owner", json={"app_user_id": str(CARER)})
    assert r.status_code == 409


async def test_transfer_kills_old_invites(store: Store, pet: FakePet):
    store.pet_members.append((pet.id, CARER))
    _invite(store, pet)
    client_as(OWNER).post(f"/app/pets/{pet.id}/owner", json={"app_user_id": str(CARER)})
    assert store.pet_invites == []


async def test_carer_cannot_transfer(store: Store, pet: FakePet):
    store.pet_members.append((pet.id, CARER))
    r = client_as(CARER).post(f"/app/pets/{pet.id}/owner", json={"app_user_id": str(CARER)})
    assert r.status_code == 404
```

⚠️ `test_transfer_respects_new_owner_limit` 에서 CARER 의 강아지는 4마리다 — 맥스까지 세면 `count_accessible` 이 5 이고, 승계해도 여전히 5 다. **상한 검사는 "승계 후에도 방에 서는 아이 수가 안 늘어난다"** 는 사실을 이용하면 통과해 버린다. 그래서 검사 대상은 `count_accessible` 이 아니라 **`count_for_owner(new_owner) >= MAX_PETS_PER_USER`** 여야 한다 — 승계는 소유를 늘리는 일이기 때문이다. 테스트는 CARER 가 **자기 소유 5마리**를 갖도록 고쳐 쓴다:

```python
    for i in range(5):
        store.pets.append(FakePet(app_user_id=CARER, name=f"강아지{i}", breed="믹스"))
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd backend && uv run pytest tests/test_pet_members.py -v -k transfer`
Expected: FAIL — 404.

- [ ] **Step 3: 서비스에 승계를 더한다**

```python
class NotAMemberError(Exception):
    """승계 대상이 돌보미가 아닙니다. 승계와 초대를 한 번에 하지 않습니다."""


async def transfer_owner(
    session: AsyncSession,
    app_user_id: uuid.UUID,
    pet_id: uuid.UUID,
    new_owner_id: uuid.UUID,
) -> Pet:
    """대표를 넘깁니다. **대표만.**

    ⚠️ **순서가 중요합니다.** 옛 대표를 `pet_members` 에 먼저 넣으면 `pet_members_not_owner`
    트리거가 터집니다 — 그 순간 옛 대표는 아직 `pets.app_user_id` 입니다.
    """
    pet = await pet_repo.get_owned(session, app_user_id, pet_id, for_update=True)
    if pet is None:
        raise PetNotFoundError
    if not await member_repo.is_member(session, pet_id, new_owner_id):
        raise NotAMemberError

    # 승계는 새 대표의 **소유**를 늘립니다. 미니룸 상한은 소유 기준이 아니지만
    # (`count_accessible`), 이 검사만은 소유로 봅니다 — 방에 선 아이 수는 안 변하고
    # 늘어나는 것은 소유뿐이기 때문입니다.
    if await pet_repo.count_for_owner(session, new_owner_id) >= MAX_PETS_PER_USER:
        raise PetLimitError

    pet.app_user_id = new_owner_id                              # ① 먼저
    await member_repo.remove(session, pet_id, new_owner_id)     # ②
    member_repo.add(session, pet_id, app_user_id)               # ③ 이제 안전
    await member_repo.delete_invites_for_pet(session, pet_id)   # ④ 옛 링크를 죽인다

    # 옛 대표의 primary_pet_id 는 건드리지 않습니다 — 돌보미로 남아 계속 접근합니다.
    await session.commit()
    log.info("대표 승계 (pet=%s, %s → %s)", pet_id, app_user_id, new_owner_id)
    return pet
```

- [ ] **Step 4: 라우터에 더한다**

```python
@router.post("/pets/{pet_id}/owner", status_code=status.HTTP_200_OK)
async def transfer_owner(
    pet_id: uuid.UUID, body: OwnerTransfer, user: CurrentAppUser, session: Session
) -> dict:
    """대표를 넘깁니다. 대상은 **이미 돌보미여야** 합니다."""
    try:
        pet = await member_service.transfer_owner(
            session, user.app_user_id, pet_id, body.app_user_id
        )
    except PetNotFoundError:
        raise HTTPException(status.HTTP_404_NOT_FOUND, _PET_NOT_FOUND) from None
    except member_service.NotAMemberError:
        raise HTTPException(
            status.HTTP_409_CONFLICT, "먼저 초대해서 보호자로 참여시키세요."
        ) from None
    except member_service.PetLimitError:
        raise HTTPException(
            status.HTTP_409_CONFLICT, "넘겨받는 분이 등록할 수 있는 마릿수를 넘습니다."
        ) from None
    return {"pet_id": str(pet.id), "owner_app_user_id": str(pet.app_user_id)}
```

- [ ] **Step 5: 테스트가 통과하는지 본다**

Run: `cd backend && uv run pytest tests/test_pet_members.py -v`
Expected: 전부 PASS.

- [ ] **Step 6: 커밋**

```bash
git add backend/src/daengs_backend/ backend/tests/
git commit -m "feat: 대표 승계 — pets 를 먼저 바꾸고 옛 대표를 돌보미로

순서를 뒤집으면 상호배제 트리거가 터진다. 옛 대표는 계속 접근하므로
primary_pet_id 는 건드리지 않는다."
```

---

## Task 8: 탈퇴 가드

**Files:**
- Modify: `backend/src/daengs_backend/services/pet.py` (`delete_all_for_owner`)
- Modify: `backend/src/daengs_backend/services/app_auth.py`, `backend/src/daengs_backend/routers/app_auth.py:297`
- Test: `backend/tests/test_app_auth.py`

**Interfaces:**
- Produces: `services.pet.OwnerHasCarersError(pet_names: list[str])`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`backend/tests/test_app_auth.py` 에 더한다.

```python
async def test_withdraw_blocked_while_carers_remain(store, client):
    """돌보미가 남아 있으면 탈퇴가 강아지를 날린다. 서버가 막는다 — 앱 UX 만으로는 부족하다."""
    pet = FakePet(app_user_id=OWNER, name="맥스", breed="믹스")
    store.pets.append(pet)
    store.pet_members.append((pet.id, CARER))

    r = client_as(OWNER).post("/app/auth/withdraw")
    assert r.status_code == 409
    assert "맥스" in r.json()["detail"]
    assert pet in store.pets, "강아지가 지워졌다"


async def test_withdraw_message_offers_both_exits(store, client):
    """탈퇴를 영구히 막는 모양이 되면 안 된다 — 두 출구가 메시지에 있어야 한다."""
    pet = FakePet(app_user_id=OWNER, name="맥스", breed="믹스")
    store.pets.append(pet)
    store.pet_members.append((pet.id, CARER))

    detail = client_as(OWNER).post("/app/auth/withdraw").json()["detail"]
    assert "대표" in detail and "내보" in detail


async def test_carer_can_withdraw_freely(store, client):
    """돌보미로만 참여 중인 사람은 그냥 나간다. 트리거가 정리한다."""
    pet = FakePet(app_user_id=OWNER, name="맥스", breed="믹스")
    store.pets.append(pet)
    store.pet_members.append((pet.id, CARER))
    assert client_as(CARER).post("/app/auth/withdraw").status_code == 204


async def test_withdraw_passes_after_removing_carer(store, client):
    pet = FakePet(app_user_id=OWNER, name="맥스", breed="믹스")
    store.pets.append(pet)
    store.pet_members.append((pet.id, CARER))
    client_as(OWNER).delete(f"/app/pets/{pet.id}/members/{CARER}")
    assert client_as(OWNER).post("/app/auth/withdraw").status_code == 204
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd backend && uv run pytest tests/test_app_auth.py -v -k withdraw`
Expected: FAIL — 204 가 오고 강아지가 지워진다.

- [ ] **Step 3: `services/pet.py` 의 `delete_all_for_owner` 안쪽에 가드를 넣는다**

락이 이미 있다. **그 안쪽에** 넣는 것이 핵심이다 — 밖에서 검사하면 검사와 삭제 사이에 돌보미가 들어와 강아지가 날아간다.

```python
class OwnerHasCarersError(Exception):
    """돌보미가 남은 강아지의 대표는 그냥 탈퇴할 수 없습니다.

    막는 것이 목적이 아니라 **순서를 요구하는 것**입니다 — 승계하거나 내보내면 바로 됩니다.
    """

    def __init__(self, pet_names: list[str]) -> None:
        self.pet_names = pet_names
        super().__init__(", ".join(pet_names))
```

```python
async def delete_all_for_owner(session: AsyncSession, app_user_id: uuid.UUID) -> int:
    """탈퇴용 bulk delete. 단일 삭제와 같은 cleanup 경로를 씁니다.

    ⚠️ **돌보미가 남은 아이가 하나라도 있으면 거절합니다.** 검사는 아래 `for_update` **안쪽**
    이라야 합니다 — 검사와 삭제 사이에 수락이 끼면 남의 강아지를 날립니다. 수락 쪽도 같은
    `pets` 행을 잠그므로 둘이 줄을 섭니다 (docs/co-care.md §3).
    """
    from daengs_backend.services.activity_game import acquire

    await acquire(session)
    pets = await pet_repo.list_for_owner_for_update(session, app_user_id)

    shared = [p.name for p in pets if await member_repo.list_members(session, p.id)]
    if shared:
        raise OwnerHasCarersError(shared)

    await gait_service.cleanup_for_pets(session, [pet.id for pet in pets])
    await cleanup_photos_for_pets(session, pets)
    return await pet_repo.delete_all_for_owner(session, app_user_id)
```

- [ ] **Step 4: 라우터가 409 로 바꾸게 한다**

`routers/app_auth.py:297` 의 `withdraw` 를 감싼다.

```python
    try:
        await app_auth_service.withdraw(session, app_user_id=user.app_user_id)
    except pet_service.OwnerHasCarersError as exc:
        names = ", ".join(exc.pet_names)
        raise HTTPException(
            status.HTTP_409_CONFLICT,
            f"{names}의 공동 보호자가 남아 있습니다. 대표를 넘기거나 보호자를 "
            f"내보낸 뒤에 탈퇴할 수 있습니다.",
        ) from None
```

`services/app_auth.py` 의 `withdraw` 는 `except Exception: rollback; raise` 를 이미 갖고 있어 예외가 그대로 올라온다 — **손댈 필요가 없다.**

- [ ] **Step 5: 테스트가 통과하는지 본다**

Run: `cd backend && uv run pytest tests/test_app_auth.py tests/test_app_user_pii.py -v`
Expected: PASS.

- [ ] **Step 6: 커밋**

```bash
git add backend/src/daengs_backend/ backend/tests/
git commit -m "feat: 대표 탈퇴 가드 — 돌보미가 남으면 409, 두 출구를 안내

앱 UX 만으로는 구버전 앱과 직접 호출에서 뚫린다. 검사는 pets 락 안쪽이라
수락과 줄을 서고, 검사-삭제 사이에 돌보미가 들어오는 창이 없다."
```

---

## Task 9: 약 중복 확인

**Files:**
- Modify: `backend/src/daengs_backend/services/care_event.py`, `backend/src/daengs_backend/schemas/care_event.py`, `backend/src/daengs_backend/routers/care_event.py`
- Modify: `backend/src/daengs_backend/repositories/care_event.py`
- Test: `backend/tests/test_care_events.py`

**Interfaces:**
- Produces: `services.care_event.MedicationConflictError(conflicts: list[CareEvent])`, `CareEventCreate.confirm: bool`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
def _med(pet, when, confirm=False, key=None):
    return {
        "pet_id": str(pet.id), "kind": "medication", "occurred_at": when,
        "client_event_id": str(key or uuid.uuid4()), "confirm": confirm,
    }


async def test_second_medication_within_window_is_409(store, pet):
    client_as(OWNER).post("/app/care-events", json=_med(pet, "2026-09-09T08:15:00+09:00"))
    r = client_as(CARER_OR_OWNER).post(
        "/app/care-events", json=_med(pet, "2026-09-09T10:00:00+09:00")
    )
    assert r.status_code == 409
    body = r.json()["detail"]
    assert "conflicts" in body and len(body["conflicts"]) == 1


async def test_confirm_true_records_anyway(store, pet):
    client_as(OWNER).post("/app/care-events", json=_med(pet, "2026-09-09T08:15:00+09:00"))
    r = client_as(OWNER).post(
        "/app/care-events", json=_med(pet, "2026-09-09T10:00:00+09:00", confirm=True)
    )
    assert r.status_code == 201


async def test_twelve_hours_apart_is_fine(store, pet):
    client_as(OWNER).post("/app/care-events", json=_med(pet, "2026-09-09T08:00:00+09:00"))
    r = client_as(OWNER).post("/app/care-events", json=_med(pet, "2026-09-09T20:00:00+09:00"))
    assert r.status_code == 201


async def test_meal_is_never_blocked(store, pet):
    body = {
        "pet_id": str(pet.id), "kind": "meal", "occurred_at": "2026-09-09T08:00:00+09:00",
        "client_event_id": str(uuid.uuid4()),
    }
    client_as(OWNER).post("/app/care-events", json=body)
    body["client_event_id"] = str(uuid.uuid4())
    body["occurred_at"] = "2026-09-09T09:00:00+09:00"
    assert client_as(OWNER).post("/app/care-events", json=body).status_code == 201


async def test_idempotency_wins_over_conflict_check(store, pet):
    """확인하고 기록한 직후 재시도가 409 가 되면 앱은 올라갔는지 모르게 된다.

    이 테스트가 services/care_event.py 의 검사 **순서**를 지킨다 — 멱등이 먼저다.
    """
    key = uuid.uuid4()
    client_as(OWNER).post("/app/care-events", json=_med(pet, "2026-09-09T08:15:00+09:00"))
    first = client_as(OWNER).post(
        "/app/care-events", json=_med(pet, "2026-09-09T10:00:00+09:00", confirm=True, key=key)
    )
    assert first.status_code == 201
    retry = client_as(OWNER).post(
        "/app/care-events", json=_med(pet, "2026-09-09T10:00:00+09:00", confirm=True, key=key)
    )
    assert retry.status_code == 200, "재시도가 409 가 되면 멱등이 깨진 것이다"
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd backend && uv run pytest tests/test_care_events.py -v -k "medication or confirm or idempotency_wins or meal_is_never"`
Expected: FAIL — 전부 201 이 온다.

- [ ] **Step 3: 스키마에 `confirm` 을 더한다**

`CareEventCreate` 끝에:

```python
    #: 중복 경고를 이미 보고 "그래도 기록" 을 누른 요청. **쿼리가 아니라 body 인 것이 의도**
    #: 입니다 — 재시도가 같은 body 를 그대로 다시 보내면 됩니다.
    #:
    #: ⚠️ 앱은 이때 **`client_event_id` 를 그대로 둡니다.** 새 키를 만들면 재시도가 두 줄이
    #: 됩니다 (docs/co-care.md §4).
    confirm: bool = False
```

- [ ] **Step 4: 리포지토리에 창 조회를 더한다**

```python
async def list_kind_between(
    session: AsyncSession,
    pet_id: uuid.UUID,
    kind: str,
    start: datetime,
    end: datetime,
) -> list[CareEvent]:
    """그 아이의 같은 종류 기록, 창 안에서 최근 먼저. 중복 확인이 씁니다."""
    stmt = (
        select(CareEvent)
        .where(
            CareEvent.pet_id == pet_id,
            CareEvent.kind == kind,
            CareEvent.occurred_at >= start,
            CareEvent.occurred_at <= end,
        )
        .order_by(CareEvent.occurred_at.desc())
    )
    return list(await session.scalars(stmt))
```

- [ ] **Step 5: 서비스에 상수·예외·검사를 넣는다**

상수 둘을 `DAY_TIMEZONE` 옆에 놓는다 (본문은 docs/co-care.md §4 와 같다). 예외를 더한다:

```python
class MedicationConflictError(Exception):
    """창 안에 이미 같은 종류가 있습니다. `confirm=True` 로 다시 보내면 기록됩니다."""

    def __init__(self, conflicts: list[CareEvent]) -> None:
        self.conflicts = conflicts
        super().__init__(f"{len(conflicts)}건")
```

`record()` 의 멱등 조회 **다음**, INSERT **앞**에 넣는다:

```python
    # ⚠️ 이 검사는 멱등 조회보다 **뒤**여야 합니다. 앞으로 옮기면, 확인하고 기록한 직후의
    #    재시도가 방금 자기가 만든 행을 중복으로 보고 409 를 냅니다 — 앱은 올라갔는지
    #    모르게 됩니다 (test_idempotency_wins_over_conflict_check).
    if body.kind in CONFIRM_KINDS and not body.confirm:
        window = MEDICATION_CONFIRM_WINDOW
        conflicts = await care_repo.list_kind_between(
            session,
            pet.id,
            body.kind,
            body.occurred_at - window,
            body.occurred_at + window,
        )
        if conflicts:
            raise MedicationConflictError(conflicts)
```

- [ ] **Step 6: 라우터가 409 를 dict detail 로 낸다**

```python
    except care_service.MedicationConflictError as exc:
        latest = exc.conflicts[0]
        raise HTTPException(
            status.HTTP_409_CONFLICT,
            {
                "message": "이미 약을 챙긴 기록이 있습니다.",
                "conflicts": [
                    {
                        "id": str(e.id),
                        "occurred_at": e.occurred_at.isoformat(),
                        "note": e.note,
                        "actor": {
                            "app_user_id": str(e.actor_app_user_id)
                            if e.actor_app_user_id
                            else None,
                            "nickname": await member_service.actor_label(
                                session, e.pet_id, e.actor_app_user_id
                            ),
                        },
                    }
                    for e in exc.conflicts
                ],
            },
        ) from None
```

⚠️ `HTTPException(detail=...)` 에 dict 를 넣는 것은 **이 저장소에서 처음**이다. 라우터 docstring 에 그 사실과 이유를 적는다. `await` 를 리스트 컴프리헨션 안에서 쓰므로, 실제로는 컴프리헨션 앞에서 `labels = {e.id: await member_service.actor_label(...) for e in exc.conflicts}` 를 먼저 만들고 그것을 쓴다.

- [ ] **Step 7: 테스트가 통과하는지 본다**

Run: `cd backend && uv run pytest tests/test_care_events.py -v`
Expected: 전부 PASS.

- [ ] **Step 8: 커밋**

```bash
git add backend/src/daengs_backend/ backend/tests/
git commit -m "feat: 약 중복 확인 — 6시간 창 안이면 409, confirm 으로 통과

멱등 조회가 중복 검사보다 먼저다. 뒤집으면 확인 직후의 재시도가 409 가 되어
앱이 올라갔는지 모르게 된다."
```

---

## Task 10: `actor` 를 케어 로그 응답에 싣기

**Files:**
- Modify: `backend/src/daengs_backend/schemas/care_event.py`, `backend/src/daengs_backend/routers/care_event.py`, `backend/src/daengs_backend/services/care_event.py`
- Test: `backend/tests/test_care_events.py`

**Interfaces:**
- Consumes: Task 6 의 `member_service.actor_label`
- Produces: `CareEventResponse.actor: ActorOut | None`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
async def test_actor_shows_nickname_for_current_member(store, pet):
    store.pet_members.append((pet.id, CARER))
    store.app_users[CARER_KAKAO].nickname = "아빠"
    client_as(CARER).post("/app/care-events", json={
        "pet_id": str(pet.id), "kind": "meal",
        "occurred_at": "2026-09-09T08:12:00+09:00",
        "client_event_id": str(uuid.uuid4()),
    })
    got = client_as(OWNER).get(f"/app/care-events/today?pet_id={pet.id}&day=2026-09-09").json()
    assert got["events"][0]["actor"]["nickname"] == "아빠"


async def test_actor_hides_nickname_after_leaving(store, pet):
    """나간 사람의 이름은 안 낸다. 재가입해도 다시 초대받기 전엔 그대로다."""
    store.pet_members.append((pet.id, CARER))
    store.app_users[CARER_KAKAO].nickname = "아빠"
    client_as(CARER).post("/app/care-events", json={
        "pet_id": str(pet.id), "kind": "meal",
        "occurred_at": "2026-09-09T08:12:00+09:00",
        "client_event_id": str(uuid.uuid4()),
    })
    client_as(OWNER).delete(f"/app/pets/{pet.id}/members/{CARER}")

    got = client_as(OWNER).get(f"/app/care-events/today?pet_id={pet.id}&day=2026-09-09").json()
    assert got["events"][0]["actor"]["nickname"] is None
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd backend && uv run pytest tests/test_care_events.py -v -k actor`
Expected: FAIL — `KeyError: 'actor'`.

- [ ] **Step 3: 스키마에 더한다**

```python
class ActorOut(BaseModel):
    """누가 챙겼나. **`nickname` 은 지금도 구성원일 때만 옵니다** (docs/co-care.md §3)."""

    app_user_id: uuid.UUID | None
    nickname: str | None


class CareEventResponse(BaseModel):
    ...
    #: 없을 수 있습니다 — 이 컬럼보다 먼저 쌓인 기록과 탈퇴한 보호자의 기록입니다.
    actor: ActorOut | None = None
```

`__all__` 에 `"ActorOut"` 을 더한다.

- [ ] **Step 4: 라우터의 `_to_response` 가 actor 를 채우게 한다**

`_to_response` 는 동기 함수라 `actor_label` 을 못 부른다. 라벨을 **미리 한 번에** 만들어 넘긴다 — 이벤트마다 부르면 목록 길이만큼 왕복한다.

```python
async def _actor_labels(session, events: list[CareEvent]) -> dict[uuid.UUID, str | None]:
    """actor id → 표시 이름. 없는 사람과 나간 사람은 None 입니다."""
    ids = {e.actor_app_user_id for e in events if e.actor_app_user_id}
    if not ids:
        return {}
    pet_id = events[0].pet_id
    return {
        uid: await member_service.actor_label(session, pet_id, uid) for uid in ids
    }


def _to_response(event: CareEvent, labels: dict) -> CareEventResponse:
    return CareEventResponse(
        ...,
        actor=ActorOut(
            app_user_id=event.actor_app_user_id,
            nickname=labels.get(event.actor_app_user_id),
        ),
    )
```

`list_events` · `today` · `record_event` 세 곳의 `_to_response(...)` 호출을 고친다.

- [ ] **Step 5: 테스트가 통과하는지 본다**

Run: `cd backend && uv run pytest tests/test_care_events.py -v`
Expected: 전부 PASS.

- [ ] **Step 6: 커밋**

```bash
git add backend/src/daengs_backend/ backend/tests/
git commit -m "feat: 케어 로그에 actor — 지금도 구성원일 때만 이름을 낸다

탈퇴자는 재가입하면 같은 행이 살아난다. 이 규칙이 없으면 옛 기록이 어느 날
갑자기 남의 현재 닉네임으로 뜬다."
```

---

## Task 11: 문서 갱신과 전체 검증

**Files:**
- Modify: `docs/care-events.md`, `docs/decisions.md`
- Test: 전체 스위트

- [ ] **Step 1: `docs/care-events.md` 를 고친다**

이 문서가 지금 거짓이 된 곳이 셋이다.

- "내 강아지가 아니면 404" → 구성원 기준으로 바뀌었다
- `app_user_id` 컬럼 설명 → `actor_app_user_id`
- 하루 요약의 산책 수 → 소유자가 아니라 강아지 기준

각 자리에 `docs/co-care.md` 로 가는 링크를 건다.

- [ ] **Step 2: `docs/decisions.md` 에 결정을 남긴다**

되돌리기 번거로운 결정이 둘이다. 기존 `D-` 번호 다음 번호를 쓰고, 문서 상단 표에도 줄을 더한다.

- **돌봄은 강아지 것, 성취는 사람 것** — `care_events`·`walks` 는 강아지에, `territory_claims`·`dog_cards` 는 사람에. 되돌리려면 소유 컬럼을 다시 옮겨야 한다.
- **탈퇴 정리를 트리거로 한다** — `app_users` 가 안 지워져 FK 가 안 도는 것에 대한 답. `21_activity_game.sql` 의 선례를 따랐다.

- [ ] **Step 3: 전체 스위트를 돌린다**

Run: `cd backend && uv run pytest -q`
Expected: PASS (약 6분). 층 2 는 로컬 postgres 가 없으면 SKIP 이고, 그때는 CI 가 본다.

- [ ] **Step 4: 마이그레이션을 두 번 돌려 본다**

Run: `psql "postgresql://postgres:postgres@127.0.0.1:5432/vectordb" -f db/migrations/2026-09-09_pet_members.sql` 을 **두 번**
Expected: 두 번 다 에러 없음. 실패하면 Task 1 Step 4 의 가드가 부족한 것이다.

- [ ] **Step 5: 커밋하고 PR 을 연다**

```bash
git add docs/
git commit -m "docs: 공동 돌봄으로 거짓이 된 문장을 고치고 결정을 남긴다"
```

PR 본문에는 `## 작업 목록` 을 두고 이 계획의 Task 1~11 을 체크리스트로 옮긴다 (`docs/collaboration.md` 규칙).

---

## 자체 검토 기록

**스펙 대응** — §1 → Task 1, §2 → Task 3·4, §3 → Task 5·6·7·8, §4 → Task 9·10, §5 → Task 1·2 + 각 Task 의 테스트. **모든 절에 대응 Task 가 있다.**

**Task 7 에서 스펙을 하나 정정했다.** 스펙 §3 은 승계의 상한 검사를 "미니룸 상한(`count_accessible`)" 이라고 적었는데, 승계는 방에 선 아이 수를 안 바꾸므로 그 검사가 **항상 통과한다.** 승계가 늘리는 것은 소유뿐이라 `count_for_owner` 로 봐야 한다. 계획은 그렇게 썼다 — 실행 후 `docs/co-care.md` §3 의 그 줄도 같이 고친다.

**남은 판단 자리** — Task 4 Step 4 의 "조회 셋 / 쓰기 그대로" 는 `services/pet.py` 의 실제 함수 이름을 보고 실행자가 가른다. 기준은 **"되돌리기 어려운가"** 이고, 헷갈리면 `get_owned` 쪽(안전한 실패)이다.
